---
phase: 02-mcp-basic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/com.shtl.mcp-unity/Editor~/Server/package.json
  - Packages/com.shtl.mcp-unity/Editor~/Server/tsconfig.json
  - Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts
autonomous: true
requirements:
  - MCP-01
  - MCP-02

must_haves:
  truths:
    - "npm install && npm run build завершается без ошибок в Editor~/Server/"
    - "node dist/index.js запускается и не падает при EOF на stdin"
    - "Сервер объявляет инструменты compile, play, stop, list_scenes, open_scene, import_asset"
  artifacts:
    - path: "Packages/com.shtl.mcp-unity/Editor~/Server/package.json"
      provides: "npm конфигурация с build:tsc и зависимостями"
      contains: '"build": "tsc"'
    - path: "Packages/com.shtl.mcp-unity/Editor~/Server/tsconfig.json"
      provides: "TypeScript конфигурация (ES2022, Node16, outDir: dist)"
    - path: "Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts"
      provides: "Точка входа MCP-сервера с регистрацией всех 6 инструментов и stdio-транспортом"
  key_links:
    - from: "src/index.ts"
      to: "@modelcontextprotocol/sdk"
      via: "import McpServer, StdioServerTransport"
      pattern: "McpServer.*StdioServerTransport"
    - from: "src/index.ts"
      to: "http://localhost:8765/"
      via: "fetch() в каждом tool handler"
      pattern: "fetch.*localhost:8765"
---

<objective>
Создать TypeScript scaffold для MCP-сервера: package.json, tsconfig.json, и src/index.ts с полной регистрацией всех 6 инструментов через MCP SDK. Каждый инструмент проксирует запрос к C# bridge на localhost:8765.

Purpose: Создать компилируемую и запускаемую точку входа для MCP-сервера, от которой зависят планы 02 и 03.
Output: Editor~/Server/ с работающим `npm run build` и заглушками-хендлерами для всех tools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mcp-basic/2-CONTEXT.md
@.planning/phases/02-mcp-basic/2-RESEARCH.md

<interfaces>
<!-- Паттерн MCP SDK из RESEARCH.md — использовать точно такой же -->

```typescript
// Импорт (пути точно такие, с .js расширением — Node16 module resolution)
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import { z } from "zod";

// Регистрация инструмента без параметров:
server.tool("compile", "описание", {}, async () => { ... });

// Регистрация инструмента с параметром:
server.tool("open_scene", "описание",
  { path: z.string().describe("...") },
  async ({ path }) => { ... }
);

// Подключение транспорта (в конце файла):
const transport = new StdioServerTransport();
await server.connect(transport);
```

Формат ответа из каждого tool handler (per D-10):
```typescript
return { content: [{ type: "text", text: JSON.stringify(data) }] };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Создать package.json и tsconfig.json для TypeScript-сервера</name>
  <files>
    Packages/com.shtl.mcp-unity/Editor~/Server/package.json
    Packages/com.shtl.mcp-unity/Editor~/Server/tsconfig.json
  </files>
  <read_first>
    - Packages/com.shtl.mcp-unity/package.json (понять версию и структуру пакета)
    - .planning/phases/02-mcp-basic/2-CONTEXT.md (D-02, D-03 — locked decisions)
    - .planning/phases/02-mcp-basic/2-RESEARCH.md (Standard Stack с версиями)
  </read_first>
  <action>
Создать `Packages/com.shtl.mcp-unity/Editor~/Server/package.json`:
```json
{
  "name": "mcp-unity-server",
  "version": "0.1.0",
  "description": "TypeScript MCP server for Unity Editor integration",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.28.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
```

Создать `Packages/com.shtl.mcp-unity/Editor~/Server/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

Затем запустить `npm install` в `Packages/com.shtl.mcp-unity/Editor~/Server/`.
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor~/Server && grep -q '"build": "tsc"' package.json && grep -q '"module": "Node16"' tsconfig.json && ls node_modules/@modelcontextprotocol/sdk 2>/dev/null && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - package.json содержит `"build": "tsc"` (per D-02)
    - package.json содержит `"@modelcontextprotocol/sdk": "1.28.0"` (per D-03)
    - package.json содержит `"zod": "^3.24.0"` (per D-03)
    - tsconfig.json содержит `"module": "Node16"` (для ESM + Node resolution)
    - tsconfig.json содержит `"outDir": "dist"` (per D-02)
    - node_modules/@modelcontextprotocol/sdk существует после npm install
  </acceptance_criteria>
  <done>package.json и tsconfig.json созданы, npm install завершён без ошибок</done>
</task>

<task type="auto">
  <name>Задача 2: Создать src/index.ts — точка входа MCP-сервера со всеми 6 инструментами</name>
  <files>
    Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts
  </files>
  <read_first>
    - Packages/com.shtl.mcp-unity/Editor~/Server/package.json (убедиться что создан в Задаче 1)
    - Packages/com.shtl.mcp-unity/Editor~/Server/tsconfig.json (убедиться что создан в Задаче 1)
    - .planning/phases/02-mcp-basic/2-CONTEXT.md (D-01, D-04, D-10..D-14 — форматы ответов)
    - .planning/phases/02-mcp-basic/2-RESEARCH.md (Pattern 1 — точный шаблон TypeScript)
  </read_first>
  <action>
Создать директорию `src/` и файл `Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts`.

Файл должен:
1. Импортировать `McpServer` из `"@modelcontextprotocol/sdk/server/mcp.js"`
2. Импортировать `StdioServerTransport` из `"@modelcontextprotocol/sdk/server/stdio.js"`
3. Импортировать `z` из `"zod"`
4. Создать `const server = new McpServer({ name: "mcp-unity", version: "0.1.0" })`
5. Зарегистрировать 6 инструментов через `server.tool(...)`:

**compile** (нет параметров):
```typescript
server.tool("compile", "Compile Unity scripts via AssetDatabase.Refresh() and return compilation status", {}, async () => {
  const response = await fetch("http://localhost:8765/compile", {
    method: "POST",
    signal: AbortSignal.timeout(5000),
  });
  const data = await response.json();
  return { content: [{ type: "text", text: JSON.stringify(data) }] };
});
```

**play** (нет параметров):
```typescript
server.tool("play", "Enter Unity Editor Play Mode (EditorApplication.isPlaying = true)", {}, async () => {
  const response = await fetch("http://localhost:8765/play", {
    method: "POST",
    signal: AbortSignal.timeout(5000),
  });
  const data = await response.json();
  return { content: [{ type: "text", text: JSON.stringify(data) }] };
});
```

**stop** (нет параметров):
```typescript
server.tool("stop", "Exit Unity Editor Play Mode (EditorApplication.isPlaying = false)", {}, async () => {
  const response = await fetch("http://localhost:8765/stop", {
    method: "POST",
    signal: AbortSignal.timeout(5000),
  });
  const data = await response.json();
  return { content: [{ type: "text", text: JSON.stringify(data) }] };
});
```

**list_scenes** (нет параметров):
```typescript
server.tool("list_scenes", "List all Unity scene files (.unity) in the project via AssetDatabase", {}, async () => {
  const response = await fetch("http://localhost:8765/list_scenes", {
    method: "POST",
    signal: AbortSignal.timeout(5000),
  });
  const data = await response.json();
  return { content: [{ type: "text", text: JSON.stringify(data) }] };
});
```

**open_scene** (параметр `path: string`):
```typescript
server.tool(
  "open_scene",
  "Open a Unity scene by asset path (e.g. Assets/Scenes/Main.unity)",
  { path: z.string().describe("Scene asset path, e.g. Assets/Scenes/Main.unity") },
  async ({ path }) => {
    const response = await fetch("http://localhost:8765/open_scene", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ path }),
      signal: AbortSignal.timeout(5000),
    });
    const data = await response.json();
    return { content: [{ type: "text", text: JSON.stringify(data) }] };
  }
);
```

**import_asset** (параметр `path: string`):
```typescript
server.tool(
  "import_asset",
  "Force reimport an asset via AssetDatabase.ImportAsset(path)",
  { path: z.string().describe("Asset path relative to project root, e.g. Assets/Textures/sprite.png") },
  async ({ path }) => {
    const response = await fetch("http://localhost:8765/import_asset", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ path }),
      signal: AbortSignal.timeout(5000),
    });
    const data = await response.json();
    return { content: [{ type: "text", text: JSON.stringify(data) }] };
  }
);
```

6. В конце файла подключить транспорт:
```typescript
const transport = new StdioServerTransport();
await server.connect(transport);
```

После создания файла запустить сборку:
```bash
cd Packages/com.shtl.mcp-unity/Editor~/Server && npm run build
```
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor~/Server && npm run build && ls dist/index.js && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - Файл src/index.ts существует
    - src/index.ts содержит `McpServer` импорт из `"@modelcontextprotocol/sdk/server/mcp.js"`
    - src/index.ts содержит `StdioServerTransport` из `"@modelcontextprotocol/sdk/server/stdio.js"`
    - src/index.ts содержит регистрацию всех 6 инструментов: compile, play, stop, list_scenes, open_scene, import_asset
    - src/index.ts содержит `fetch("http://localhost:8765/` (6 раз, по одному на каждый tool)
    - src/index.ts содержит `AbortSignal.timeout(5000)` в каждом fetch-вызове
    - src/index.ts содержит `await server.connect(transport)` в конце
    - `npm run build` завершается с кодом 0
    - `dist/index.js` существует после сборки
    - `node dist/index.js < /dev/null` завершается без stack trace (может завершиться с ненулевым кодом — это ок для EOF)
  </acceptance_criteria>
  <done>TypeScript сервер компилируется без ошибок, dist/index.js создан с регистрацией всех 6 MCP tools</done>
</task>

</tasks>

<verification>
```bash
cd Packages/com.shtl.mcp-unity/Editor~/Server
npm run build
# Ожидаем: exit 0, dist/index.js создан

node dist/index.js < /dev/null
# Ожидаем: нет TypeError/SyntaxError, нет crash при старте
```
</verification>

<success_criteria>
- `npm run build` в Editor~/Server/ завершается exit 0
- dist/index.js существует и содержит все 6 зарегистрированных инструментов
- TypeScript компилирует без ошибок (strict mode)
</success_criteria>

<output>
После выполнения создать `.planning/phases/02-mcp-basic/02-mcp-basic-01-SUMMARY.md`
</output>
