---
phase: 02-mcp-basic
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/com.shtl.mcp-unity/Runtime/McpUnityRuntime.asmdef
  - Packages/com.shtl.mcp-unity/Editor~/Server/mcp.json
  - Packages/com.shtl.mcp-unity/Editor~/Server/README.md
  - .mcp.json
autonomous: true
requirements:
  - MCP-01
  - MCP-12
  - MCP-13

must_haves:
  truths:
    - ".mcp.json существует в корне проекта и содержит валидный JSON с mcp-unity сервером"
    - "Путь в .mcp.json указывает на Packages/com.shtl.mcp-unity/Editor~/Server/dist/index.js"
    - "McpUnityRuntime.asmdef существует в Runtime/ — пакет имеет два asmdef (MCP-01)"
    - "README.md содержит инструкции npm install, npm run build, регистрация в Claude Code"
  artifacts:
    - path: ".mcp.json"
      provides: "Регистрация MCP-сервера в Claude Code"
      contains: '"mcp-unity"'
    - path: "Packages/com.shtl.mcp-unity/Runtime/McpUnityRuntime.asmdef"
      provides: "Runtime assembly definition (пустой, для MCP-01)"
    - path: "Packages/com.shtl.mcp-unity/Editor~/Server/mcp.json"
      provides: "MCP конфиг внутри пакета (reference copy)"
    - path: "Packages/com.shtl.mcp-unity/Editor~/Server/README.md"
      provides: "Инструкция по установке и запуску"
  key_links:
    - from: ".mcp.json"
      to: "Packages/com.shtl.mcp-unity/Editor~/Server/dist/index.js"
      via: "args в mcpServers.mcp-unity"
      pattern: "dist/index.js"
---

<objective>
Создать конфигурационные файлы: .mcp.json для регистрации сервера в Claude Code, mcp.json внутри пакета, README.md с инструкцией, и McpUnityRuntime.asmdef чтобы пакет удовлетворял MCP-01 (два asmdef).

Purpose: Сделать MCP-сервер видимым для Claude Code и завершить структуру пакета com.shtl.mcp-unity.
Output: .mcp.json в корне проекта, файлы документации, Runtime asmdef.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mcp-basic/2-CONTEXT.md

<interfaces>
<!-- Точный формат .mcp.json из D-15 — не отклоняться -->
```json
{
  "mcpServers": {
    "mcp-unity": {
      "command": "node",
      "args": ["Packages/com.shtl.mcp-unity/Editor~/Server/dist/index.js"]
    }
  }
}
```

Путь в args — относительный от корня проекта (per D-16).
Claude Code auto-detect: `.mcp.json` в корне проекта.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Создать McpUnityRuntime.asmdef и .mcp.json</name>
  <files>
    Packages/com.shtl.mcp-unity/Runtime/McpUnityRuntime.asmdef
    .mcp.json
  </files>
  <read_first>
    - Packages/com.shtl.mcp-unity/package.json (имя пакета, версия)
    - .planning/phases/02-mcp-basic/2-CONTEXT.md (D-15, D-16 — locked decisions для .mcp.json)
  </read_first>
  <action>
1. Создать `Packages/com.shtl.mcp-unity/Runtime/McpUnityRuntime.asmdef`:
```json
{
  "name": "McpUnityRuntime",
  "rootNamespace": "Shtl.McpUnity.Runtime",
  "references": [],
  "includePlatforms": [],
  "excludePlatforms": [],
  "allowUnsafeCode": false,
  "overrideReferences": false,
  "precompiledReferences": [],
  "autoReferenced": true,
  "defineConstraints": [],
  "versionDefines": [],
  "noEngineReferences": false
}
```
Параметры:
- `"includePlatforms": []` — все платформы включая WebGL (Runtime-сборка)
- `"autoReferenced": true` — доступна игровым скриптам автоматически
- Пустое содержимое — Phase 8 добавит RuntimeBridgeProxy.cs в эту сборку

2. Создать `.mcp.json` в корне проекта (`/Users/selstrom/work/projects/asteroids-specs/.mcp.json`):
```json
{
  "mcpServers": {
    "mcp-unity": {
      "command": "node",
      "args": ["Packages/com.shtl.mcp-unity/Editor~/Server/dist/index.js"]
    }
  }
}
```
Путь к `dist/index.js` — относительный от корня проекта (per D-16).
  </action>
  <verify>
    <automated>python3 -m json.tool "/Users/selstrom/work/projects/asteroids-specs/.mcp.json" && grep -q "dist/index.js" "/Users/selstrom/work/projects/asteroids-specs/.mcp.json" && grep -q '"McpUnityRuntime"' "/Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Runtime/McpUnityRuntime.asmdef" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - .mcp.json существует в корне проекта (рядом с Assets/, Packages/, ProjectSettings/)
    - .mcp.json содержит `"mcp-unity"` ключ в mcpServers (per D-15)
    - .mcp.json содержит `"command": "node"` (per D-15)
    - .mcp.json содержит `"Packages/com.shtl.mcp-unity/Editor~/Server/dist/index.js"` в args (per D-15, D-16)
    - python3 -m json.tool .mcp.json завершается без ошибок (валидный JSON)
    - McpUnityRuntime.asmdef существует в Runtime/
    - McpUnityRuntime.asmdef содержит `"name": "McpUnityRuntime"`
    - McpUnityRuntime.asmdef содержит `"includePlatforms": []` (все платформы включая WebGL)
  </acceptance_criteria>
  <done>.mcp.json и McpUnityRuntime.asmdef созданы с корректным содержимым</done>
</task>

<task type="auto">
  <name>Задача 2: Создать mcp.json и README.md внутри Editor~/Server/</name>
  <files>
    Packages/com.shtl.mcp-unity/Editor~/Server/mcp.json
    Packages/com.shtl.mcp-unity/Editor~/Server/README.md
  </files>
  <read_first>
    - .mcp.json (создан в Задаче 1 — для сверки формата)
    - .planning/phases/02-mcp-basic/2-CONTEXT.md (D-15, D-16 для mcp.json; MCP-13 для README)
    - .planning/REQUIREMENTS.md §MCP-12, MCP-13 (точное описание требований)
  </read_first>
  <action>
1. Создать `Packages/com.shtl.mcp-unity/Editor~/Server/mcp.json` (reference copy для MCP-12):
```json
{
  "mcpServers": {
    "mcp-unity": {
      "command": "node",
      "args": ["Packages/com.shtl.mcp-unity/Editor~/Server/dist/index.js"]
    }
  }
}
```

2. Создать `Packages/com.shtl.mcp-unity/Editor~/Server/README.md`:

```markdown
# MCP Unity Server

TypeScript MCP-сервер для интеграции Unity Editor с Claude Code.

## Установка

1. Перейти в директорию сервера:
   ```bash
   cd Packages/com.shtl.mcp-unity/Editor~/Server
   ```

2. Установить зависимости:
   ```bash
   npm install
   ```

3. Скомпилировать TypeScript:
   ```bash
   npm run build
   ```

## Регистрация в Claude Code

Файл `.mcp.json` уже создан в корне проекта. Проверить:
```bash
claude mcp list
```

В списке должен появиться `mcp-unity`.

## Инструменты (MCP Tools)

| Tool | Описание |
|------|---------|
| `compile` | AssetDatabase.Refresh() + статус компиляции |
| `play` | Войти в Play Mode |
| `stop` | Выйти из Play Mode |
| `list_scenes` | Список .unity файлов в проекте |
| `open_scene` | Открыть сцену по пути (path: string) |
| `import_asset` | AssetDatabase.ImportAsset(path) |

## Требования

- Node.js 18+ (встроенный fetch)
- Unity 2022.3 LTS с открытым Editor
- C# bridge `McpUnityBridge.cs` компилируется при открытии Unity

## Bridge

C# часть (`Editor/McpUnityBridge.cs`) стартует автоматически при загрузке Unity Editor на `localhost:8765`. TypeScript-сервер проксирует MCP-вызовы к bridge по HTTP.
```
  </action>
  <verify>
    <automated>python3 -m json.tool "/Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor~/Server/mcp.json" && grep -q "npm install" "/Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor~/Server/README.md" && grep -q "npm run build" "/Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor~/Server/README.md" && grep -q "claude mcp list" "/Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor~/Server/README.md" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - mcp.json существует в Editor~/Server/ и содержит валидный JSON (per MCP-12)
    - mcp.json содержит `"command": "node"` и `"dist/index.js"` в args
    - README.md существует в Editor~/Server/ (per MCP-13)
    - README.md содержит команду `npm install`
    - README.md содержит команду `npm run build`
    - README.md содержит `claude mcp list` (инструкция по регистрации)
    - README.md содержит описание всех 6 инструментов: compile, play, stop, list_scenes, open_scene, import_asset
  </acceptance_criteria>
  <done>mcp.json и README.md созданы, пакет удовлетворяет MCP-12 и MCP-13</done>
</task>

</tasks>

<verification>
```bash
# Валидация JSON
python3 -m json.tool .mcp.json
python3 -m json.tool Packages/com.shtl.mcp-unity/Editor~/Server/mcp.json
python3 -m json.tool Packages/com.shtl.mcp-unity/Runtime/McpUnityRuntime.asmdef

# Проверка содержимого
grep "dist/index.js" .mcp.json
grep "npm install" Packages/com.shtl.mcp-unity/Editor~/Server/README.md
grep "claude mcp list" Packages/com.shtl.mcp-unity/Editor~/Server/README.md
```
</verification>

<success_criteria>
- .mcp.json в корне с корректным путём к dist/index.js (per D-15, D-16)
- McpUnityRuntime.asmdef создан в Runtime/ (пакет имеет два asmdef — MCP-01)
- mcp.json и README.md в Editor~/Server/ (MCP-12, MCP-13)
- Все JSON-файлы валидны
</success_criteria>

<output>
После выполнения создать `.planning/phases/02-mcp-basic/02-mcp-basic-03-SUMMARY.md`
</output>
