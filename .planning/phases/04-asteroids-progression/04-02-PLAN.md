---
phase: 04-asteroids-progression
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - Assets/Scripts/Application/Game.cs
autonomous: true
requirements: [AST-02, AST-03, AST-04, AST-08, AST-09, PROG-01, PROG-03, PROG-04, PROG-05, PROG-07, PROG-08]

must_haves:
  truths:
    - "Уничтожение Large астероида порождает 2 Medium, Medium → 2 Small, Small → исчезает"
    - "Первая волна спаунит 4 Large астероида вдали от корабля; каждая следующая +1 Large (макс 12)"
    - "Счёт в Model.Score растёт при уничтожении: Big+1, Medium+2, Small+3 (DATA_SCHEMA)"
    - "Игрок начинает с 3 жизнями; при 0 жизнях вызывается _onGameOver"
    - "Экстра-жизнь каждые 10 000 очков (макс 6 жизней)"
    - "После Game Over кнопка Play Again вызывает Game.Start() повторно и сбрасывает волну/счёт/жизни"
  artifacts:
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "Полная игровая логика: волны, дробление, счёт, жизни, Game Over, Play Again"
      contains: "StartWave"
    - path: "Assets/Scripts/Application/Game.cs"
      contains: "SpawnFragments"
    - path: "Assets/Scripts/Application/Game.cs"
      contains: "Lives"
  key_links:
    - from: "AsteroidVisual.OnCollisionEnter2D"
      to: "Game.OnAsteroidCollided"
      via: "AsteroidViewModel.OnCollision callback, назначается в CreateAsteroid"
    - from: "Game.OnEntityDestroyed(AsteroidModel)"
      to: "EntitiesCatalog.CreateAsteroid"
      via: "SpawnFragments вызывает _catalog.CreateAsteroid для осколков"
    - from: "Game.StartWave"
      to: "EntitiesCatalog.CreateAsteroid"
      via: "цикл for(int i=0; i<asteroidCount; i++) CreateAsteroid"
---

<objective>
Реализовать полную игровую логику Phase 4 в Game.cs: волновый спавн астероидов, логику дробления при уничтожении, систему счёта и жизней, Game Over и Play Again.

Purpose: Game.cs — сердце игровой сессии. Этот план превращает статичную демо-сцену в играбельный Asteroids с прогрессией.
Output: Game.cs с реализованными StartWave, SpawnFragments, OnAsteroidCollided, счётом, жизнями, Game Over.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-asteroids-progression/04-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/DATA_SCHEMA.md

<interfaces>
<!-- Ключевые интерфейсы из Plan 04-01 и существующего кода -->

Из Assets/Scripts/Application/EntitiesCatalog.cs (после Plan 04-01):
```csharp
// Новый метод из Plan 04-01:
public AsteroidModel CreateAsteroid(AsteroidData data, Vector2 position, Vector2 velocity);
// Существующие:
public ShipModel CreateShip();
public BulletModel CreateBullet(Vector2 position, Vector2 velocity, bool isEnemy = false);
public void Release(IGameEntityModel model);
public IGameEntityModel GetModelByGo(GameObject go);
public IEntityView GetViewByModel(IGameEntityModel model);
public ShipModel Ship { get; }
```

Из Assets/Scripts/Model/Entities/AsteroidModel.cs (после Plan 04-01):
```csharp
public class AsteroidModel : IGameEntityModel
{
    public MoveComponent Move { get; }
    public int Size { get; set; }           // 1=Small, 2=Medium, 3=Big
    public float AngularSpeed { get; set; }
}
```

Из Assets/Scripts/View/AsteroidVisual.cs (после Plan 04-01):
```csharp
public class AsteroidViewModel : AbstractViewModel
{
    public ReactiveValue<Vector2> Position;
    public ReactiveValue<Sprite> Sprite;
    public Action<Collision2D> OnCollision;
}
public class AsteroidVisual : AbstractWidgetView<AsteroidViewModel>, IEntityView { ... }
```

Из Assets/Scripts/Application/Game.cs (текущий):
```csharp
public class Game
{
    private GameData _configs;
    private EntitiesCatalog _catalog;
    private Model _model;
    private PlayerInput _input;
    private Action _onGameOver;
    private ShipModel _ship;
    private bool _isRunning;

    public void Connect(GameData configs, EntitiesCatalog catalog, Model model,
                        PlayerInput input, Action onGameOver);
    public void Start();    // подписка на ввод, CreateShip, старт OnEntityDestroyed
    public void Stop();
    public void Dispose();
    // private: OnRotate, OnThrust, OnAttack, OnLaser
    // private: OnUserGunShooting, OnUserLaserFired
    // private: OnShipCollided, OnEntityDestroyed, RespawnShip, StartBlink
}
```

Из Assets/Scripts/Model/Model.cs:
```csharp
public int Score { get; set; }
public ActionScheduler ActionScheduler { get; }
public event Action<IGameEntityModel> OnEntityDestroyed;
public void CleanUp();    // сбросить Score, очистить сущности и системы
```

Из Assets/Scripts/Utils/GameUtils.cs:
```csharp
public static Vector2 GetRandomPositionOutsideRadius(Vector2 center, float minRadius, Vector2 gameArea);
```

DATA_SCHEMA скорости (Claude's Discretion если не указаны):
- Large (Size=3): скорость 2 ед/с
- Medium (Size=2): скорость 3 ед/с
- Small (Size=1): скорость 4 ед/с

DATA_SCHEMA очки:
- AsteroidBig.Score = 1
- AsteroidMedium.Score = 2
- AsteroidSmall.Score = 3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Добавить поля волны/жизней/счёта и StartWave в Game.cs</name>
  <files>
    Assets/Scripts/Application/Game.cs
  </files>
  <read_first>
    - Assets/Scripts/Application/Game.cs — полный текущий код (обязательно перед правками)
    - Assets/Scripts/Application/EntitiesCatalog.cs — сигнатура CreateAsteroid из Plan 04-01
    - Assets/Scripts/Utils/GameUtils.cs — GetRandomPositionOutsideRadius сигнатура
    - .planning/codebase/DATA_SCHEMA.md — GameData.SpawnAllowedRadius=20, AsteroidInitialCount=10
    - .planning/phases/04-asteroids-progression/04-CONTEXT.md — D-14 (волны: start=4, +1, max=12), D-15 (волна сразу после последнего астероида), D-16 (SpawnAllowedRadius=20), D-17 (первая волна=4)
  </read_first>
  <action>
Добавить в класс Game приватные поля и метод StartWave. Не удалять существующий код.

**Новые приватные поля** (добавить к существующим `_ship`, `_isRunning` и т.д.):
```csharp
private int _waveNumber;                  // текущая волна (1, 2, 3...)
private int _lives;                       // текущие жизни
private int _highScore;                   // лучший счёт сессии
private int _asteroidCount;              // сколько астероидов живо
private int _nextBonusLifeScore;         // порог для следующей экстра-жизни
private Action<int, int> _onScoreChanged;  // (score, lives) → HUD
```

**Изменить Connect()** — добавить параметр `Action<int, int> onScoreChanged = null`:
```csharp
public void Connect(GameData configs, EntitiesCatalog catalog, Model model,
                    PlayerInput input, Action onGameOver, Action<int, int> onScoreChanged = null)
{
    _configs = configs;
    _catalog = catalog;
    _model = model;
    _input = input;
    _onGameOver = onGameOver;
    _onScoreChanged = onScoreChanged;
}
```

**Изменить Start()** — сбросить состояние и запустить волну:
```csharp
public void Start()
{
    _isRunning = true;
    _waveNumber = 0;
    _lives = 3;                 // PROG-03: начинаем с 3 жизней
    _nextBonusLifeScore = 10000; // PROG-04: первая экстра-жизнь на 10 000

    // Сбросить счёт модели
    _model.Score = 0;

    // Подписаться на ввод
    _input.OnRotateAction += OnRotate;
    _input.OnTrustAction  += OnThrust;
    _input.OnAttackAction += OnAttack;
    _input.OnLaserAction  += OnLaser;

    // Создать корабль
    _ship = _catalog.CreateShip();
    if (_catalog.GetViewByModel(_ship) is ShipVisual shipVisual)
    {
        shipVisual.ViewModel.OnCollision = OnShipCollided;
    }

    // Подписаться на уничтожение сущностей
    _model.OnEntityDestroyed += OnEntityDestroyed;

    // Подписаться на выстрел и лазер
    _ship.Gun.OnShooting = OnUserGunShooting;
    _ship.Laser.OnLaserFired = OnUserLaserFired;

    // Запустить первую волну
    StartWave();

    // Уведомить HUD
    _onScoreChanged?.Invoke(_model.Score, _lives);
}
```

**Добавить метод StartWave()**:
```csharp
private void StartWave()
{
    _waveNumber++;
    // D-14: первая волна = 4, каждая следующая +1, макс. 12
    var asteroidCount = Mathf.Min(3 + _waveNumber, 12); // wave 1 → 4, wave 2 → 5, ... wave 9+ → 12
    _asteroidCount = 0;

    var gameArea = _model.GetSystem<MoveSystem>().GameArea;
    var shipPos = _ship != null ? _ship.Move.Position.Value : Vector2.zero;

    for (var i = 0; i < asteroidCount; i++)
    {
        // D-16: спаун вдали от корабля (SpawnAllowedRadius=20 из GameData)
        var pos = GameUtils.GetRandomPositionOutsideRadius(
            shipPos, _configs.SpawnAllowedRadius, gameArea);

        // Случайное направление и скорость Large (2 ед/с)
        var dir = Random.insideUnitCircle.normalized;
        var velocity = dir * 2f; // Large: скорость 2 ед/с (Claude's Discretion)

        var asteroid = _catalog.CreateAsteroid(_configs.AsteroidBig, pos, velocity);
        BindAsteroidCollision(asteroid);
        _asteroidCount++;
    }
}
```

**Добавить вспомогательный метод BindAsteroidCollision()**:
```csharp
private void BindAsteroidCollision(AsteroidModel asteroid)
{
    if (_catalog.GetViewByModel(asteroid) is AsteroidVisual av)
    {
        av.ViewModel.OnCollision = col => OnAsteroidCollided(asteroid, col);
    }
}
```

Импорты вверху файла добавить если отсутствуют:
```csharp
using UnityEngine;
```
(уже есть в текущем файле — оставить)
  </action>
  <verify>
    <automated>grep -n "StartWave\|_waveNumber\|_lives\|_asteroidCount\|_nextBonusLifeScore\|BindAsteroidCollision" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs</automated>
  </verify>
  <acceptance_criteria>
    - Game.cs содержит `private int _waveNumber`
    - Game.cs содержит `private int _lives`
    - Game.cs содержит `private int _asteroidCount`
    - Game.cs содержит `private int _nextBonusLifeScore`
    - Game.cs содержит `private void StartWave()`
    - Game.cs содержит `Mathf.Min(3 + _waveNumber, 12)` в StartWave
    - Game.cs содержит `private void BindAsteroidCollision(AsteroidModel asteroid)`
    - Game.cs содержит `_lives = 3` в Start()
  </acceptance_criteria>
  <done>Game.cs имеет поля для прогрессии и метод StartWave, спаунящий Large астероиды первой волны.</done>
</task>

<task type="auto">
  <name>Задача 2: Дробление астероидов, счёт, жизни, Game Over и Play Again в Game.cs</name>
  <files>
    Assets/Scripts/Application/Game.cs
  </files>
  <read_first>
    - Assets/Scripts/Application/Game.cs — состояние после Задачи 1 (обязательно перечитать)
    - .planning/phases/04-asteroids-progression/04-CONTEXT.md — D-04 (Large→2×Medium, Medium→2×Small), D-05 (случайные направления осколков), D-09 (High Score в памяти), D-11 (Game Over экран — disabled Submit/Leaderboard), D-12 (Play Again сбрасывает всё), D-03 (экстра-жизнь каждые 10 000)
    - .planning/codebase/DATA_SCHEMA.md — AsteroidBig.Score=1, Medium.Score=2, Small.Score=3; скорости осколков
  </read_first>
  <action>
Добавить в Game.cs методы дробления, обработки коллизий астероидов, счёта, жизней, Game Over и Play Again.

**Метод OnAsteroidCollided** — вызывается из BindAsteroidCollision (callback от AsteroidVisual):
```csharp
private void OnAsteroidCollided(AsteroidModel asteroid, Collision2D col)
{
    if (!_isRunning || asteroid.IsDead()) { return; }

    // Начислить очки (D-01: DATA_SCHEMA значения)
    var data = GetAsteroidData(asteroid.Size);
    _model.Score += data.Score; // Big=1, Medium=2, Small=3

    // Проверить экстра-жизнь (PROG-04: каждые 10 000 очков, макс. 6)
    while (_model.Score >= _nextBonusLifeScore && _lives < 6)
    {
        _lives++;
        _nextBonusLifeScore += 10000;
    }

    // Уничтожить астероид
    asteroid.Kill();

    // Уведомить HUD
    _onScoreChanged?.Invoke(_model.Score, _lives);
}
```

**Метод GetAsteroidData** — вернуть AsteroidData по size:
```csharp
private AsteroidData GetAsteroidData(int size)
{
    return size switch {
        3 => _configs.AsteroidBig,
        2 => _configs.AsteroidMedium,
        _ => _configs.AsteroidSmall
    };
}
```

**Метод SpawnFragments** — вызывается из OnEntityDestroyed при уничтожении астероида:
```csharp
private void SpawnFragments(AsteroidModel asteroid)
{
    // D-04: Large(3)→2 Medium(2), Medium(2)→2 Small(1), Small(1)→исчезает
    var childSize = asteroid.Size - 1;
    if (childSize <= 0) { return; } // Small — исчезает, осколков нет

    var childData = GetAsteroidData(childSize);
    // Скорости: Small=4, Medium=3 (осколки быстрее родителя, AST-06)
    var childSpeed = childSize == 1 ? 4f : 3f;
    var parentPos = asteroid.Move.Position.Value;

    for (var i = 0; i < 2; i++)
    {
        // D-05: случайные направления осколков
        var dir = Random.insideUnitCircle.normalized;
        var vel = dir * childSpeed;
        var child = _catalog.CreateAsteroid(childData, parentPos, vel);
        BindAsteroidCollision(child);
        _asteroidCount++;
    }
}
```

**Расширить OnEntityDestroyed** — добавить обработку AsteroidModel рядом с BulletModel:
```csharp
private void OnEntityDestroyed(IGameEntityModel model)
{
    if (model is BulletModel bullet)
    {
        if (bullet.Gun != null) { bullet.Gun.CurrentShoots--; }
    }

    if (model is AsteroidModel asteroid)
    {
        // Дробление (D-04, D-05)
        SpawnFragments(asteroid);
        _asteroidCount--;

        // Следующая волна когда все астероиды уничтожены (D-15)
        if (_asteroidCount <= 0 && _isRunning)
        {
            StartWave();
        }
    }

    if (model is ShipModel)
    {
        _lives--;

        if (_lives <= 0)
        {
            // Game Over (PROG-05)
            _isRunning = false;

            // Обновить High Score сессии (D-09, PROG-07)
            if (_model.Score > _highScore)
            {
                _highScore = _model.Score;
            }

            _onGameOver?.Invoke();
        }
        else
        {
            // Respawn через 2 секунды (SHIP-06)
            _model.ActionScheduler.Schedule(2f, RespawnShip);
        }

        // Уведомить HUD
        _onScoreChanged?.Invoke(_model.Score, _lives);
    }

    _catalog.Release(model);
}
```

**Примечание:** в существующем `OnEntityDestroyed` уже есть блок `if (model is ShipModel)` с вызовом Schedule(2f, RespawnShip). Его нужно ЗАМЕНИТЬ на новый блок выше — добавить `_lives--` и логику Game Over перед расписыванием respawn.

**Добавить публичный метод Restart()** (для Play Again, PROG-08, D-12):
```csharp
public void Restart()
{
    // Остановить текущую игру без сброса High Score
    _isRunning = false;
    _input.OnRotateAction -= OnRotate;
    _input.OnTrustAction  -= OnThrust;
    _input.OnAttackAction -= OnAttack;
    _input.OnLaserAction  -= OnLaser;
    _model.OnEntityDestroyed -= OnEntityDestroyed;

    // Очистить все сущности и системы (включает Score=0)
    _model.CleanUp();
    _catalog.Dispose();
    // Переинициализировать catalog (pool остаётся, prefabs перерегистрировать)
    _catalog.Connect(_configs, null, null); // ВНИМАНИЕ: см. ниже

    // Запустить заново (Start() сбрасывает волну/жизни/счёт)
    Start();
}
```

**ВНИМАНИЕ по Restart():** `EntitiesCatalog.Dispose()` очищает словари, но `GameObjectPool` и `ModelFactory` продолжают работать. Вместо вызова `_catalog.Dispose()` + переинициализации, лучше добавить метод `EntitiesCatalog.Reset()` который только очищает словари, не трогая pool и prefabRegistry. Однако если это сложно — упрощённый вариант: вместо Restart() вызывать Start() напрямую (Application.cs управляет пересозданием). **Реализуй упрощённый вариант:**

```csharp
public void Restart()
{
    // Сбросить флаги для безопасного повторного Start()
    _isRunning = false;
    Stop(); // отписаться от ввода и событий

    // Очистить model (Score=0, убрать все сущности из систем)
    _model.CleanUp();

    // Снова запустить (Start() подпишет ввод, создаст корабль, запустит волну)
    Start();
}
```

**Добавить публичные свойства** для чтения состояния (Application нужны для передачи в UI):
```csharp
public int Score => _model.Score;
public int Lives => _lives;
public int HighScore => _highScore;
public int WaveNumber => _waveNumber;
```
  </action>
  <verify>
    <automated>grep -n "SpawnFragments\|OnAsteroidCollided\|GetAsteroidData\|public void Restart\|public int Lives\|public int HighScore\|_highScore\|_onScoreChanged" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs</automated>
  </verify>
  <acceptance_criteria>
    - Game.cs содержит `private void SpawnFragments(AsteroidModel asteroid)`
    - Game.cs содержит `private void OnAsteroidCollided(AsteroidModel asteroid, Collision2D col)`
    - Game.cs содержит `private AsteroidData GetAsteroidData(int size)`
    - Game.cs содержит `public void Restart()`
    - Game.cs содержит `public int Lives => _lives`
    - Game.cs содержит `public int HighScore => _highScore`
    - Game.cs содержит `_lives--` в OnEntityDestroyed
    - Game.cs содержит `if (_asteroidCount <= 0 && _isRunning)` → StartWave
    - Game.cs содержит `if (_model.Score > _highScore)` в логике Game Over
    - Game.cs содержит `_lives < 6` в условии экстра-жизни
  </acceptance_criteria>
  <done>Game.cs содержит полный игровой цикл: дробление астероидов, волновый прогресс, счёт с экстра-жизнями, Game Over, Play Again (Restart).</done>
</task>

</tasks>

<verification>
После выполнения обоих задач:
1. `grep -c "StartWave\|SpawnFragments\|OnAsteroidCollided\|Restart\|HighScore" Assets/Scripts/Application/Game.cs` — все 5 паттернов найдены (count > 0)
2. MCP compile — проект собирается без ошибок
3. Проверить: в Start() вызывается StartWave() и `_model.OnEntityDestroyed += OnEntityDestroyed`
4. Проверить: OnEntityDestroyed обрабатывает BulletModel, AsteroidModel, ShipModel
</verification>

<success_criteria>
- StartWave() спаунит 4 Large на первой волне, +1 каждую волну, макс. 12
- SpawnFragments() создаёт 2 Medium из Large, 2 Small из Medium, ничего из Small
- OnAsteroidCollided() начисляет очки по DATA_SCHEMA (1/2/3) и уведомляет HUD через _onScoreChanged
- Lives убывает при гибели корабля; при 0 — вызов _onGameOver и _isRunning=false
- Restart() очищает model, сбрасывает счёт/волну/жизни и перезапускает игру без перезапуска приложения
- HighScore обновляется при Game Over и сохраняется между Restart()'ами
- Проект компилируется
</success_criteria>

<output>
После завершения создать `.planning/phases/04-asteroids-progression/04-02-SUMMARY.md`
</output>
