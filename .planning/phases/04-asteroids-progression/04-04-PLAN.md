---
phase: 04-asteroids-progression
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - Assets/Scripts/Application/Application.cs
  - Assets/Scripts/Application/ApplicationEntry.cs
  - Assets/Scripts/Application/Screens/GameScreen.cs
autonomous: false
requirements: [AST-01, AST-09, AST-10, PROG-05, PROG-08]

must_haves:
  truths:
    - "При нажатии Play в TitleScreen начинается игра с астероидами и HUD"
    - "HUD показывает Score=0, 3 иконки жизней при старте"
    - "При гибели последней жизни появляется Game Over экран с Score и HighScore"
    - "Кнопка Play Again скрывает GameOver, очищает модель и запускает новую игру"
    - "Asteroid prefabs трёх размеров существуют с Rigidbody2D(Kinematic), CircleCollider2D, AsteroidVisual, Layer=8"
    - "GameData.asset содержит ссылки на AsteroidBig/Medium/Small assets"
  artifacts:
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "Application wire-up с GameOverScreen, Game.Restart(), onScoreChanged callback"
      contains: "GameOverScreen"
    - path: "Assets/Scripts/Application/ApplicationEntry.cs"
      provides: "ApplicationEntry с [SerializeField] GameOverView _gameOverView"
      contains: "_gameOverView"
    - path: "Assets/Media/prefabs/asteroid_big.prefab"
      provides: "Prefab большого астероида"
    - path: "Assets/Media/prefabs/asteroid_medium.prefab"
      provides: "Prefab среднего астероида"
    - path: "Assets/Media/prefabs/asteroid_small.prefab"
      provides: "Prefab малого астероида"
    - path: "Assets/Media/configs/AsteroidBigData.asset"
      provides: "AsteroidData со Score=1, Prefab=asteroid_big.prefab, SpriteVariants"
    - path: "Assets/Media/configs/AsteroidMediumData.asset"
      provides: "AsteroidData со Score=2, Prefab=asteroid_medium.prefab"
    - path: "Assets/Media/configs/AsteroidSmallData.asset"
      provides: "AsteroidData со Score=3, Prefab=asteroid_small.prefab"
  key_links:
    - from: "ApplicationEntry._gameOverView"
      to: "GameOverScreen.Connect"
      via: "Application.Connect принимает gameOverView и создаёт GameOverScreen"
    - from: "Game._onGameOver"
      to: "GameOverScreen.Show(score, highScore)"
      via: "Application.OnGameOver вызывает _gameOverScreen.Show(_game.Score, _game.HighScore)"
    - from: "GameOverView.OnPlayAgainClicked"
      to: "Game.Restart()"
      via: "Application.OnPlayAgain скрывает GameOverScreen и вызывает _game.Restart()"
---

<objective>
Подключить все компоненты Phase 4 в Application слой и создать Unity assets (prefabs + configs). Интеграция завершает полный игровой цикл.

Purpose: Plans 01-03 создали компоненты изолированно. Этот план соединяет их в рабочую игру и создаёт Unity-ассеты через MCP инструменты.
Output: Application.cs (расширен), ApplicationEntry.cs (расширен), 3 prefabs астероидов, 3 asset конфигов, GameData.asset обновлён, сцена Main.unity обновлена.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-asteroids-progression/04-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/DATA_SCHEMA.md
@.planning/codebase/UNITY_CONFIG.md

<interfaces>
<!-- Все компоненты из предыдущих планов -->

Из Application.cs (текущий — перечитать обязательно):
```csharp
public void Connect(IApplicationComponent entry, GameData configs, HudVisual hudVisual,
    TitleScreenView titleScreenView, GameObject titleScreenGo, GameObject hudGo)
private void OnGameStart()    // скрывает TitleScreen, показывает HUD, запускает _game.Start()
private void OnGameOver()     // сейчас только Debug.Log
```

Из ApplicationEntry.cs (текущий — перечитать обязательно):
```csharp
[SerializeField] private GameData _configs;
[SerializeField] private HudVisual _hudVisual;
[SerializeField] private TitleScreenView _titleScreenView;
[SerializeField] private GameObject _titleScreenGo;
[SerializeField] private GameObject _hudGo;
```

Из Game.cs (после Plan 04-02):
```csharp
public void Connect(GameData configs, EntitiesCatalog catalog, Model model,
                    PlayerInput input, Action onGameOver, Action<int, int> onScoreChanged = null);
public void Start();
public void Restart();
public int Score => _model.Score;
public int Lives => _lives;
public int HighScore => _highScore;
public int WaveNumber => _waveNumber;
```

Из GameScreen.cs (после Plan 04-03):
```csharp
public void Connect(HudVisual hudVisual, ShipViewModel shipVm);
public void UpdateHud(int score, int lives, int highScore);
```

Из GameOverScreen.cs (Plan 04-03):
```csharp
public void Connect(GameOverView view, Action onPlayAgain);
public void Show(int finalScore, int highScore);
public void Hide();
```

DATA_SCHEMA значения для assets:
- AsteroidBigData.asset: Score=1, Prefab=asteroid_big.prefab, SpriteVariants=[3 спрайта big]
- AsteroidMediumData.asset: Score=2, Prefab=asteroid_medium.prefab, SpriteVariants=[3 спрайта medium]
- AsteroidSmallData.asset: Score=3, Prefab=asteroid_small.prefab, SpriteVariants=[3 спрайта small]

UNITY_CONFIG.md:
- Layer 8 = "Asteroid" (имя слоя для prefab)
- Gravity: всегда GravityScale=0 в Rigidbody2D
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Подключить GameOverScreen в Application.cs и ApplicationEntry.cs</name>
  <files>
    Assets/Scripts/Application/Application.cs
    Assets/Scripts/Application/ApplicationEntry.cs
    Assets/Scripts/Application/Screens/GameScreen.cs
  </files>
  <read_first>
    - Assets/Scripts/Application/Application.cs — полный текущий код (обязательно)
    - Assets/Scripts/Application/ApplicationEntry.cs — полный текущий код (обязательно)
    - Assets/Scripts/Application/Screens/GameScreen.cs — состояние после Plan 04-03
    - Assets/Scripts/Application/Screens/GameOverScreen.cs — новый файл из Plan 04-03
    - Assets/Scripts/View/GameOverView.cs — новый файл из Plan 04-03
    - .planning/phases/04-asteroids-progression/04-CONTEXT.md — D-12 (Restart), D-13 (GameOverScreen — отдельный Canvas объект)
  </read_first>
  <action>
**Application.cs** — расширить для поддержки GameOver экрана:

Добавить новые приватные поля:
```csharp
private GameOverView _gameOverView;
private GameObject _gameOverGo;
private GameOverScreen _gameOverScreen;
private GameScreen _gameScreen;
```

**Изменить сигнатуру Connect()** — добавить параметры для GameOver:
```csharp
public void Connect(IApplicationComponent entry, GameData configs, HudVisual hudVisual,
    TitleScreenView titleScreenView, GameObject titleScreenGo, GameObject hudGo,
    GameOverView gameOverView, GameObject gameOverGo)
{
    _entry = entry;
    _configs = configs;
    _hudVisual = hudVisual;
    _titleScreenView = titleScreenView;
    _titleScreenGo = titleScreenGo;
    _hudGo = hudGo;
    _gameOverView = gameOverView;
    _gameOverGo = gameOverGo;

    _entry.OnUpdate += OnUpdate;

    // TitleScreen
    var titleScreen = new TitleScreen();
    titleScreen.Connect(_titleScreenView, OnGameStart);

    // GameOver экран — скрыт при старте
    if (_gameOverGo != null) { _gameOverGo.SetActive(false); }
    _gameOverScreen = new GameOverScreen();
    if (_gameOverView != null)
    {
        _gameOverScreen.Connect(_gameOverView, OnPlayAgain);
    }
}
```

**Изменить Start()** — создать GameScreen:
После строки `_game.Connect(...)` добавить:
```csharp
_gameScreen = new GameScreen();
```

**Изменить OnGameStart()** — инициализировать HUD и передать callback счёта:
```csharp
private void OnGameStart()
{
    if (_titleScreenGo != null) { _titleScreenGo.SetActive(false); }
    if (_hudGo != null) { _hudGo.SetActive(true); }

    // Подключить Game с callback для HUD
    // Переподключить Game с onScoreChanged (если Connect уже был — обновить через новый метод или переинициализировать)
    // Простое решение: Game.Connect принимает onScoreChanged=null по умолчанию,
    // здесь мы должны передать callback ДО Start(). Используем отдельный путь:
    _game.Connect(_configs, _catalog, _model, _input, OnGameOver, OnScoreChanged);
    _game.Start();

    // Подключить GameScreen к HUD
    _gameScreen.Connect(_hudVisual, null); // ShipViewModel будет null — HUD работает через UpdateHud

    // Показать начальный HUD (3 жизни, счёт 0)
    _gameScreen.UpdateHud(0, 3, 0);
}
```

**Изменить OnGameOver()**:
```csharp
private void OnGameOver()
{
    // Показать Game Over экран (D-11)
    if (_gameOverGo != null) { _gameOverGo.SetActive(true); }
    _gameOverScreen.Show(_game.Score, _game.HighScore);
}
```

**Добавить OnPlayAgain()** — Play Again callback:
```csharp
private void OnPlayAgain()
{
    // Скрыть Game Over экран (D-12)
    if (_gameOverGo != null) { _gameOverGo.SetActive(false); }
    _gameOverScreen.Hide();

    // Сбросить и перезапустить игру
    _game.Restart();

    // Показать HUD
    if (_hudGo != null) { _hudGo.SetActive(true); }
    _gameScreen.UpdateHud(0, 3, _game.HighScore);
}
```

**Добавить OnScoreChanged()** — callback для HUD:
```csharp
private void OnScoreChanged(int score, int lives)
{
    _gameScreen.UpdateHud(score, lives, _game.HighScore);
}
```

**ApplicationEntry.cs** — добавить поля для GameOver:

Добавить `[SerializeField]` поля:
```csharp
[SerializeField] private GameOverView _gameOverView;
[SerializeField] private GameObject _gameOverGo;
```

Изменить вызов `_application.Connect()` в `Awake()`:
```csharp
_application.Connect(this, _configs, _hudVisual, _titleScreenView, _titleScreenGo, _hudGo,
    _gameOverView, _gameOverGo);
```

**GameScreen.cs** — мелкое улучшение: Connect() должен допускать null ShipViewModel:
Проверить что в Connect() нет NPE при shipVm=null. Если передаётся null, Connect просто не биндит ShipViewModel к HUD (HUD обновляется через UpdateHud). Существующий код `if (_hudVisual != null) { _hudVisual.Connect(new HudData { Ship = shipVm }); }` безопасен при null shipVm.
  </action>
  <verify>
    <automated>grep -n "GameOverScreen\|GameOverView\|_gameOverGo\|OnGameOver\|OnPlayAgain\|OnScoreChanged" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Application.cs && grep -n "_gameOverView\|_gameOverGo" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/ApplicationEntry.cs</automated>
  </verify>
  <acceptance_criteria>
    - Application.cs содержит `private GameOverScreen _gameOverScreen`
    - Application.cs содержит `private void OnPlayAgain()`
    - Application.cs содержит `private void OnScoreChanged(int score, int lives)`
    - Application.cs содержит `_gameOverScreen.Show(_game.Score, _game.HighScore)` в OnGameOver
    - Application.cs содержит `_game.Restart()` в OnPlayAgain
    - ApplicationEntry.cs содержит `[SerializeField] private GameOverView _gameOverView`
    - ApplicationEntry.cs содержит `[SerializeField] private GameObject _gameOverGo`
    - ApplicationEntry.cs вызов Connect содержит `_gameOverView, _gameOverGo` в качестве аргументов
  </acceptance_criteria>
  <done>Application.cs полностью связывает Game логику с UI: OnGameOver показывает экран, OnPlayAgain скрывает и перезапускает, OnScoreChanged обновляет HUD.</done>
</task>

<task type="auto">
  <name>Задача 2: Создать Unity assets через MCP — prefabs астероидов и конфиги</name>
  <files>
    Assets/Media/prefabs/asteroid_big.prefab
    Assets/Media/prefabs/asteroid_medium.prefab
    Assets/Media/prefabs/asteroid_small.prefab
    Assets/Media/configs/AsteroidBigData.asset
    Assets/Media/configs/AsteroidMediumData.asset
    Assets/Media/configs/AsteroidSmallData.asset
  </files>
  <read_first>
    - Assets/Media/prefabs/ship.prefab — пример существующего prefab структуры (Layer, Rigidbody2D, Collider)
    - Assets/Media/configs/GameData.asset — текущий asset для понимания формата и добавления ссылок
    - .planning/phases/04-asteroids-progression/04-CONTEXT.md — D-18, D-19, D-20, D-21 (asset детали)
    - .planning/codebase/DATA_SCHEMA.md — AsteroidData поля, значения Score, SpriteVariants
    - .planning/codebase/UNITY_CONFIG.md — Layer 8 = Asteroid
  </read_first>
  <action>
Использовать MCP инструменты Unity для создания assets. Все операции выполнять через MCP, не вручную.

**Шаг 1: Определить имена sub-спрайтов астероидов**

Прочитать метаданные PNG через MCP или файловую систему:
```bash
ls Assets/Media/sprites/
```
Найти слайсированные спрайты астероидов в импортированном PNG. Имена sub-спрайтов обычно по шаблону: `asteroids_N` или по имени Atlas slice. Использовать MCP `import_asset` чтобы Unity обновил метадату, затем прочитать `.meta` файл спрайта для получения имён slice.

**Шаг 2: Создать 3 prefab через MCP**

Для каждого из asteroid_big, asteroid_medium, asteroid_small создать prefab с:
- `SpriteRenderer` компонент (спрайт из соответствующей группы вариантов)
- `Rigidbody2D` с `bodyType=Kinematic (1)`, `gravityScale=0`, `useFullKinematicContacts=true`
- `CircleCollider2D` с `radius` по размеру спрайта (approx: big=1.0, medium=0.6, small=0.35)
- `AsteroidVisual` компонент (MonoBehaviour из Plan 04-01)
- Layer = 8 (Asteroid)

Если MCP не поддерживает создание prefab напрямую — создать C# EditorScript `Phase4Setup.cs` в `Assets/Editor/` по аналогии с существующим `Phase3Setup.cs` (если он есть), который создаёт prefabs и assets при запуске из меню `Tools/Phase4Setup`.

**Шаг 3: Создать 3 AsteroidData.asset через MCP**

```yaml
AsteroidBigData.asset:
  Score: 1
  Prefab: → asteroid_big.prefab (GUID ссылка)
  SpriteVariants: [3 big-варианта спрайтов]

AsteroidMediumData.asset:
  Score: 2
  Prefab: → asteroid_medium.prefab
  SpriteVariants: [3 medium-варианта]

AsteroidSmallData.asset:
  Score: 3
  Prefab: → asteroid_small.prefab
  SpriteVariants: [3 small-варианта]
```

**Шаг 4: Обновить GameData.asset — добавить ссылки AsteroidBig/Medium/Small**

GameData.cs уже имеет поля:
- `public AsteroidData AsteroidBig`
- `public AsteroidData AsteroidMedium`
- `public AsteroidData AsteroidSmall`

Через MCP (или Editor Script) назначить соответствующие .asset файлы в Inspector.

**Шаг 5: Обновить сцену Main.unity через MCP**

Добавить `GameOverScreen` Canvas объект в сцену:
- Создать GameObject `GameOverScreen` как дочерний к корневому Canvas
- Добавить компонент `GameOverView`
- Добавить дочерние TextMeshPro объекты: `ScoreText`, `HighScoreText`
- Добавить кнопки: `PlayAgainButton`, `SubmitScoreButton` (interactable=false), `LeaderboardButton` (interactable=false)
- Связать все поля в Inspector GameOverView
- Установить `GameOverScreen` SetActive(false) по умолчанию

В `ApplicationEntry` на Main Camera GameObject:
- Назначить `_gameOverView` → GameOverView компонент
- Назначить `_gameOverGo` → GameOverScreen GameObject

**Шаг 6: Настроить HudVisual в сцене**

В существующем HUD объекте назначить новые [SerializeField] поля:
- `_scoreText` → TextMeshProUGUI для Score
- `_highScoreText` → TextMeshProUGUI для HighScore
- `_livesContainer` → Transform контейнера для иконок
- `_lifeIconSprite` → GameData.Ship.MainSprite (спрайт корабля)

Если нет подходящих существующих UI объектов — создать через MCP.
  </action>
  <verify>
    <automated>ls /Users/selstrom/work/projects/asteroids-specs/Assets/Media/prefabs/asteroid_big.prefab /Users/selstrom/work/projects/asteroids-specs/Assets/Media/prefabs/asteroid_medium.prefab /Users/selstrom/work/projects/asteroids-specs/Assets/Media/prefabs/asteroid_small.prefab 2>&1 && ls /Users/selstrom/work/projects/asteroids-specs/Assets/Media/configs/AsteroidBigData.asset /Users/selstrom/work/projects/asteroids-specs/Assets/Media/configs/AsteroidMediumData.asset /Users/selstrom/work/projects/asteroids-specs/Assets/Media/configs/AsteroidSmallData.asset 2>&1 && grep -l "AsteroidBig\|AsteroidMedium\|AsteroidSmall" /Users/selstrom/work/projects/asteroids-specs/Assets/Media/configs/GameData.asset 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Файлы asteroid_big.prefab, asteroid_medium.prefab, asteroid_small.prefab существуют в Assets/Media/prefabs/
    - Файлы AsteroidBigData.asset, AsteroidMediumData.asset, AsteroidSmallData.asset существуют в Assets/Media/configs/
    - GameData.asset содержит GUID-ссылки на AsteroidBig/Medium/Small assets (grep "AsteroidBig" GameData.asset возвращает результат)
    - AsteroidBigData.asset содержит `Score: 1`
    - AsteroidMediumData.asset содержит `Score: 2`
    - AsteroidSmallData.asset содержит `Score: 3`
    - Проект компилируется после создания assets
  </acceptance_criteria>
  <done>Unity assets созданы: 3 prefab астероидов с правильными компонентами/слоями, 3 AsteroidData конфига с правильными Score значениями, GameData.asset обновлён со ссылками.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Полный игровой цикл Phase 4: астероиды трёх размеров, дробление, волны, HUD со счётом и жизнями, Game Over экран, Play Again.
  </what-built>
  <how-to-verify>
    1. Открыть Unity Editor → нажать Play
    2. На TitleScreen нажать Play — должны появиться 4 Large астероида (не ближе ~20 ед. от центра)
    3. Пострелять по Large астероиду — он должен разделиться на 2 Medium
    4. Уничтожить Medium — он должен разделиться на 2 Small
    5. Уничтожить Small — он должен исчезнуть без осколков
    6. HUD слева-вверху должен показывать Score (начинается с 0, растёт: +1, +2, +3)
    7. HUD справа-вверху должен показывать HighScore
    8. Под Score должны быть 3 иконки корабля (жизни)
    9. Уничтожить все астероиды первой волны — должна начаться вторая волна с 5 Large
    10. Допустить столкновение корабля с астероидом 3 раза → Game Over экран
    11. Game Over показывает финальный Score и Best Score
    12. Кнопка "Play Again" перезапускает игру с 0 очков, 3 жизнями, волна 1
    13. HighScore сохраняется между сессиями (не обнуляется при Play Again)
    14. Кнопки Submit Score и Leaderboard неактивны (серые, не нажимаются)
  </how-to-verify>
  <resume-signal>Напечатать "approved" если всё работает, или описать найденные проблемы</resume-signal>
</task>

</tasks>

<verification>
После Задачи 2 (до checkpoint):
1. MCP compile — 0 ошибок компиляции
2. Все 6 asset файлов существуют (3 prefab + 3 config)
3. GameData.asset обновлён с ссылками на 3 asteroid configs

После checkpoint:
- Пользователь подтвердил полный игровой цикл
</verification>

<success_criteria>
- Нажать Play → TitleScreen → Play → 4 Large астероида спаунятся вдали от корабля
- Дробление: Large→2 Medium→2 Small→исчезает (AST-02, AST-03, AST-04)
- Score в HUD растёт: уничтожение Small=+3, Medium=+2, Large=+1 (D-01, DATA_SCHEMA)
- 3 иконки жизней в HUD при старте (PROG-03)
- Следующая волна начинается после уничтожения всех астероидов (AST-08: +1 Large каждую волну)
- При 0 жизнях — Game Over экран с Score/HighScore (PROG-05, PROG-07)
- Play Again перезапускает без перезагрузки приложения (PROG-08)
- Submit Score и Leaderboard кнопки disabled (D-11)
</success_criteria>

<output>
После завершения создать `.planning/phases/04-asteroids-progression/04-04-SUMMARY.md`
</output>
