---
phase: 04-asteroids-progression
plan: 05
type: execute
wave: 4
depends_on: [04-04]
files_modified:
  - Assets/Scripts/Application/Game.cs
  - Assets/Editor/Phase4Setup.cs
autonomous: false
requirements: [AST-01, AST-02, AST-03, AST-04, AST-05, AST-06, AST-07, AST-08, AST-09, AST-10, PROG-01, PROG-02, PROG-03, PROG-04, PROG-05, PROG-07, PROG-08]
gap_closure: true

must_haves:
  truths:
    - "Пуля уничтожается (возвращается в пул) при попадании в астероид"
    - "HUD показывает числовой счёт в реальном времени при уничтожении астероидов"
    - "HUD показывает иконки жизней (3 при старте)"
  artifacts:
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "bullet.Kill() вызывается в OnAsteroidCollided после asteroid.Kill()"
      contains: "bullet.Kill()"
    - path: "Assets/Editor/Phase4Setup.cs"
      provides: "SetupHud назначает поля HudVisual через SerializedObject, включая диагностический лог"
      contains: "SetupHud"
  key_links:
    - from: "BulletVisual.OnCollisionEnter2D"
      to: "Game.OnAsteroidCollided"
      via: "AsteroidViewModel.OnCollision callback → col содержит gameObject пули"
      pattern: "bullet\\.Kill\\(\\)"
    - from: "Game.OnScoreChanged"
      to: "GameScreen.UpdateHud"
      via: "Application._onScoreChanged callback → _gameScreen.UpdateHud(score, lives, highScore)"
      pattern: "UpdateHud"
---

<objective>
Закрыть два major gap'а UAT: уничтожение пули при коллайде с астероидом и отображение HUD со счётом.

Purpose: UAT выявил два блокирующих игровой опыт дефекта — пуля летит сквозь астероиды (todo в коде), HUD не показывает счёт (поля не назначены в Inspector). Оба фикса целевые и минимальные.
Output: Game.cs с bullet.Kill(), Phase4Setup.cs с улучшенной диагностикой назначения HUD-полей, Human Verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-asteroids-progression/04-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Ключевые контракты — прочитать перед реализацией -->

Из Game.cs (текущий):
```csharp
private void OnAsteroidCollided(AsteroidModel asteroid, Collision2D col)
{
    if (!_isRunning || asteroid.IsDead()) { return; }

    var hitModel = _catalog.GetModelByGo(col.gameObject);
    if (hitModel is not BulletModel) { return; }

    // Начислить очки
    var data = GetAsteroidData(asteroid.Size);
    _model.Score += data.Score;

    // Экстра-жизнь
    while (_model.Score >= _nextBonusLifeScore && _lives < 6) { ... }

    // Уничтожить астероид
    asteroid.Kill();

    // Уведомить HUD
    _onScoreChanged?.Invoke(_model.Score, _lives);
    // ← ПУЛЯ НЕ УБИВАЕТСЯ — gap 1
}

private void OnEntityDestroyed(IGameEntityModel model)
{
    if (model is BulletModel bullet)
    {
        if (bullet.Gun != null) { bullet.Gun.CurrentShoots--; }
    }
    // ... asteroid, ship логика
    _catalog.Release(model);
}
```

Из HudVisual.cs (текущий):
```csharp
[SerializeField] private TextMeshProUGUI _scoreText;
[SerializeField] private TextMeshProUGUI _highScoreText;
[SerializeField] private Transform _livesContainer;
[SerializeField] private Sprite _lifeIconSprite;

public void UpdateHud(int score, int lives, int highScore)
{
    if (_scoreText != null) { _scoreText.text = score.ToString(); }
    if (_highScoreText != null) { _highScoreText.text = highScore.ToString(); }
    UpdateLivesIcons(lives);
}
```

Из Phase4Setup.cs — SetupHud() уже создаёт UI-элементы и назначает через SerializedObject.
Проблема: поля могут быть не назначены если Phase4Setup не запускался или сцена не сохранилась.

Из Application.cs — OnScoreChanged уже реализован:
```csharp
private void OnScoreChanged(int score, int lives)
{
    _gameScreen.UpdateHud(score, lives, _game.HighScore);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Убивать пулю при коллайде с астероидом в Game.cs</name>
  <files>Assets/Scripts/Application/Game.cs</files>
  <read_first>
    - Assets/Scripts/Application/Game.cs — полный текущий код (обязательно перед правкой)
    - Assets/Scripts/Model/Entities/BulletModel.cs — убедиться что Kill() определён в IGameEntityModel
  </read_first>
  <action>
В методе `OnAsteroidCollided` после строки `asteroid.Kill()` добавить уничтожение пули.

Найти блок:
```csharp
// Уничтожить астероид
asteroid.Kill();

// Уведомить HUD
_onScoreChanged?.Invoke(_model.Score, _lives);
```

Заменить на:
```csharp
// Уничтожить астероид
asteroid.Kill();

// Уничтожить пулю (gap fix: пуля возвращается в пул при первом попадании)
if (hitModel is BulletModel hitBullet)
{
    hitBullet.Kill();
}

// Уведомить HUD
_onScoreChanged?.Invoke(_model.Score, _lives);
```

Примечание: `hitModel` уже объявлен выше в методе как `var hitModel = _catalog.GetModelByGo(col.gameObject)`. Переменная `hitBullet` нужна для явного cast, так как `hitModel` имеет тип `IGameEntityModel`. Если компилятор позволяет — использовать pattern matching в is-выражении.

Также: удалить TODO-комментарий если он присутствует в методе или рядом.
  </action>
  <verify>
    <automated>grep -n "hitBullet\|hitModel.*Kill\|bullet.*Kill\|Kill.*bullet" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs</automated>
  </verify>
  <acceptance_criteria>
    - Game.cs содержит вызов Kill() для пули в методе OnAsteroidCollided (grep "hitBullet.Kill()" или аналог)
    - Kill() вызывается ПОСЛЕ asteroid.Kill() и ДО или ПОСЛЕ _onScoreChanged.Invoke — порядок не критичен
    - Код компилируется без ошибок (проверить через MCP compile или убедиться что нет синтаксических ошибок)
    - Не добавлено лишней логики — только Kill() пули
  </acceptance_criteria>
  <done>При попадании пули в астероид вызывается bullet.Kill() — в OnEntityDestroyed пуля будет возвращена в пул через _catalog.Release(model).</done>
</task>

<task type="auto">
  <name>Задача 2: Диагностика и надёжное назначение HUD полей в Phase4Setup.cs</name>
  <files>Assets/Editor/Phase4Setup.cs</files>
  <read_first>
    - Assets/Editor/Phase4Setup.cs — полный текущий код SetupHud() метода (обязательно)
    - Assets/Scripts/View/HudVisual.cs — проверить точные имена [SerializeField] полей: _scoreText, _highScoreText, _livesContainer, _lifeIconSprite
    - Assets/Scenes/Main.unity — не читать целиком; только если нужно понять структуру Hud GameObject
  </read_first>
  <action>
В методе `SetupHud` добавить диагностические логи ПОСЛЕ `serializedHud.ApplyModifiedProperties()`.

Найти конец метода SetupHud — строку:
```csharp
UnityEngine.Debug.Log("[Phase4Setup] HUD обновлён: Score, HighScore, LivesContainer добавлены.");
```

Заменить на расширенный диагностический лог:
```csharp
// Диагностика: проверить что поля назначены
var checkScore = serializedHud.FindProperty("_scoreText").objectReferenceValue;
var checkHighScore = serializedHud.FindProperty("_highScoreText").objectReferenceValue;
var checkLives = serializedHud.FindProperty("_livesContainer").objectReferenceValue;
var checkIcon = serializedHud.FindProperty("_lifeIconSprite").objectReferenceValue;

UnityEngine.Debug.Log($"[Phase4Setup] HUD обновлён:" +
    $"\n  _scoreText={checkScore?.name ?? "NULL (проверить Hud объект в сцене)"}" +
    $"\n  _highScoreText={checkHighScore?.name ?? "NULL"}" +
    $"\n  _livesContainer={checkLives?.name ?? "NULL"}" +
    $"\n  _lifeIconSprite={checkIcon?.name ?? "NULL (ship_idle спрайт не найден в PNG)"}");
```

Также добавить в начало `SetupHud` после `if (hudVisual == null) { return; }` проверку:
```csharp
UnityEngine.Debug.Log($"[Phase4Setup] Найден HudVisual на '{hudGo.name}'. Настраиваем поля...");
```

Это позволит исполнителю (и пользователю) видеть в Console точно что назначено/не назначено при следующем запуске `Asteroids > Setup Phase 4 Assets`.
  </action>
  <verify>
    <automated>grep -n "checkScore\|NULL.*проверить\|Найден HudVisual" /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase4Setup.cs</automated>
  </verify>
  <acceptance_criteria>
    - Phase4Setup.cs содержит диагностические логи проверки назначения полей (_scoreText, _highScoreText, _livesContainer, _lifeIconSprite)
    - Лог явно указывает "NULL" если поле не назначено — пользователь увидит проблему в Console
    - Компилируется без ошибок — только добавлены Debug.Log вызовы
  </acceptance_criteria>
  <done>При следующем запуске Setup Phase 4 Assets Console покажет точный статус каждого HUD поля, что упростит диагностику если HUD по-прежнему не виден.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Gap 1: Game.cs — bullet.Kill() вызывается в OnAsteroidCollided.
    Gap 2: Phase4Setup.cs — улучшена диагностика назначения HUD полей.
  </what-built>
  <how-to-verify>
    **Шаг 1: Пересобрать и переназначить assets**
    1. Открыть Unity Editor — дождаться компиляции без ошибок
    2. Выполнить `Asteroids → Setup Phase 4 Assets`
    3. Проверить Console — должны появиться строки вида:
       `[Phase4Setup] HUD обновлён: _scoreText=score_text _highScoreText=high_score_text _livesContainer=lives_container _lifeIconSprite=ship_idle`
    4. Если любое поле = NULL — сообщить об этом

    **Шаг 2: Проверить gap 1 — уничтожение пули**
    5. Нажать Play → Play на TitleScreen
    6. Выстрелить в большой астероид (Space)
    7. При попадании: пуля должна исчезать немедленно (не лететь дальше сквозь астероид)
    8. Астероид разделяется на 2 Medium — это поведение уже работало

    **Шаг 3: Проверить gap 2 — HUD со счётом**
    9. В левом верхнем углу HUD должны быть видны:
       - Числовой счёт "0" при старте
       - 3 маленькие иконки корабля (жизни)
    10. Уничтожить Large астероид — счёт должен стать "1"
    11. Уничтожить Medium — счёт должен вырасти на 2
    12. Уничтожить Small — счёт должен вырасти на 3
    13. Текущий счёт и иконки жизней обновляются в реальном времени
  </how-to-verify>
  <resume-signal>Напечатать "approved" если оба gap'а закрыты. Если проблема осталась — описать что именно не работает (какой шаг провалился, что видно в Console)</resume-signal>
</task>

</tasks>

<verification>
После Задачи 1:
- grep "hitBullet.Kill\|Kill().*bullet" Game.cs — должен найти строку уничтожения пули

После Задачи 2:
- grep "checkScore\|NULL.*проверить" Phase4Setup.cs — должен найти диагностические строки

После checkpoint:
- Пользователь подтвердил: пуля исчезает при попадании в астероид
- Пользователь подтвердил: HUD показывает счёт и иконки жизней
</verification>

<success_criteria>
- Пуля исчезает при первом попадании в астероид (не летит сквозь)
- HUD показывает счёт "0" при старте, растёт на +1/+2/+3 при уничтожении Large/Medium/Small
- HUD показывает 3 иконки жизней при старте
- Оба gap'а из UAT закрыты
</success_criteria>

<output>
После завершения создать `.planning/phases/04-asteroids-progression/04-05-SUMMARY.md`
</output>
