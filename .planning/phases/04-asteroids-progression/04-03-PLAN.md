---
phase: 04-asteroids-progression
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - Assets/Scripts/View/HudVisual.cs
  - Assets/Scripts/Application/Screens/GameScreen.cs
  - Assets/Scripts/View/GameOverView.cs
  - Assets/Scripts/Application/Screens/GameOverScreen.cs
autonomous: true
requirements: [PROG-02, PROG-03, PROG-07, PROG-08]

must_haves:
  truths:
    - "HUD отображает Score (верх-лево), Lives-иконки (под Score), High Score (верх-право)"
    - "Иконки жизней динамически добавляются/удаляются при изменении Lives"
    - "Game Over экран показывает Score и High Score, кнопку Play Again (активна)"
    - "Кнопки Submit Score и Leaderboard на GameOver экране — disabled (не кликабельны)"
  artifacts:
    - path: "Assets/Scripts/View/HudVisual.cs"
      provides: "HudVisual с score_text, highScore_text, livesContainer + UpdateHud(score, lives, highScore)"
      contains: "UpdateHud"
    - path: "Assets/Scripts/Application/Screens/GameScreen.cs"
      provides: "GameScreen.UpdateHud(score, lives, highScore)"
      contains: "UpdateHud"
    - path: "Assets/Scripts/View/GameOverView.cs"
      provides: "GameOverViewModel + GameOverView MonoBehaviour с кнопками"
      exports: ["GameOverViewModel", "GameOverView"]
    - path: "Assets/Scripts/Application/Screens/GameOverScreen.cs"
      provides: "GameOverScreen : AbstractScreen с Connect() и Show(score, highScore)"
      contains: "Show"
  key_links:
    - from: "HudVisual.UpdateHud(score, lives, highScore)"
      to: "GameData.Ship.MainSprite"
      via: "Instantiate иконок жизней в _livesContainer с Ship.MainSprite"
    - from: "GameOverScreen.Connect"
      to: "GameOverView"
      via: "view.Connect(vm) + vm.OnPlayAgainClicked"
    - from: "Application.OnScoreChanged"
      to: "GameScreen.UpdateHud"
      via: "onScoreChanged callback передаётся в Game.Connect; Game вызывает _onScoreChanged(score, lives) → Application.OnScoreChanged → _gameScreen.UpdateHud(score, lives, _game.HighScore)"
---

<objective>
Создать HUD с отображением Score/Lives/HighScore и Game Over экран с кнопкой Play Again. Параллельно с Plan 04-02 (разные файлы).

Purpose: Визуальная обратная связь — игрок видит счёт, жизни и финальный экран. Без HUD логика прогрессии из Plan 04-02 невидима.
Output: HudVisual.cs (расширен), GameScreen.cs (расширен), GameOverView.cs (новый), GameOverScreen.cs (новый).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-asteroids-progression/04-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Существующие типы для использования -->

Из Assets/Scripts/View/HudVisual.cs (текущее состояние):
```csharp
public struct HudData { public ShipViewModel Ship; }

public class HudVisual : MonoBehaviour
{
    [SerializeField] private TextMeshProUGUI _coordinates;
    [SerializeField] private TextMeshProUGUI _rotationAngle;
    [SerializeField] private TextMeshProUGUI _speedText;
    [SerializeField] private TextMeshProUGUI _laserShootCount;
    [SerializeField] private TextMeshProUGUI _laserReloadTime;
    private ShipViewModel _ship;
    public void Connect(HudData data);  // существующий метод
    private void Update();              // обновляет координаты из _ship
}
```

Из Assets/Scripts/Application/Screens/GameScreen.cs (текущее):
```csharp
public class GameScreen : AbstractScreen
{
    private HudVisual _hudVisual;
    public void Connect(HudVisual hudVisual, ShipViewModel shipVm);
}
```

Из Assets/Scripts/Application/Screens/TitleScreen.cs (паттерн для GameOverScreen):
```csharp
public class TitleScreen : AbstractScreen
{
    private TitleScreenView _view;
    public void Connect(TitleScreenView view, Action onStart)
    {
        _view = view;
        var vm = new TitleScreenViewModel();
        vm.OnPlayClicked = onStart;
        _view.Connect(vm);
    }
}
```

Из Assets/Scripts/View/TitleScreenView.cs (паттерн для GameOverView):
- Наследует AbstractWidgetView<TitleScreenViewModel>
- [SerializeField] private Button _playButton
- protected override void OnConnected(): биндинг кнопки

GameData.Ship.MainSprite — спрайт корабля для иконок жизней (D-08)

Из CONTEXT.md:
- D-07: HUD сохраняет отладочные координаты + добавляет Score/Lives/HighScore
- D-08: иконки жизней — Image компоненты с Ship.MainSprite, размер 20×20px
- D-09: HighScore только в памяти
- D-11: Game Over экран — кнопки Submit/Leaderboard disabled, Play Again активна
- PROG-02: Score — верх-лево; PROG-03: Lives — под Score; PROG-07: HighScore — верх-право
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Расширить HudVisual — добавить Score, HighScore, Lives иконки</name>
  <files>
    Assets/Scripts/View/HudVisual.cs
    Assets/Scripts/Application/Screens/GameScreen.cs
  </files>
  <read_first>
    - Assets/Scripts/View/HudVisual.cs — полный текущий код (обязательно)
    - Assets/Scripts/Application/Screens/GameScreen.cs — полный текущий код (обязательно)
    - Assets/Scripts/Configs/GameData.cs — структура ShipData.MainSprite
    - .planning/phases/04-asteroids-progression/04-CONTEXT.md — D-07, D-08, D-10 (HighScore с 0)
  </read_first>
  <action>
**HudVisual.cs** — добавить поля и метод UpdateHud, сохранив существующий код:

Добавить новые using (если ещё не импортированы):
```csharp
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
```

Добавить новые `[SerializeField]` поля к существующим:
```csharp
[SerializeField] private TextMeshProUGUI _scoreText;        // Score верх-лево (PROG-02)
[SerializeField] private TextMeshProUGUI _highScoreText;    // HighScore верх-право (PROG-07)
[SerializeField] private Transform _livesContainer;         // контейнер для иконок жизней (D-08)
[SerializeField] private Sprite _lifeIconSprite;           // Ship спрайт для иконок (D-08)
```

Добавить приватное поле для хранения иконок:
```csharp
private readonly List<GameObject> _lifeIcons = new();
```

Добавить публичный метод `UpdateHud(int score, int lives, int highScore)`:
```csharp
public void UpdateHud(int score, int lives, int highScore)
{
    if (_scoreText != null) { _scoreText.text = score.ToString(); }
    if (_highScoreText != null) { _highScoreText.text = highScore.ToString(); }
    UpdateLivesIcons(lives);
}
```

Добавить приватный метод `UpdateLivesIcons(int lives)`:
```csharp
private void UpdateLivesIcons(int lives)
{
    if (_livesContainer == null || _lifeIconSprite == null) { return; }

    // Удалить лишние иконки
    while (_lifeIcons.Count > lives)
    {
        var last = _lifeIcons[_lifeIcons.Count - 1];
        _lifeIcons.RemoveAt(_lifeIcons.Count - 1);
        Destroy(last);
    }

    // Добавить недостающие иконки
    while (_lifeIcons.Count < lives)
    {
        var iconGo = new GameObject("LifeIcon");
        iconGo.transform.SetParent(_livesContainer, false);
        var img = iconGo.AddComponent<Image>();
        img.sprite = _lifeIconSprite;
        // Размер 20×20px (D-08)
        var rt = iconGo.GetComponent<RectTransform>();
        rt.sizeDelta = new Vector2(20f, 20f);
        _lifeIcons.Add(iconGo);
    }
}
```

Существующий `Connect(HudData data)` и `Update()` — оставить без изменений.

**GameScreen.cs** — добавить метод UpdateHud:

Добавить поле:
```csharp
private int _highScore;
```

Добавить публичный метод `UpdateHud(int score, int lives, int highScore)`:
```csharp
public void UpdateHud(int score, int lives, int highScore)
{
    _highScore = highScore;
    _hudVisual?.UpdateHud(score, lives, highScore);
}
```

Существующий `Connect(HudVisual hudVisual, ShipViewModel shipVm)` — оставить без изменений.
  </action>
  <verify>
    <automated>grep -n "UpdateHud\|_scoreText\|_highScoreText\|_livesContainer\|UpdateLivesIcons\|_lifeIcons" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/HudVisual.cs && grep -n "UpdateHud" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Screens/GameScreen.cs</automated>
  </verify>
  <acceptance_criteria>
    - HudVisual.cs содержит `[SerializeField] private TextMeshProUGUI _scoreText`
    - HudVisual.cs содержит `[SerializeField] private TextMeshProUGUI _highScoreText`
    - HudVisual.cs содержит `[SerializeField] private Transform _livesContainer`
    - HudVisual.cs содержит `public void UpdateHud(int score, int lives, int highScore)`
    - HudVisual.cs содержит `private void UpdateLivesIcons(int lives)`
    - HudVisual.cs содержит `rt.sizeDelta = new Vector2(20f, 20f)` — размер иконок 20×20px
    - GameScreen.cs содержит `public void UpdateHud(int score, int lives, int highScore)`
    - Существующий `Connect(HudData data)` и `Update()` в HudVisual.cs сохранены
  </acceptance_criteria>
  <done>HudVisual умеет обновлять Score/HighScore текст и динамически создавать/удалять иконки жизней. GameScreen прокидывает вызов UpdateHud к HudVisual.</done>
</task>

<task type="auto">
  <name>Задача 2: Создать GameOverView + GameOverScreen</name>
  <files>
    Assets/Scripts/View/GameOverView.cs
    Assets/Scripts/Application/Screens/GameOverScreen.cs
  </files>
  <read_first>
    - Assets/Scripts/View/TitleScreenView.cs — паттерн View + ViewModel для копирования
    - Assets/Scripts/Application/Screens/TitleScreen.cs — паттерн Screen для копирования
    - Assets/Scripts/Application/Screens/AbstractScreen.cs — базовый класс
    - .planning/phases/04-asteroids-progression/04-CONTEXT.md — D-11 (Submit/Leaderboard disabled), D-12 (Play Again сбрасывает сессию), D-13 (GameOverScreen — отдельный Canvas-объект)
  </read_first>
  <action>
**GameOverView.cs** — по паттерну TitleScreenView (один файл = ViewModel + View):

Namespace: `SelStrom.Asteroids`

Usings:
```csharp
using System;
using Shtl.Mvvm;
using TMPro;
using UnityEngine;
using UnityEngine.UI;
```

`GameOverViewModel` (наследует `AbstractViewModel`):
```csharp
public class GameOverViewModel : AbstractViewModel
{
    public Action OnPlayAgainClicked;       // PROG-08: Play Again
    public int FinalScore;                  // финальный счёт для отображения
    public int HighScore;                   // High Score сессии (PROG-07)
}
```

`GameOverView` (наследует `AbstractWidgetView<GameOverViewModel>`):
```csharp
public class GameOverView : AbstractWidgetView<GameOverViewModel>
{
    [SerializeField] private TextMeshProUGUI _scoreText;      // "Score: N"
    [SerializeField] private TextMeshProUGUI _highScoreText;  // "Best: N"
    [SerializeField] private Button _playAgainButton;         // активна (PROG-08)
    [SerializeField] private Button _submitScoreButton;       // disabled (D-11)
    [SerializeField] private Button _leaderboardButton;       // disabled (D-11)

    protected override void OnConnected()
    {
        // Отобразить счёт
        if (_scoreText != null) { _scoreText.text = $"Score: {ViewModel.FinalScore}"; }
        if (_highScoreText != null) { _highScoreText.text = $"Best: {ViewModel.HighScore}"; }

        // Play Again — активна (D-11)
        if (_playAgainButton != null)
        {
            _playAgainButton.interactable = true;
            _playAgainButton.onClick.AddListener(() => ViewModel.OnPlayAgainClicked?.Invoke());
        }

        // Submit Score — disabled до Phase 7 (D-11)
        if (_submitScoreButton != null) { _submitScoreButton.interactable = false; }

        // Leaderboard — disabled до Phase 7 (D-11)
        if (_leaderboardButton != null) { _leaderboardButton.interactable = false; }
    }

    protected override void OnDisconnected()
    {
        if (_playAgainButton != null) { _playAgainButton.onClick.RemoveAllListeners(); }
    }
}
```

**GameOverScreen.cs** — по паттерну TitleScreen:

Namespace: `SelStrom.Asteroids`

Usings:
```csharp
using System;
```

```csharp
public class GameOverScreen : AbstractScreen
{
    private GameOverView _view;

    public void Connect(GameOverView view, Action onPlayAgain)
    {
        _view = view;
        var vm = new GameOverViewModel();
        vm.OnPlayAgainClicked = onPlayAgain;
        _view.Connect(vm);
    }

    // Показать экран с актуальным счётом (вызывается при Game Over)
    public void Show(int finalScore, int highScore)
    {
        if (_view == null) { return; }
        var vm = new GameOverViewModel
        {
            OnPlayAgainClicked = _view.ViewModel?.OnPlayAgainClicked,
            FinalScore = finalScore,
            HighScore = highScore
        };
        _view.Connect(vm);
        _view.gameObject.SetActive(true);
    }

    public void Hide()
    {
        if (_view != null) { _view.gameObject.SetActive(false); }
    }

    public override void Dispose()
    {
        base.Dispose();
        _view = null;
    }
}
```
  </action>
  <verify>
    <automated>grep -n "class GameOverViewModel\|class GameOverView\|OnPlayAgainClicked\|_submitScoreButton\|interactable = false" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/GameOverView.cs && grep -n "class GameOverScreen\|public void Show\|public void Hide" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Screens/GameOverScreen.cs</automated>
  </verify>
  <acceptance_criteria>
    - GameOverView.cs существует и содержит `class GameOverViewModel : AbstractViewModel`
    - GameOverView.cs содержит `class GameOverView : AbstractWidgetView<GameOverViewModel>`
    - GameOverView.cs содержит `public Action OnPlayAgainClicked`
    - GameOverView.cs содержит `_submitScoreButton.interactable = false` (disabled per D-11)
    - GameOverView.cs содержит `_leaderboardButton.interactable = false` (disabled per D-11)
    - GameOverScreen.cs существует и содержит `class GameOverScreen : AbstractScreen`
    - GameOverScreen.cs содержит `public void Show(int finalScore, int highScore)`
    - GameOverScreen.cs содержит `public void Hide()`
    - GameOverScreen.cs содержит `_view.gameObject.SetActive(true)` в Show()
  </acceptance_criteria>
  <done>GameOverView и GameOverScreen созданы по паттерну TitleScreen/TitleScreenView. Submit и Leaderboard кнопки disabled — Phase 7 только подключит логику к уже существующим кнопкам.</done>
</task>

</tasks>

<verification>
После выполнения обоих задач:
1. `ls Assets/Scripts/View/GameOverView.cs Assets/Scripts/Application/Screens/GameOverScreen.cs` — оба файла существуют
2. `grep -n "UpdateHud\|_scoreText\|_livesContainer" Assets/Scripts/View/HudVisual.cs` — все три найдены
3. MCP compile — проект собирается без ошибок
</verification>

<success_criteria>
- HudVisual.UpdateHud(score, lives, highScore) обновляет Score текст, HighScore текст и иконки жизней
- Иконки жизней размером 20×20px создаются динамически как Image компоненты с _lifeIconSprite
- GameOverScreen.Show(finalScore, highScore) отображает экран с актуальными данными
- GameOverView имеет 3 кнопки: Play Again (interactable=true), Submit Score (false), Leaderboard (false)
- GameScreen.UpdateHud() делегирует вызов в HudVisual
- Проект компилируется
</success_criteria>

<output>
После завершения создать `.planning/phases/04-asteroids-progression/04-03-SUMMARY.md`
</output>
