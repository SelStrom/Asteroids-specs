---
phase: 04-asteroids-progression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Model/Entities/AsteroidModel.cs
  - Assets/Scripts/View/AsteroidVisual.cs
  - Assets/Scripts/Application/EntitiesCatalog.cs
  - Assets/Scripts/Model/Model.cs
autonomous: true
requirements: [AST-01, AST-05, AST-06, AST-07, AST-10]

must_haves:
  truths:
    - "AsteroidModel хранит Size (1/2/3), скорость вращения и начальную скорость как Vector2"
    - "AsteroidVisual рендерит спрайт из ViewModel, вращает объект каждый кадр, отправляет collision"
    - "EntitiesCatalog.CreateAsteroid создаёт и регистрирует астероид с ViewModel-биндингами"
    - "GameObjectPool содержит prefabs трёх размеров астероидов"
    - "Model.GroupCreator.Visit(AsteroidModel) регистрирует его в MoveSystem"
  artifacts:
    - path: "Assets/Scripts/Model/Entities/AsteroidModel.cs"
      provides: "AsteroidModel с AngularSpeed, InitialVelocity, Size"
      contains: "public float AngularSpeed"
    - path: "Assets/Scripts/View/AsteroidVisual.cs"
      provides: "AsteroidViewModel + AsteroidVisual MonoBehaviour"
      exports: ["AsteroidViewModel", "AsteroidVisual"]
    - path: "Assets/Scripts/Application/EntitiesCatalog.cs"
      provides: "CreateAsteroid(AsteroidData, Vector2 position, Vector2 velocity)"
      contains: "CreateAsteroid"
  key_links:
    - from: "EntitiesCatalog.CreateAsteroid"
      to: "AsteroidVisual"
      via: "pool.Get<AsteroidVisual>(data.Prefab)"
    - from: "AsteroidModel"
      to: "MoveSystem"
      via: "Model.GroupCreator.Visit(AsteroidModel) → GetSystem<MoveSystem>().Add"
---

<objective>
Реализовать ECS + View слой для астероидов: расширить AsteroidModel данными о вращении и скорости, создать AsteroidVisual/ViewModel по паттерну ShipVisual, добавить CreateAsteroid в EntitiesCatalog, подключить астероид к MoveSystem в GroupCreator.

Purpose: Это фундамент Phase 4 — без этого плана невозможны ни дробление, ни волны, ни коллизии. Plans 02 и 03 используют CreateAsteroid и AsteroidVisual.
Output: AsteroidModel.cs (расширен), AsteroidVisual.cs (новый), EntitiesCatalog.cs (метод CreateAsteroid), Model.cs (GroupCreator.Visit реализован).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-asteroids-progression/04-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/DATA_SCHEMA.md
@.planning/codebase/UNITY_CONFIG.md

<interfaces>
<!-- Существующие типы для использования в этом плане. Читать исходники обязательно. -->

Из Assets/Scripts/Model/Entities/AsteroidModel.cs (текущее состояние — заглушка):
```csharp
public class AsteroidModel : IGameEntityModel
{
    public MoveComponent Move { get; } = new();
    public int Size { get; set; } // 1=Small, 2=Medium, 3=Big
    // IsDead(), Kill(), AcceptWith(IGroupVisitor)
}
```

Из Assets/Scripts/View/ShipVisual.cs (паттерн для копирования):
```csharp
public class ShipViewModel : AbstractViewModel
{
    public ReactiveValue<Vector2> Position = new();
    public ReactiveValue<bool> IsThrusting = new();
    public ReactiveValue<bool> IsVisible = new(true);
    public Action<Collision2D> OnCollision;
}
public class ShipVisual : AbstractWidgetView<ShipViewModel>, IEntityView
{
    [SerializeField] private SpriteRenderer _spriteRenderer;
    private int _prefabInstanceId;
    public int PrefabInstanceId => _prefabInstanceId;
    public void SetPrefabId(int id) { _prefabInstanceId = id; }
    protected override void OnConnected() { /* биндинги */ }
    private void OnCollisionEnter2D(Collision2D col) { ViewModel?.OnCollision?.Invoke(col); }
}
```

Из Assets/Scripts/Application/EntitiesCatalog.cs (паттерн CreateBullet для повторения):
```csharp
public BulletModel CreateBullet(Vector2 position, Vector2 velocity, bool isEnemy = false)
{
    var model = _modelFactory.Create<BulletModel>();
    model.Move.Position.Value = position;
    model.Move.Direction = velocity.normalized;
    model.Move.Speed.Value = velocity.magnitude;
    var view = _pool.Get<BulletVisual>(prefab);
    view.SetPrefabId(prefab.GetInstanceID());
    var vm = new BulletViewModel();
    var bind = new EventBindingContext();
    bind.From(model.Move.Position).To(vm.Position);
    view.Connect(vm);
    _modelToView[model] = view;
    _modelToBind[model] = bind;
    _modelToGo[model] = view.gameObject;
    _goToModel[view.gameObject] = model;
    return model;
}
```

Из Assets/Scripts/Configs/AsteroidData.cs:
```csharp
// namespace SelStrom.Asteroids.Configs
public class AsteroidData : BaseGameEntityData
{
    public int Score;        // 1/2/3
    public GameObject Prefab;
    public Sprite[] SpriteVariants; // 3 варианта
}
```

Из Assets/Scripts/Model/Model.cs — GroupCreator (заглушки):
```csharp
public void Visit(AsteroidModel model) { /* Phase 4 */ }   // ← реализовать
public void Visit(UfoBigModel model) { /* Phase 4-5 */ }   // ← пока оставить
```

Слои Unity (UNITY_CONFIG.md):
- Asteroid = Layer 8 (имя "Asteroid")
- Матрица: Asteroid ↔ PlayerBullet, Asteroid ↔ Player
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Расширить AsteroidModel — добавить AngularSpeed, Speed поля; реализовать View+ViewModel</name>
  <files>
    Assets/Scripts/Model/Entities/AsteroidModel.cs
    Assets/Scripts/View/AsteroidVisual.cs
  </files>
  <read_first>
    - Assets/Scripts/Model/Entities/AsteroidModel.cs — текущее состояние заглушки
    - Assets/Scripts/View/ShipVisual.cs — паттерн ViewModel + Visual для копирования
    - Assets/Scripts/View/BulletVisual.cs — минимальный паттерн Visual
    - Assets/Scripts/Model/Components/MoveComponent.cs — поля Position, Speed, Direction
    - .planning/codebase/CONVENTIONS.md — namespace, стиль кода
    - .planning/phases/04-asteroids-progression/04-CONTEXT.md — D-06 (вращение в AsteroidVisual.Update), D-05 (направления осколков)
  </read_first>
  <action>
**AsteroidModel.cs** — расширить существующую заглушку:
- Добавить поле `public float AngularSpeed { get; set; }` — угловая скорость вращения (°/с), задаётся при спавне случайно в диапазоне 30–120
- Поле `Size` уже есть (1=Small, 2=Medium, 3=Big) — оставить
- Поле `MoveComponent Move` уже есть — оставить
- Namespace остаётся `SelStrom.Asteroids`
- using: `using Model.Components;`

```csharp
public class AsteroidModel : IGameEntityModel
{
    private bool _killed;

    public MoveComponent Move { get; } = new();
    public int Size { get; set; }            // 1=Small, 2=Medium, 3=Big
    public float AngularSpeed { get; set; }  // °/с, диапазон 30–120

    public bool IsDead() => _killed;
    public void Kill() { _killed = true; }
    public void AcceptWith(IGroupVisitor visitor) { visitor.Visit(this); }
}
```

**AsteroidVisual.cs** — создать новый файл по паттерну ShipVisual (один файл = ViewModel + Visual):

Namespace: `SelStrom.Asteroids`

Usings:
```
using System;
using Shtl.Mvvm;
using UnityEngine;
```

`AsteroidViewModel` (наследует `AbstractViewModel`):
- `public ReactiveValue<Vector2> Position = new();` — для биндинга позиции
- `public ReactiveValue<Sprite> Sprite = new();` — для смены спрайта при спавне
- `public Action<Collision2D> OnCollision;` — callback для Game

`AsteroidVisual` (наследует `AbstractWidgetView<AsteroidViewModel>`, реализует `IEntityView`):
- `[SerializeField] private SpriteRenderer _spriteRenderer;`
- `private int _prefabInstanceId;`
- `private float _angularSpeed;` — угловая скорость вращения (°/с)
- `public int PrefabInstanceId => _prefabInstanceId;`
- `public void SetPrefabId(int id) { _prefabInstanceId = id; }`
- `public void SetAngularSpeed(float speed) { _angularSpeed = speed; }`

`protected override void OnConnected()`:
```csharp
ViewModel.Position.Connect(val =>
    transform.position = new Vector3(val.x, val.y, transform.position.z));
ViewModel.Sprite.Connect(val => {
    if (_spriteRenderer != null && val != null) { _spriteRenderer.sprite = val; }
});
```

`private void Update()` — визуальное вращение (D-06, не RotateComponent):
```csharp
transform.Rotate(0f, 0f, _angularSpeed * Time.deltaTime);
```

`private void OnCollisionEnter2D(Collision2D col)`:
```csharp
ViewModel?.OnCollision?.Invoke(col);
```

`public void Dispose()` — вызвать `base.Disconnect()` для снятия биндингов ViewModel.

Слой GameObject: Layer должен быть 8 ("Asteroid") — задаётся в prefab через Inspector, НЕ в коде.
  </action>
  <verify>
    <automated>grep -n "public float AngularSpeed" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Entities/AsteroidModel.cs && grep -n "class AsteroidViewModel" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/AsteroidVisual.cs && grep -n "class AsteroidVisual" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/AsteroidVisual.cs && grep -n "OnCollision" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/AsteroidVisual.cs</automated>
  </verify>
  <acceptance_criteria>
    - AsteroidModel.cs содержит строку `public float AngularSpeed { get; set; }`
    - AsteroidVisual.cs существует и содержит `class AsteroidViewModel : AbstractViewModel`
    - AsteroidVisual.cs содержит `class AsteroidVisual : AbstractWidgetView<AsteroidViewModel>, IEntityView`
    - AsteroidVisual.cs содержит `public Action<Collision2D> OnCollision`
    - AsteroidVisual.cs содержит `transform.Rotate(0f, 0f, _angularSpeed`
    - AsteroidVisual.cs содержит `private void OnCollisionEnter2D`
  </acceptance_criteria>
  <done>AsteroidModel.cs расширен, AsteroidVisual.cs создан с полным MVVM паттерном по образцу ShipVisual.</done>
</task>

<task type="auto">
  <name>Задача 2: EntitiesCatalog.CreateAsteroid + Release + Model.GroupCreator</name>
  <files>
    Assets/Scripts/Application/EntitiesCatalog.cs
    Assets/Scripts/Model/Model.cs
  </files>
  <read_first>
    - Assets/Scripts/Application/EntitiesCatalog.cs — существующий код: Connect, CreateBullet, Release, RegisterPrefab
    - Assets/Scripts/Model/Model.cs — GroupCreator с заглушками Visit(AsteroidModel) и Visit(UfoBigModel)
    - Assets/Scripts/Scripts/Configs/AsteroidData.cs — структура данных конфига
    - Assets/Scripts/View/AsteroidVisual.cs — только что созданный (Задача 1)
    - .planning/codebase/CONVENTIONS.md — switch expressions паттерн для выбора data по size
    - .planning/phases/04-asteroids-progression/04-CONTEXT.md — D-05 (скорости Large=2, Medium=3, Small=4), D-16 (спавн вдали от корабля)
  </read_first>
  <action>
**EntitiesCatalog.cs** — добавить:

1. В `Connect()` дополнить регистрацию prefabs после существующих строк:
```csharp
RegisterPrefab(_configs.AsteroidBig.Prefab);
RegisterPrefab(_configs.AsteroidMedium.Prefab);
RegisterPrefab(_configs.AsteroidSmall.Prefab);
```

2. Добавить публичный метод `CreateAsteroid`:
```csharp
public AsteroidModel CreateAsteroid(AsteroidData data, Vector2 position, Vector2 velocity)
{
    // 1. Model
    var model = _modelFactory.Create<AsteroidModel>();
    model.Size = data == _configs.AsteroidBig ? 3
               : data == _configs.AsteroidMedium ? 2
               : 1;
    model.AngularSpeed = UnityEngine.Random.Range(30f, 120f)
                       * (UnityEngine.Random.value > 0.5f ? 1f : -1f);
    model.Move.Position.Value = position;
    model.Move.Direction = velocity.normalized;
    model.Move.Speed.Value = velocity.magnitude;

    // 2. View
    var view = _pool.Get<AsteroidVisual>(data.Prefab);
    view.SetPrefabId(data.Prefab.GetInstanceID());
    view.SetAngularSpeed(model.AngularSpeed);

    // 3. Выбрать случайный спрайт из SpriteVariants
    var sprite = data.SpriteVariants != null && data.SpriteVariants.Length > 0
        ? data.SpriteVariants[UnityEngine.Random.Range(0, data.SpriteVariants.Length)]
        : null;

    // 4. ViewModel + Bindings
    var vm = new AsteroidViewModel();
    var bind = new EventBindingContext();
    bind.From(model.Move.Position).To(vm.Position);
    vm.Sprite.Value = sprite;

    view.Connect(vm);

    // 5. Register
    _modelToView[model] = view;
    _modelToBind[model] = bind;
    _modelToGo[model] = view.gameObject;
    _goToModel[view.gameObject] = model;

    return model;
}
```

3. В `Release()` — в блоке `if (_modelToView.TryGetValue(model, out var view))` добавить ветку для AsteroidVisual в switch/else-if:
```csharp
else if (view is AsteroidVisual av) { av.Dispose(); }
```

**Model.cs** — реализовать заглушку `Visit(AsteroidModel model)` в GroupCreator:
```csharp
public void Visit(AsteroidModel model)
{
    _owner.GetSystem<MoveSystem>().Add(model, model.Move);
}
```

`Visit(UfoBigModel model)` оставить пустым (Phase 5).
  </action>
  <verify>
    <automated>grep -n "CreateAsteroid" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/EntitiesCatalog.cs && grep -n "AsteroidBig.Prefab\|AsteroidMedium.Prefab\|AsteroidSmall.Prefab" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/EntitiesCatalog.cs && grep -n "Visit(AsteroidModel" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Model.cs</automated>
  </verify>
  <acceptance_criteria>
    - EntitiesCatalog.cs содержит `public AsteroidModel CreateAsteroid(AsteroidData data, Vector2 position, Vector2 velocity)`
    - EntitiesCatalog.cs содержит `RegisterPrefab(_configs.AsteroidBig.Prefab)`
    - EntitiesCatalog.cs содержит `RegisterPrefab(_configs.AsteroidMedium.Prefab)`
    - EntitiesCatalog.cs содержит `RegisterPrefab(_configs.AsteroidSmall.Prefab)`
    - EntitiesCatalog.cs содержит `av.Dispose()` в Release для AsteroidVisual
    - EntitiesCatalog.cs содержит `model.AngularSpeed = UnityEngine.Random.Range(30f, 120f)`
    - Model.cs метод `Visit(AsteroidModel model)` содержит `GetSystem<MoveSystem>().Add(model, model.Move)` (не пустая заглушка)
  </acceptance_criteria>
  <done>EntitiesCatalog умеет создавать и освобождать астероиды всех трёх размеров. GroupCreator регистрирует астероид в MoveSystem — он начинает двигаться.</done>
</task>

</tasks>

<verification>
После выполнения обоих задач:
1. `grep -rn "class AsteroidVisual\|class AsteroidViewModel" Assets/Scripts/` — оба класса найдены в AsteroidVisual.cs
2. `grep -n "CreateAsteroid\|RegisterPrefab.*Asteroid" Assets/Scripts/Application/EntitiesCatalog.cs` — метод и регистрация prefabs присутствуют
3. `grep -n "GetSystem<MoveSystem>().Add(model, model.Move)" Assets/Scripts/Model/Model.cs` — Visit(AsteroidModel) реализован
4. MCP compile — проект собирается без ошибок
</verification>

<success_criteria>
- AsteroidModel имеет поле AngularSpeed (float, публичное)
- AsteroidVisual.cs создан по паттерну ShipVisual: ViewModel с Position/Sprite/OnCollision, Visual с Update()-вращением
- EntitiesCatalog.CreateAsteroid(AsteroidData, Vector2, Vector2) создаёт модель + view + биндинги + регистрирует в словарях
- Release() корректно освобождает AsteroidVisual
- Model.GroupCreator.Visit(AsteroidModel) регистрирует в MoveSystem (не пустой)
- Проект компилируется
</success_criteria>

<output>
После завершения создать `.planning/phases/04-asteroids-progression/04-01-SUMMARY.md`
</output>
