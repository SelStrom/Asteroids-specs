---
phase: 03-core-mechanics
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - Assets/Scripts/View/Base/BaseVisual.cs
  - Assets/Scripts/View/Base/IEntityView.cs
  - Assets/Scripts/View/Bindings/BindingToExtensions.cs
  - Assets/Scripts/View/Components/GuiText.cs
  - Assets/Scripts/View/ShipVisual.cs
  - Assets/Scripts/View/BulletVisual.cs
  - Assets/Scripts/View/EffectVisual.cs
  - Assets/Scripts/View/HudVisual.cs
  - Assets/Scripts/View/TitleScreenView.cs
  - Assets/Scripts/Application/IApplicationComponent.cs
  - Assets/Scripts/Application/ApplicationEntry.cs
  - Assets/Scripts/Application/Application.cs
  - Assets/Scripts/Application/ModelFactory.cs
  - Assets/Scripts/Application/ViewFactory.cs
  - Assets/Scripts/Application/EntitiesCatalog.cs
  - Assets/Scripts/Application/Game.cs
  - Assets/Scripts/Application/Screens/AbstractScreen.cs
  - Assets/Scripts/Application/Screens/TitleScreen.cs
  - Assets/Scripts/Application/Screens/GameScreen.cs
  - Assets/Media/prefabs/ship.prefab
  - Assets/Media/prefabs/bullet.prefab
  - Assets/Media/prefabs/bullet_enemy.prefab
  - Assets/Media/prefabs/gui/gui_text.prefab
  - Assets/Media/configs/GameData.asset
  - Assets/Media/configs/UserGunData.asset
  - Assets/Media/configs/UfoGunData.asset
  - Assets/Scenes/Main.unity
autonomous: false
requirements:
  - SHIP-06
  - SHIP-07
  - SHIP-08
  - SHOT-05
  - SHOT-06

must_haves:
  truths:
    - "Unity запускает Play Mode без ошибок в консоли"
    - "TitleScreen отображается при старте с текстом ASTEROIDS и кнопкой PLAY"
    - "Нажатие PLAY скрывает TitleScreen и показывает HUD"
    - "Корабль появляется в центре сцены и видим"
    - "Корабль управляется: A/D вращают, W добавляет скорость"
  artifacts:
    - path: "Assets/Scripts/Application/ApplicationEntry.cs"
      provides: "MonoBehaviour точка входа — Awake создаёт Application, Start запускает"
    - path: "Assets/Scripts/Application/EntitiesCatalog.cs"
      provides: "Фабрика+реестр: CreateShip, CreateBullet, Release — с Object Pool"
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "Игровой процесс: Start, respawn логика, коллизии, PlayerInput подписки"
    - path: "Assets/Scripts/View/ShipVisual.cs"
      provides: "ShipViewModel + ShipVisual — биндинги Position/IsThrusting/IsVisible"
    - path: "Assets/Scenes/Main.unity"
      provides: "Сцена с ApplicationEntry на GameObject, Camera, Canvas с TitleScreen и Hud"
  key_links:
    - from: "Assets/Scripts/Application/ApplicationEntry.cs"
      to: "Assets/Scripts/Application/Application.cs"
      via: "ApplicationEntry.Awake создаёт Application и передаёт _configs (GameData)"
    - from: "Assets/Scripts/Application/Game.cs"
      to: "Assets/Scripts/Input/PlayerInput.cs"
      via: "Game.Start() подписывается на OnRotateAction/OnTrustAction/OnAttackAction/OnLaserAction"
    - from: "Assets/Scripts/View/ShipVisual.cs"
      to: "Assets/Scripts/Application/Game.cs"
      via: "ShipVisual.ViewModel.OnCollision callback → Game.OnShipCollided → Ship.Kill()"
---

<objective>
Создать View layer (Visual + ViewModel классы), Application layer (ApplicationEntry, Application, Game, EntitiesCatalog, фабрики, экраны), prefabs, config assets и настроить сцену Main.unity.

Purpose: Это финальная «сборка» — соединяет Model из Plan 02 с Unity игровым миром. После этого плана игра должна запуститься и корабль должен управляться.
Output: 20 C# файлов + 4 prefabs + 3 config assets + настроенная сцена. Checkpoint: визуальная проверка.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-mechanics/3-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-core-mechanics/03-UI-SPEC.md
@.planning/phases/03-core-mechanics/03-RESEARCH.md
@.planning/phases/03-core-mechanics/03-02-SUMMARY.md

<interfaces>
<!-- Из Plan 02 (созданы): -->

<!-- ShipModel: Setup(GameData), Reset(), IsInvulnerable, Move, Rotate, Thrust, Gun, Laser -->
<!-- BulletModel: Setup(float lifeTime), IsEnemy, Move, LifeTime -->

<!-- com.shtl.mvvm (PackageCache): -->
<!--
AbstractViewModel          — public ReactiveValue<T> fields (не properties!); constructor собирает через reflection
AbstractWidgetView<TVM>    — protected IEventBindingContext Bind; TViewModel ViewModel; OnConnected() override
EventBindingContext         — CleanUp() снимает все биндинги
BindingToExtensions        — From(ReactiveValue<Vector2>).To(Transform) → Transform.position
-->

<!-- GameData (Assets/Scripts/Configs/GameData.cs): -->
<!--
Ship.Prefab, Ship.MainSprite, Ship.ThrustSprite, Ship.ThrustUnitsPerSecond=6, Ship.MaxSpeed=15, Ship.Gun
Bullet.Prefab, Bullet.EnemyPrefab, Bullet.LifeTimeSeconds=2 (int), Bullet.Speed=20
Laser.Prefab, Laser.BeamEffectLifetimeSec=0.5, Laser.LaserUpdateDurationSec=10, Laser.LaserMaxShoots=3
AsteroidInitialCount=10, SpawnAllowedRadius=20, SpawnNewEnemyDurationSec=25
VfxBlowPrefab = null (D-06)
-->

<!-- UI-SPEC данные: -->
<!--
Canvas: Screen Space - Camera, 1920×1080 (CanvasScaler Scale With Screen Size)
TitleScreen: TMP "ASTEROIDS" size=56, кнопка "PLAY" size=28
Hud: якорь top-left (16,-16), 5 TMP меток с шагом 24px: coordinates/rotation_angle/speed_text/laser_shoot_count/laser_reload_time
Camera: Orthographic Size=22.5, Position=(0,0,-10), Clear Color Black
Wrap-around граница: |x|>40, |y|>22.5
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: View layer — Visual классы и Application layer C#</name>
  <files>
    Assets/Scripts/View/Base/BaseVisual.cs
    Assets/Scripts/View/Base/IEntityView.cs
    Assets/Scripts/View/Bindings/BindingToExtensions.cs
    Assets/Scripts/View/Components/GuiText.cs
    Assets/Scripts/View/ShipVisual.cs
    Assets/Scripts/View/BulletVisual.cs
    Assets/Scripts/View/EffectVisual.cs
    Assets/Scripts/View/HudVisual.cs
    Assets/Scripts/View/TitleScreenView.cs
    Assets/Scripts/Application/IApplicationComponent.cs
    Assets/Scripts/Application/ApplicationEntry.cs
    Assets/Scripts/Application/Application.cs
    Assets/Scripts/Application/ModelFactory.cs
    Assets/Scripts/Application/ViewFactory.cs
    Assets/Scripts/Application/EntitiesCatalog.cs
    Assets/Scripts/Application/Game.cs
    Assets/Scripts/Application/Screens/AbstractScreen.cs
    Assets/Scripts/Application/Screens/TitleScreen.cs
    Assets/Scripts/Application/Screens/GameScreen.cs
  </files>
  <read_first>
    - .planning/codebase/ARCHITECTURE.md §"Слой представления", §"Поток данных — Коллизия", §"Слой приложения"
    - .planning/phases/03-core-mechanics/03-RESEARCH.md §Pattern 1 (CreateShip код), §Pattern 3 (биндинги), §Pattern 4 (ActionScheduler respawn)
    - .planning/phases/03-core-mechanics/03-RESEARCH.md §Pitfall 1 (ReactiveValue одна подписка — CleanUp при Release)
    - .planning/phases/03-core-mechanics/03-UI-SPEC.md §HUD Data Bindings, §Interaction Contract, §Thrust indicator
    - .planning/phases/03-core-mechanics/3-CONTEXT.md §D-05 (VfxBlow null), §D-08 (OnCollisionEnter2D)
    - Assets/Scripts/View/ (проверить текущее состояние)
    - Assets/Scripts/Application/ (проверить текущее состояние)
  </read_first>
  <action>
Создать View layer и Application layer. Все в namespace `SelStrom.Asteroids`. Биндинги в `SelStrom.Asteroids.Bindings`.

**View/Base/IEntityView.cs**:
```csharp
using UnityEngine;
namespace SelStrom.Asteroids
{
    public interface IEntityView
    {
        GameObject gameObject { get; }
        int PrefabInstanceId { get; }
    }
}
```

**View/Base/BaseVisual.cs** — абстрактный MonoBehaviour вью для игровых сущностей:
```csharp
using UnityEngine;
namespace SelStrom.Asteroids
{
    public abstract class BaseVisual : MonoBehaviour, IEntityView
    {
        [SerializeField] private GameObject _prefabReference; // для PrefabInstanceId
        private int _prefabInstanceId;

        public int PrefabInstanceId => _prefabInstanceId;

        public void SetPrefabId(int id)
        {
            _prefabInstanceId = id;
        }
    }
}
```

**View/Bindings/BindingToExtensions.cs** — extension-методы биндингов:
```csharp
using Shtl.Mvvm;
using UnityEngine;
namespace SelStrom.Asteroids.Bindings
{
    public static class BindingToExtensions
    {
        // Биндинг Position ObservableValue → Transform.position
        public static void To(this IEventBinding<Vector2> binding, Transform transform)
        {
            binding.Action = val => transform.position = new Vector3(val.x, val.y, transform.position.z);
        }

        // Биндинг float → Transform.eulerAngles.z (для вращения)
        public static void ToRotation(this IEventBinding<float> binding, Transform transform)
        {
            binding.Action = val => transform.eulerAngles = new Vector3(0f, 0f, val);
        }

        // Биндинг bool → SpriteRenderer.enabled
        public static void To(this IEventBinding<bool> binding, SpriteRenderer spriteRenderer)
        {
            binding.Action = val => spriteRenderer.enabled = val;
        }
    }
}
```

ПРИМЕЧАНИЕ: `BindingToExtensions` зависит от конкретного API `com.shtl.mvvm`. Реальный API нужно проверить в PackageCache по пути `Packages/com.shtl.mvvm@c7bda1c328/`. Паттерн `Bind.From(x).To(y)` из ARCHITECTURE.md означает что `EventBindingContext` имеет метод создающий биндинг. Проверить реальные типы в PackageCache и адаптировать.

**View/Components/GuiText.cs** — TMP компонент для HUD меток:
```csharp
using TMPro;
using UnityEngine;
namespace SelStrom.Asteroids
{
    public class GuiText : MonoBehaviour
    {
        [SerializeField] private TextMeshProUGUI _text;

        public void SetText(string text)
        {
            if (_text != null) { _text.text = text; }
        }
    }
}
```

**View/ShipVisual.cs** — ShipViewModel + ShipVisual в одном файле (конвенция):
```csharp
using Shtl.Mvvm;
using UnityEngine;
using UnityEngine.EventSystems;
using System;
namespace SelStrom.Asteroids
{
    public class ShipViewModel : AbstractViewModel
    {
        public ReactiveValue<Vector2> Position = new();
        public ReactiveValue<bool> IsThrusting = new();
        public ReactiveValue<bool> IsVisible = new();
        public Action<Collision2D> OnCollision;
    }

    public class ShipVisual : AbstractWidgetView<ShipViewModel>, IEntityView
    {
        [SerializeField] private SpriteRenderer _spriteRenderer;
        [SerializeField] private Sprite _mainSprite;
        [SerializeField] private Sprite _thrustSprite;
        private int _prefabInstanceId;

        public int PrefabInstanceId => _prefabInstanceId;
        public void SetPrefabId(int id) { _prefabInstanceId = id; }

        protected override void OnConnected()
        {
            // Биндинг позиции: Position → Transform.position
            Bind.From(ViewModel.Position).Action = val =>
                transform.position = new Vector3(val.x, val.y, transform.position.z);

            // Биндинг тяги: IsThrusting → смена спрайта (SHIP-08)
            Bind.From(ViewModel.IsThrusting).Action = val =>
                _spriteRenderer.sprite = val ? _thrustSprite : _mainSprite;

            // Биндинг видимости: IsVisible → SpriteRenderer.enabled (мигание SHIP-07)
            Bind.From(ViewModel.IsVisible).Action = val =>
                _spriteRenderer.enabled = val;
        }

        private void OnCollisionEnter2D(Collision2D col)
        {
            // Коллизия → callback → Game (D-08)
            ViewModel?.OnCollision?.Invoke(col);
        }
    }
}
```

**View/BulletVisual.cs** — BulletViewModel + BulletVisual:
```csharp
using Shtl.Mvvm;
using UnityEngine;
using System;
namespace SelStrom.Asteroids
{
    public class BulletViewModel : AbstractViewModel
    {
        public ReactiveValue<Vector2> Position = new();
        public Action<Collision2D> OnCollision;
    }

    public class BulletVisual : AbstractWidgetView<BulletViewModel>, IEntityView
    {
        private int _prefabInstanceId;
        public int PrefabInstanceId => _prefabInstanceId;
        public void SetPrefabId(int id) { _prefabInstanceId = id; }

        protected override void OnConnected()
        {
            Bind.From(ViewModel.Position).Action = val =>
                transform.position = new Vector3(val.x, val.y, transform.position.z);
        }

        private void OnCollisionEnter2D(Collision2D col)
        {
            ViewModel?.OnCollision?.Invoke(col);
        }
    }
}
```

**View/EffectVisual.cs** — эффект (лазер, взрыв):
```csharp
using UnityEngine;
namespace SelStrom.Asteroids
{
    public class EffectVisual : MonoBehaviour, IEntityView
    {
        private int _prefabInstanceId;
        public int PrefabInstanceId => _prefabInstanceId;
        public void SetPrefabId(int id) { _prefabInstanceId = id; }
    }
}
```

**View/HudVisual.cs** — HudData + HudVisual (биндинги HUD к ShipViewModel, per UI-SPEC §HUD Data Bindings):
```csharp
using TMPro;
using UnityEngine;
namespace SelStrom.Asteroids
{
    public struct HudData
    {
        public ShipViewModel Ship;
    }

    public class HudVisual : MonoBehaviour
    {
        [SerializeField] private TextMeshProUGUI _coordinates;
        [SerializeField] private TextMeshProUGUI _rotationAngle;
        [SerializeField] private TextMeshProUGUI _speedText;
        [SerializeField] private TextMeshProUGUI _laserShootCount;
        [SerializeField] private TextMeshProUGUI _laserReloadTime;

        private ShipViewModel _ship;

        public void Connect(HudData data)
        {
            _ship = data.Ship;
        }

        private void Update()
        {
            if (_ship == null) { return; }
            var pos = _ship.Position.Value;
            if (_coordinates != null)    { _coordinates.text    = $"X: {pos.x:F1}  Y: {pos.y:F1}"; }
        }
    }
}
```

ПРИМЕЧАНИЕ: HudVisual в Phase 3 отображает debug данные (координаты). Полные биндинги через ObservableValue/ReactiveValue добавятся когда LaserComponent подключится в EntitiesCatalog. Update-loop — упрощённый подход вместо реактивных биндингов для HUD (допустимо для debug HUD Phase 3).

**View/TitleScreenView.cs** — TitleScreenViewModel + TitleScreenView:
```csharp
using Shtl.Mvvm;
using UnityEngine;
using UnityEngine.UI;
using System;
namespace SelStrom.Asteroids
{
    public class TitleScreenViewModel : AbstractViewModel
    {
        public Action OnPlayClicked;
    }

    public class TitleScreenView : AbstractWidgetView<TitleScreenViewModel>
    {
        [SerializeField] private Button _playButton;

        protected override void OnConnected()
        {
            _playButton.onClick.AddListener(() => ViewModel?.OnPlayClicked?.Invoke());
        }

        protected override void OnDisconnected()
        {
            _playButton.onClick.RemoveAllListeners();
        }
    }
}
```

**Application/IApplicationComponent.cs** — мост MonoBehaviour ↔ pure C#:
```csharp
using System;
namespace SelStrom.Asteroids
{
    public interface IApplicationComponent
    {
        event Action<float> OnUpdate;
        event Action OnPause;
        event Action OnResume;
    }
}
```

**Application/ApplicationEntry.cs** — единственный MonoBehaviour (D-12):
```csharp
using UnityEngine;
using SelStrom.Asteroids.Configs;
using System;
namespace SelStrom.Asteroids
{
    public class ApplicationEntry : MonoBehaviour, IApplicationComponent
    {
        [SerializeField] private GameData _configs;
        [SerializeField] private HudVisual _hudVisual;
        [SerializeField] private TitleScreenView _titleScreenView;
        [SerializeField] private GameObject _titleScreenGo;
        [SerializeField] private GameObject _hudGo;

        public event Action<float> OnUpdate;
        public event Action OnPause;
        public event Action OnResume;

        private Application _application;

        private void Awake()
        {
            _application = new Application();
            _application.Connect(this, _configs, _hudVisual, _titleScreenView, _titleScreenGo, _hudGo);
        }

        private void Start()
        {
            _application.Start();
        }

        private void Update()
        {
            OnUpdate?.Invoke(Time.deltaTime);
        }

        private void OnApplicationPause(bool pauseStatus)
        {
            if (pauseStatus) { OnPause?.Invoke(); } else { OnResume?.Invoke(); }
        }
    }
}
```

**Application/ModelFactory.cs**:
```csharp
namespace SelStrom.Asteroids
{
    public class ModelFactory
    {
        private Model _model;

        public void Connect(Model model)
        {
            _model = model;
        }

        public TModel Create<TModel>() where TModel : IGameEntityModel, new()
        {
            var model = new TModel();
            _model.AddEntity(model);
            return model;
        }

        public void Release(IGameEntityModel model)
        {
            // TODO @a.shatalov: model pool
        }
    }
}
```

**Application/ViewFactory.cs** — обёртка над GameObjectPool:
```csharp
using UnityEngine;
namespace SelStrom.Asteroids
{
    public class ViewFactory
    {
        private GameObjectPool _pool;

        public void Connect(GameObjectPool pool)
        {
            _pool = pool;
        }

        public T Get<T>(GameObject prefab) where T : Component, IEntityView
        {
            var view = _pool.Get<T>(prefab);
            view.SetPrefabId(prefab.GetInstanceID());
            return view;
        }

        public void Release(IEntityView view)
        {
            // Нужен prefab для возврата в пул. ViewFactory хранит маппинг prefabId → prefab
            // Упрощённо: _pool.Release передаёт gameObject и prefab
            // В EntitiesCatalog хранится ссылка на prefab через _prefabRegistry
        }
    }
}
```

**Application/EntitiesCatalog.cs** — фабрика+реестр сущностей (паттерн из ARCHITECTURE.md §CreateShip):
```csharp
using System;
using System.Collections.Generic;
using UnityEngine;
using Shtl.Mvvm;
using SelStrom.Asteroids.Configs;
namespace SelStrom.Asteroids
{
    public class EntitiesCatalog
    {
        private GameData _configs;
        private ModelFactory _modelFactory;
        private ViewFactory _viewFactory;
        private GameObjectPool _pool;

        private readonly Dictionary<IGameEntityModel, IEntityView> _modelToView = new();
        private readonly Dictionary<IGameEntityModel, EventBindingContext> _modelToBind = new();
        private readonly Dictionary<IGameEntityModel, GameObject> _modelToGo = new();
        private readonly Dictionary<GameObject, IGameEntityModel> _goToModel = new();
        private readonly Dictionary<int, GameObject> _prefabRegistry = new(); // prefabId → prefab

        public ShipModel Ship { get; private set; }

        public event Action<BulletModel> OnBulletCreated;

        public void Connect(GameData configs, ModelFactory modelFactory, ViewFactory viewFactory, GameObjectPool pool)
        {
            _configs = configs;
            _modelFactory = modelFactory;
            _viewFactory = viewFactory;
            _pool = pool;

            // Регистрация prefabs в пуле
            RegisterPrefab(_configs.Ship.Prefab);
            RegisterPrefab(_configs.Bullet.Prefab);
            RegisterPrefab(_configs.Bullet.EnemyPrefab);
        }

        private void RegisterPrefab(GameObject prefab)
        {
            if (prefab == null) { return; }
            _pool.Register(prefab);
            _prefabRegistry[prefab.GetInstanceID()] = prefab;
        }

        public ShipModel CreateShip()
        {
            // 1. Model
            var model = _modelFactory.Create<ShipModel>();
            model.Setup(_configs);
            model.Reset();

            // 2. View
            var view = _pool.Get<ShipVisual>(_configs.Ship.Prefab);
            view.SetPrefabId(_configs.Ship.Prefab.GetInstanceID());

            // 3. ViewModel + Bindings
            var vm = new ShipViewModel();
            var bind = new EventBindingContext();
            bind.From(model.Move.Position).To(vm.Position);
            bind.From(model.Thrust.IsActive).To(vm.IsThrusting);
            vm.IsVisible.Value = true;

            // Collision → Game через OnCollision callback (D-08)
            view.Connect(vm);

            // 4. Register
            _modelToView[model] = view;
            _modelToBind[model] = bind;
            _modelToGo[model] = view.gameObject;
            _goToModel[view.gameObject] = model;

            Ship = model;
            return model;
        }

        public BulletModel CreateBullet(Vector2 position, Vector2 velocity, bool isEnemy = false)
        {
            var prefab = isEnemy ? _configs.Bullet.EnemyPrefab : _configs.Bullet.Prefab;

            // 1. Model
            var model = _modelFactory.Create<BulletModel>();
            model.IsEnemy = isEnemy;
            model.Setup(_configs.Bullet.LifeTimeSeconds);
            model.Move.Position.Value = position;
            model.Move.Direction = velocity.normalized;
            model.Move.Speed.Value = velocity.magnitude;

            // 2. View
            var view = _pool.Get<BulletVisual>(prefab);
            view.SetPrefabId(prefab.GetInstanceID());

            // 3. ViewModel + Bindings
            var vm = new BulletViewModel();
            var bind = new EventBindingContext();
            bind.From(model.Move.Position).To(vm.Position);

            view.Connect(vm);

            // 4. Register
            _modelToView[model] = view;
            _modelToBind[model] = bind;
            _modelToGo[model] = view.gameObject;
            _goToModel[view.gameObject] = model;

            OnBulletCreated?.Invoke(model);
            return model;
        }

        public void Release(IGameEntityModel model)
        {
            if (_modelToBind.TryGetValue(model, out var bind))
            {
                bind.CleanUp(); // КРИТИЧНО: снять биндинги (Pitfall 1)
                _modelToBind.Remove(model);
            }

            if (_modelToView.TryGetValue(model, out var view))
            {
                var go = view.gameObject;
                var prefabId = view.PrefabInstanceId;
                if (_prefabRegistry.TryGetValue(prefabId, out var prefab))
                {
                    _pool.Release(go, prefab);
                }
                _goToModel.Remove(go);
                _modelToView.Remove(model);
                _modelToGo.Remove(model);
            }
        }

        public IGameEntityModel GetModelByGo(GameObject go)
        {
            _goToModel.TryGetValue(go, out var model);
            return model;
        }

        public void Dispose()
        {
            foreach (var bind in _modelToBind.Values) { bind.CleanUp(); }
            _modelToView.Clear();
            _modelToBind.Clear();
            _modelToGo.Clear();
            _goToModel.Clear();
            Ship = null;
        }
    }
}
```

ПРИМЕЧАНИЕ: `bind.From(x).To(y)` — реальный синтаксис зависит от com.shtl.mvvm API. Проверить в PackageCache. Скорее всего это `bind.From(observableValue).To(reactiveValue)` или через extension methods. Адаптировать по реальному API.

**Application/Game.cs** — игровой процесс, respawn логика (D-05, SHIP-06, SHIP-07):
```csharp
using System;
using UnityEngine;
using SelStrom.Asteroids.Configs;
namespace SelStrom.Asteroids
{
    public class Game
    {
        private GameData _configs;
        private EntitiesCatalog _catalog;
        private Model _model;
        private PlayerInput _input;
        private Action _onGameOver;

        private ShipModel _ship;
        private bool _isRunning;

        public void Connect(GameData configs, EntitiesCatalog catalog, Model model, PlayerInput input, Action onGameOver)
        {
            _configs = configs;
            _catalog = catalog;
            _model = model;
            _input = input;
            _onGameOver = onGameOver;
        }

        public void Start()
        {
            _isRunning = true;

            // Подписаться на ввод
            _input.OnRotateAction   += OnRotate;
            _input.OnTrustAction    += OnThrust;
            _input.OnAttackAction   += OnAttack;
            _input.OnLaserAction    += OnLaser;

            // Создать корабль
            _ship = _catalog.CreateShip();

            // Подписаться на коллизии
            if (_catalog.Ship != null && _catalog.GetViewByModel(_ship) is ShipVisual shipVisual)
            {
                shipVisual.ViewModel.OnCollision = OnShipCollided;
            }

            // Подписаться на уничтожение пуль
            _model.OnEntityDestroyed += OnEntityDestroyed;

            // Подписаться на создание пуль (для callback в GunSystem)
            _ship.Gun.OnShooting = OnUserGunShooting;
            _ship.Laser.OnLaserFired = OnUserLaserFired;
        }

        public void Stop()
        {
            _isRunning = false;
            _input.OnRotateAction   -= OnRotate;
            _input.OnTrustAction    -= OnThrust;
            _input.OnAttackAction   -= OnAttack;
            _input.OnLaserAction    -= OnLaser;
            _model.OnEntityDestroyed -= OnEntityDestroyed;
        }

        private void OnRotate(float direction)
        {
            if (_ship == null || _ship.IsDead()) { return; }
            _ship.Rotate.RotateDirection = direction;
        }

        private void OnThrust(bool isActive)
        {
            if (_ship == null || _ship.IsDead()) { return; }
            _ship.Thrust.IsActive.Value = isActive;
        }

        private void OnAttack()
        {
            if (_ship == null || _ship.IsDead() || _ship.IsInvulnerable) { return; }
            _ship.Gun.Shooting = true;
        }

        private void OnLaser()
        {
            if (_ship == null || _ship.IsDead() || _ship.IsInvulnerable) { return; }
            _ship.Laser.IsLaserFiring = true;
        }

        private void OnUserGunShooting(Model.Components.GunComponent gun)
        {
            // Вычислить позицию и скорость пули из носа корабля (SHOT-02)
            var shipPos = _ship.Move.Position.Value;
            var shipDir = _ship.Move.Direction;
            var bulletPos = shipPos + shipDir * 0.5f; // небольшое смещение вперёд от носа
            var shipVelocity = shipDir * _ship.Move.Speed.Value;
            var bulletVelocity = shipDir * _configs.Bullet.Speed + shipVelocity; // SHOT-02

            var bullet = _catalog.CreateBullet(bulletPos, bulletVelocity);
            bullet.Gun = gun; // сохраняем ссылку на GunComponent для декремента CurrentShoots
        }

        private void OnUserLaserFired(Model.Components.LaserComponent laser)
        {
            // Лазер: луч от носа корабля (D-03)
            // В Phase 3 — просто визуальный эффект через EffectVisual (без коллизий)
            Debug.Log("[Game] Laser fired");
        }

        private void OnShipCollided(Collision2D col)
        {
            if (_ship == null || _ship.IsInvulnerable) { return; }

            // Корабль погибает (D-05: без VFX, только скрыть)
            _ship.Kill();
            // Скрытие через ShipVisual.IsVisible = false происходит при Release в OnEntityDestroyed
        }

        private void OnEntityDestroyed(IGameEntityModel model)
        {
            if (model is BulletModel bullet)
            {
                // Декремент счётчика пуль в GunComponent
                if (bullet.Gun != null) { bullet.Gun.CurrentShoots--; }
            }

            if (model is ShipModel)
            {
                // Respawn через 2 секунды (D-05, SHIP-06)
                _model.ActionScheduler.Schedule(2f, RespawnShip);
            }

            _catalog.Release(model);
        }

        private void RespawnShip()
        {
            // Пересоздать корабль в центре (0, 0) (SHIP-06)
            _ship = _catalog.CreateShip();
            if (_catalog.GetViewByModel(_ship) is ShipVisual shipVisual)
            {
                shipVisual.ViewModel.OnCollision = OnShipCollided;
            }
            _ship.Gun.OnShooting = OnUserGunShooting;
            _ship.Laser.OnLaserFired = OnUserLaserFired;

            // Мигание неуязвимости 3 секунды (SHIP-07)
            _ship.IsInvulnerable = true;
            StartBlink(15, false); // 15 миганий × 0.2с = 3с (D-05)
        }

        private void StartBlink(int blinksLeft, bool visible)
        {
            if (_ship == null) { return; }

            if (blinksLeft <= 0)
            {
                // Мигание завершено — показать корабль постоянно
                if (_catalog.GetViewByModel(_ship) is ShipVisual sv)
                {
                    sv.ViewModel.IsVisible.Value = true;
                }
                _ship.IsInvulnerable = false;
                return;
            }

            if (_catalog.GetViewByModel(_ship) is ShipVisual shipVis)
            {
                shipVis.ViewModel.IsVisible.Value = visible;
            }

            _model.ActionScheduler.Schedule(0.15f, () => StartBlink(blinksLeft - 1, !visible));
        }

        public void Dispose()
        {
            Stop();
            _catalog = null;
            _model = null;
            _input = null;
        }
    }
}
```

ПРИМЕЧАНИЕ: BulletModel нужно добавить поле `GunComponent Gun` для связи с GunSystem — добавить в BulletModel.cs: `public Model.Components.GunComponent Gun;` — чтобы OnEntityDestroyed мог декрементировать CurrentShoots.

**Application/Application.cs** — корневой C# объект:
```csharp
using System;
using UnityEngine;
using SelStrom.Asteroids.Configs;
namespace SelStrom.Asteroids
{
    public class Application
    {
        private IApplicationComponent _entry;
        private GameData _configs;
        private Model _model;
        private EntitiesCatalog _catalog;
        private Game _game;
        private PlayerInput _input;
        private GameObjectPool _pool;
        private ModelFactory _modelFactory;
        private ViewFactory _viewFactory;

        private HudVisual _hudVisual;
        private TitleScreenView _titleScreenView;
        private GameObject _titleScreenGo;
        private GameObject _hudGo;

        public void Connect(IApplicationComponent entry, GameData configs, HudVisual hudVisual,
            TitleScreenView titleScreenView, GameObject titleScreenGo, GameObject hudGo)
        {
            _entry = entry;
            _configs = configs;
            _hudVisual = hudVisual;
            _titleScreenView = titleScreenView;
            _titleScreenGo = titleScreenGo;
            _hudGo = hudGo;

            _entry.OnUpdate += OnUpdate;

            // TitleScreen экран
            var titleScreen = new TitleScreen();
            titleScreen.Connect(_titleScreenView, OnGameStart);
        }

        public void Start()
        {
            // Вычислить размер игровой области по камере (Pitfall 6)
            var cam = Camera.main;
            var height = cam.orthographicSize * 2f; // 22.5 * 2 = 45
            var width = height * cam.aspect;         // 45 * (16/9) ≈ 80
            var gameArea = new Vector2(width, height);

            // Создать слои
            _pool = new GameObjectPool();
            _model = new Model();
            _model.GetSystem<MoveSystem>().SetGameArea(gameArea);

            _modelFactory = new ModelFactory();
            _modelFactory.Connect(_model);

            _viewFactory = new ViewFactory();
            _viewFactory.Connect(_pool);

            _catalog = new EntitiesCatalog();
            _catalog.Connect(_configs, _modelFactory, _viewFactory, _pool);

            _input = new PlayerInput();
            _input.Connect();

            _game = new Game();
            _game.Connect(_configs, _catalog, _model, _input, OnGameOver);
        }

        private void OnGameStart()
        {
            _titleScreenGo.SetActive(false);
            _hudGo.SetActive(true);
            _game.Start();
        }

        private void OnGameOver()
        {
            // Phase 4 — пока только лог
            Debug.Log("[Application] Game Over");
        }

        private void OnUpdate(float deltaTime)
        {
            _model?.Update(deltaTime);
        }

        public void Dispose()
        {
            _entry.OnUpdate -= OnUpdate;
            _game?.Dispose();
            _input?.Dispose();
            _catalog?.Dispose();
            _model = null;
        }
    }
}
```

**Application/Screens/AbstractScreen.cs**:
```csharp
using Shtl.Mvvm;
namespace SelStrom.Asteroids
{
    public abstract class AbstractScreen
    {
        protected EventBindingContext Bind { get; } = new();

        public virtual void Dispose()
        {
            Bind.CleanUp();
        }
    }
}
```

**Application/Screens/TitleScreen.cs**:
```csharp
using System;
namespace SelStrom.Asteroids
{
    public class TitleScreen : AbstractScreen
    {
        private TitleScreenView _view;

        public void Connect(TitleScreenView view, Action onStart)
        {
            _view = view;
            var vm = new TitleScreenViewModel();
            vm.OnPlayClicked = onStart;
            _view.Connect(vm);
        }
    }
}
```

**Application/Screens/GameScreen.cs** — Phase 3: только HUD отображение:
```csharp
namespace SelStrom.Asteroids
{
    public class GameScreen : AbstractScreen
    {
        private HudVisual _hudVisual;

        public void Connect(HudVisual hudVisual, ShipModel ship)
        {
            _hudVisual = hudVisual;
            if (_hudVisual != null)
            {
                _hudVisual.Connect(new HudData { Ship = ship != null ? new ShipViewModel() : null });
            }
        }
    }
}
```

ПОСЛЕ создания всех файлов: запустить `mcp__mcp-unity__compile` — исправить все ошибки компиляции.
Главная потенциальная проблема — реальный API `com.shtl.mvvm` (методы EventBindingContext.From, AbstractWidgetView.Bind). Проверить в `Packages/com.shtl.mvvm@c7bda1c328/Runtime/` и адаптировать код.
  </action>
  <verify>
    <automated>grep -l "class ApplicationEntry" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/ApplicationEntry.cs && grep "class EntitiesCatalog" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/EntitiesCatalog.cs && grep "class ShipVisual" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/ShipVisual.cs && grep "OnCollisionEnter2D" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/ShipVisual.cs</automated>
  </verify>
  <acceptance_criteria>
    - ApplicationEntry.cs содержит `class ApplicationEntry : MonoBehaviour, IApplicationComponent`
    - ApplicationEntry.cs имеет `[SerializeField] private GameData _configs`
    - EntitiesCatalog.cs содержит методы `CreateShip()`, `CreateBullet(Vector2, Vector2, bool)`, `Release(IGameEntityModel)`
    - EntitiesCatalog.Release вызывает `bind.CleanUp()` перед возвратом в пул
    - ShipVisual.cs содержит `OnCollisionEnter2D(Collision2D col)` вызывающий `ViewModel.OnCollision`
    - Game.cs содержит `RespawnShip()` использующий `_model.ActionScheduler.Schedule(2f, ...)`
    - Game.cs содержит `StartBlink(int blinksLeft, bool visible)` с `Schedule(0.15f, ...)`
    - Game.cs содержит `_ship.IsInvulnerable = true` при respawn
    - TitleScreen.cs содержит `Connect(TitleScreenView view, Action onStart)`
  </acceptance_criteria>
  <done>19 C# файлов созданы; Application layer полностью связывает Model → View → Unity; respawn с миганием реализован через ActionScheduler.</done>
</task>

<task type="auto">
  <name>Task 2: Prefabs, Config Assets, настройка сцены Main.unity</name>
  <files>
    Assets/Media/prefabs/ship.prefab
    Assets/Media/prefabs/bullet.prefab
    Assets/Media/prefabs/bullet_enemy.prefab
    Assets/Media/prefabs/gui/gui_text.prefab
    Assets/Media/configs/GameData.asset
    Assets/Media/configs/UserGunData.asset
    Assets/Media/configs/UfoGunData.asset
    Assets/Scenes/Main.unity
  </files>
  <read_first>
    - .planning/phases/03-core-mechanics/03-UI-SPEC.md §Prefab Inventory, §Sprite Contract, §Camera Contract, §Canvas Hierarchy Contract
    - .planning/phases/03-core-mechanics/1-CONTEXT.md §D-09 (слои Layer 7-11) — проверить Layer числа
    - .planning/phases/03-core-mechanics/3-CONTEXT.md §D-01 (спрайты из sub-sprites), §D-08 (Rigidbody2D Kinematic, GravityScale=0)
    - .planning/phases/03-core-mechanics/03-RESEARCH.md §Pitfall 5 (GravityScale=0 обязательно)
    - Assets/Media/sprites/asteroids.png (проверить доступные sub-sprites)
    - Assets/Scenes/Main.unity (текущее состояние сцены)
  </read_first>
  <action>
Создать prefabs, config assets и настроить сцену через MCP Unity инструменты или YAML/JSON сериализацию.

**Подход:** Поскольку это Unity проект, prefabs и сцены создаются через Unity Editor (не напрямую в файлах). Использовать `mcp__mcp-unity__compile` → `mcp__mcp-unity__play` + редактирование через Unity API если MCP позволяет, или создать C# Editor скрипт для автоматического создания.

**Создать Editor скрипт** `Assets/Editor/Phase3Setup.cs` для одноразового создания:

```csharp
#if UNITY_EDITOR
using UnityEditor;
using UnityEngine;
using UnityEngine.InputSystem;
using TMPro;
using UnityEngine.UI;
using SelStrom.Asteroids.Configs;

namespace SelStrom.Asteroids.Editor
{
    public static class Phase3Setup
    {
        [MenuItem("Asteroids/Setup Phase 3 Assets")]
        public static void SetupAll()
        {
            CreateConfigAssets();
            CreatePrefabs();
            SetupScene();
            AssetDatabase.SaveAssets();
            AssetDatabase.Refresh();
            UnityEngine.Debug.Log("[Phase3Setup] Done. All Phase 3 assets created.");
        }

        private static void CreateConfigAssets()
        {
            // UserGunData.asset — MaxShoots=5, ReloadDurationSec=2 (D-07)
            var userGun = ScriptableObject.CreateInstance<GunData>();
            userGun.MaxShoots = 5;
            userGun.ReloadDurationSec = 2f;
            AssetDatabase.CreateAsset(userGun, "Assets/Media/configs/UserGunData.asset");

            // UfoGunData.asset — MaxShoots=1, ReloadDurationSec=2 (D-07)
            var ufoGun = ScriptableObject.CreateInstance<GunData>();
            ufoGun.MaxShoots = 1;
            ufoGun.ReloadDurationSec = 2f;
            AssetDatabase.CreateAsset(ufoGun, "Assets/Media/configs/UfoGunData.asset");

            // GameData.asset — все числа из DATA_SCHEMA (D-07)
            var gameData = ScriptableObject.CreateInstance<GameData>();
            gameData.AsteroidInitialCount = 10;
            gameData.SpawnAllowedRadius = 20;
            gameData.SpawnNewEnemyDurationSec = 25f;
            gameData.LeaderboardId = "asteroids_highscores";
            // VfxBlowPrefab = null (D-06)
            // Bullet
            gameData.Bullet = new GameData.BulletData
            {
                LifeTimeSeconds = 2,
                Speed = 20f
                // Prefab и EnemyPrefab — назначить вручную после создания prefabs
            };
            // Laser
            gameData.Laser = new GameData.LaserData
            {
                BeamEffectLifetimeSec = 0.5f,
                LaserUpdateDurationSec = 10,
                LaserMaxShoots = 3
                // Prefab — null в Phase 3
            };
            // Ship
            gameData.Ship = new GameData.ShipData
            {
                ThrustUnitsPerSecond = 6f,
                MaxSpeed = 15f
                // Prefab, MainSprite, ThrustSprite, Gun — назначить через Inspector
            };
            AssetDatabase.CreateAsset(gameData, "Assets/Media/configs/GameData.asset");

            UnityEngine.Debug.Log("[Phase3Setup] Config assets created.");
        }

        private static void CreatePrefabs()
        {
            // ship.prefab — Layer 7 (Player), PolygonCollider2D, Rigidbody2D Kinematic GravityScale=0
            var shipGo = new GameObject("ship");
            shipGo.layer = 7; // Player layer (D-09)
            var shipSr = shipGo.AddComponent<SpriteRenderer>();
            shipSr.sortingOrder = 0;
            var shipRb = shipGo.AddComponent<Rigidbody2D>();
            shipRb.bodyType = RigidbodyType2D.Kinematic;
            shipRb.gravityScale = 0f; // Pitfall 5
            var shipCol = shipGo.AddComponent<PolygonCollider2D>();
            // Треугольник по UI-SPEC §Prefab Inventory: (1,0)(-0.5,0.5)(-0.5,-0.5)
            shipCol.SetPath(0, new Vector2[] {
                new Vector2(1f, 0f),
                new Vector2(-0.5f, 0.5f),
                new Vector2(-0.5f, -0.5f)
            });
            shipGo.AddComponent<ShipVisual>();
            PrefabUtility.SaveAsPrefabAsset(shipGo, "Assets/Media/prefabs/ship.prefab");
            Object.DestroyImmediate(shipGo);

            // bullet.prefab — Layer 9 (PlayerBullet), CircleCollider2D radius=0.2
            var bulletGo = new GameObject("bullet");
            bulletGo.layer = 9; // PlayerBullet layer
            bulletGo.AddComponent<SpriteRenderer>();
            var bulletRb = bulletGo.AddComponent<Rigidbody2D>();
            bulletRb.bodyType = RigidbodyType2D.Kinematic;
            bulletRb.gravityScale = 0f;
            var bulletCol = bulletGo.AddComponent<CircleCollider2D>();
            bulletCol.radius = 0.2f;
            bulletGo.AddComponent<BulletVisual>();
            PrefabUtility.SaveAsPrefabAsset(bulletGo, "Assets/Media/prefabs/bullet.prefab");
            Object.DestroyImmediate(bulletGo);

            // bullet_enemy.prefab — Layer 10 (EnemyBullet)
            var bulletEnemyGo = new GameObject("bullet_enemy");
            bulletEnemyGo.layer = 10; // EnemyBullet layer
            bulletEnemyGo.AddComponent<SpriteRenderer>();
            var beRb = bulletEnemyGo.AddComponent<Rigidbody2D>();
            beRb.bodyType = RigidbodyType2D.Kinematic;
            beRb.gravityScale = 0f;
            var beCol = bulletEnemyGo.AddComponent<CircleCollider2D>();
            beCol.radius = 0.2f;
            bulletEnemyGo.AddComponent<BulletVisual>();
            PrefabUtility.SaveAsPrefabAsset(bulletEnemyGo, "Assets/Media/prefabs/bullet_enemy.prefab");
            Object.DestroyImmediate(bulletEnemyGo);

            UnityEngine.Debug.Log("[Phase3Setup] Prefabs created.");
        }

        private static void SetupScene()
        {
            // Открыть Main.unity
            var scene = UnityEditor.SceneManagement.EditorSceneManager.OpenScene("Assets/Scenes/Main.unity");

            // Настроить Main Camera
            var cam = Camera.main;
            if (cam != null)
            {
                cam.orthographic = true;
                cam.orthographicSize = 22.5f; // UI-SPEC §Camera Contract
                cam.transform.position = new Vector3(0f, 0f, -10f);
                cam.clearFlags = CameraClearFlags.SolidColor;
                cam.backgroundColor = Color.black;
            }

            // Создать ApplicationEntry GameObject
            var appEntry = new GameObject("ApplicationEntry");
            appEntry.AddComponent<ApplicationEntry>();

            // Создать Canvas
            var canvasGo = new GameObject("UI");
            var canvas = canvasGo.AddComponent<Canvas>();
            canvas.renderMode = RenderMode.ScreenSpaceCamera;
            canvas.worldCamera = Camera.main;
            canvas.planeDistance = 1f;
            var scaler = canvasGo.AddComponent<UnityEngine.UI.CanvasScaler>();
            scaler.uiScaleMode = UnityEngine.UI.CanvasScaler.ScaleMode.ScaleWithScreenSize;
            scaler.referenceResolution = new Vector2(1920f, 1080f);
            scaler.matchWidthOrHeight = 0f; // Match Width
            canvasGo.AddComponent<UnityEngine.UI.GraphicRaycaster>();

            // TitleScreen — активен при старте
            var titleGo = new GameObject("TitleScreen");
            titleGo.transform.SetParent(canvasGo.transform, false);
            var titleRect = titleGo.GetComponent<RectTransform>();
            titleRect.anchorMin = Vector2.zero; titleRect.anchorMax = Vector2.one;
            titleRect.offsetMin = titleRect.offsetMax = Vector2.zero;

            // Title label "ASTEROIDS" size=56, anchor=center-top
            var titleLabel = new GameObject("Title");
            titleLabel.transform.SetParent(titleGo.transform, false);
            var titleTmp = titleLabel.AddComponent<TextMeshProUGUI>();
            titleTmp.text = "ASTEROIDS";
            titleTmp.fontSize = 56f;
            titleTmp.fontStyle = FontStyles.Bold;
            titleTmp.alignment = TextAlignmentOptions.Center;
            var titleLabelRect = titleLabel.GetComponent<RectTransform>();
            titleLabelRect.anchorMin = new Vector2(0.5f, 1f);
            titleLabelRect.anchorMax = new Vector2(0.5f, 1f);
            titleLabelRect.anchoredPosition = new Vector2(0f, -120f);
            titleLabelRect.sizeDelta = new Vector2(600f, 80f);

            // Play button "PLAY"
            var playGo = new GameObject("Play");
            playGo.transform.SetParent(titleGo.transform, false);
            var playBtn = playGo.AddComponent<Button>();
            var playRect = playGo.GetComponent<RectTransform>();
            playRect.anchorMin = playRect.anchorMax = new Vector2(0.5f, 0.5f);
            playRect.anchoredPosition = new Vector2(0f, -40f);
            playRect.sizeDelta = new Vector2(200f, 60f);
            var playTextGo = new GameObject("Text");
            playTextGo.transform.SetParent(playGo.transform, false);
            var playTmp = playTextGo.AddComponent<TextMeshProUGUI>();
            playTmp.text = "PLAY";
            playTmp.fontSize = 28f;
            playTmp.alignment = TextAlignmentOptions.Center;
            playTextGo.GetComponent<RectTransform>().anchorMin = Vector2.zero;
            playTextGo.GetComponent<RectTransform>().anchorMax = Vector2.one;
            playTextGo.GetComponent<RectTransform>().offsetMin = playTextGo.GetComponent<RectTransform>().offsetMax = Vector2.zero;
            titleGo.AddComponent<TitleScreenView>(); // MonoBehaviour

            // HUD — неактивен при старте (active=false)
            var hudGo = new GameObject("Hud");
            hudGo.transform.SetParent(canvasGo.transform, false);
            hudGo.SetActive(false);
            var hudRect = hudGo.GetComponent<RectTransform>();
            hudRect.anchorMin = new Vector2(0f, 1f);
            hudRect.anchorMax = new Vector2(0f, 1f);
            hudRect.anchoredPosition = new Vector2(16f, -16f);
            hudRect.sizeDelta = new Vector2(300f, 150f);
            // 5 TMP меток с шагом lg=24px
            CreateHudLabel(hudGo.transform, "coordinates",     new Vector2(0f, 0f),    "X: 0.0  Y: 0.0");
            CreateHudLabel(hudGo.transform, "rotation_angle",  new Vector2(0f, -24f),  "Angle: 0");
            CreateHudLabel(hudGo.transform, "speed_text",      new Vector2(0f, -48f),  "Speed: 0.0");
            CreateHudLabel(hudGo.transform, "laser_shoot_count",new Vector2(0f, -72f), "Laser: 3/3");
            CreateHudLabel(hudGo.transform, "laser_reload_time",new Vector2(0f, -96f), "Reload: 0.0s");
            hudGo.AddComponent<HudVisual>();

            // Сохранить сцену
            UnityEditor.SceneManagement.EditorSceneManager.SaveScene(scene);
            UnityEngine.Debug.Log("[Phase3Setup] Scene configured.");
        }

        private static void CreateHudLabel(Transform parent, string name, Vector2 pos, string defaultText)
        {
            var go = new GameObject(name);
            go.transform.SetParent(parent, false);
            var rect = go.GetComponent<RectTransform>();
            rect.anchorMin = rect.anchorMax = new Vector2(0f, 1f);
            rect.anchoredPosition = pos;
            rect.sizeDelta = new Vector2(280f, 24f);
            var tmp = go.AddComponent<TextMeshProUGUI>();
            tmp.text = defaultText;
            tmp.fontSize = 28f;
            tmp.alignment = TextAlignmentOptions.Left;
            tmp.enableWordWrapping = false; // UI-SPEC §Typography
        }
    }
}
#endif
```

**Выполнение:** После создания `Phase3Setup.cs` запустить `mcp__mcp-unity__compile`, затем через Unity Editor Menu выполнить `Asteroids → Setup Phase 3 Assets`. Или использовать MCP play mode + `ExecuteMenuItem`.

**Финальные ручные шаги в Unity Inspector** (или через Editor API):
1. `GameData.asset` → Ship.Prefab = ship.prefab; Ship.MainSprite = "ship" sub-sprite; Ship.ThrustSprite = "ship_thrust" sub-sprite; Ship.Gun = UserGunData.asset
2. `GameData.asset` → Bullet.Prefab = bullet.prefab; Bullet.EnemyPrefab = bullet_enemy.prefab
3. `ship.prefab` → ShipVisual → _mainSprite = "ship" sub-sprite; _thrustSprite = "ship_thrust" sub-sprite; _spriteRenderer = назначить
4. `ApplicationEntry` в сцене → _configs = GameData.asset; _hudVisual, _titleScreenView, _titleScreenGo, _hudGo = назначить из сцены
  </action>
  <verify>
    <automated>grep -l "Phase3Setup" /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase3Setup.cs 2>/dev/null && ls /Users/selstrom/work/projects/asteroids-specs/Assets/Media/configs/</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Editor/Phase3Setup.cs существует с `[MenuItem("Asteroids/Setup Phase 3 Assets")]`
    - Phase3Setup.cs создаёт GameData.asset с `AsteroidInitialCount = 10`, `SpawnAllowedRadius = 20`, `SpawnNewEnemyDurationSec = 25f`
    - Phase3Setup.cs создаёт UserGunData.asset с `MaxShoots = 5` и `ReloadDurationSec = 2f`
    - ship.prefab создаётся на Layer 7 с PolygonCollider2D и Rigidbody2D.GravityScale = 0
    - bullet.prefab создаётся на Layer 9 с CircleCollider2D radius=0.2 и GravityScale=0
    - Canvas создаётся с CanvasScaler ReferenceResolution=1920x1080 и ScaleWithScreenSize
    - Camera orthographicSize=22.5, Position=(0,0,-10), clearFlags=SolidColor, backgroundColor=black
    - Hud GameObject создаётся с SetActive(false)
    - TitleScreen содержит TMP text "ASTEROIDS" (size=56) и Button "PLAY" (size=28)
  </acceptance_criteria>
  <done>Editor скрипт создан; после запуска Menu → Asteroids → Setup Phase 3 Assets все prefabs, config assets и сцена настроены.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Полный игровой стек Phase 3:
    - 35+ C# файлов (Utils, Configs, Model ECS, Input, View MVVM, Application)
    - 4 prefabs (ship, bullet, bullet_enemy, gui_text)
    - 3 config assets (GameData, UserGunData, UfoGunData)
    - Настроенная сцена Main.unity с TitleScreen и HUD
    - PlayerActions.inputactions с Action Map "Player" (A/D/W/Space/Q/Escape)
  </what-built>
  <how-to-verify>
    1. В Unity Editor: нажать Compile (Ctrl+R или mcp__mcp-unity__compile) — 0 ошибок компиляции
    2. Убедиться что в Assets/Media/configs/ есть GameData.asset, UserGunData.asset, UfoGunData.asset
    3. Запустить Menu → Asteroids → Setup Phase 3 Assets (если не запускали ещё)
    4. Назначить ссылки в Inspector: GameData.asset → Ship.Prefab, Ship.MainSprite, Ship.ThrustSprite, Ship.Gun, Bullet.Prefab, Bullet.EnemyPrefab
    5. Назначить в сцене: ApplicationEntry → _configs = GameData.asset + _hudVisual/_titleScreenView/_titleScreenGo/_hudGo
    6. Нажать Play (mcp__mcp-unity__play)
    7. Ожидаемое: TitleScreen виден с "ASTEROIDS" и кнопкой "PLAY"
    8. Нажать PLAY → корабль появляется в центре, HUD видим
    9. Нажать A/D — корабль вращается
    10. Нажать W — корабль ускоряется (без замедления при отпускании)
    11. Нажать Space — пуля вылетает из носа (не более 5 одновременно)
    12. Долететь до края — корабль телепортируется на противоположный край
    13. Столкнуться с пулей (если нет астероидов — создать тест коллизию) или подождать — корабль гибнет, мигает, respawn через 2с
    14. Нажать Q — лазерный заряд уменьшается (HUD показывает Laser: 2/3)
  </how-to-verify>
  <resume-signal>Написать "approved" если корабль управляется и стреляет, или описать конкретные ошибки</resume-signal>
</task>

</tasks>

<verification>
Финальная верификация Phase 3 (выполнить перед завершением):
1. mcp__mcp-unity__compile → 0 ошибок
2. mcp__mcp-unity__play → игра запускается
3. В консоли Unity нет NullReferenceException
4. ShipVisual видим в сцене при Play Mode
5. HUD отображает координаты и лазер-заряды
</verification>

<success_criteria>
- Unity компилируется без ошибок (mcp__mcp-unity__compile)
- TitleScreen показывается при старте с текстом "ASTEROIDS" и кнопкой "PLAY"
- Нажатие PLAY запускает игру: корабль появляется в центре
- Корабль вращается (A/D), ускоряется (W), стреляет (Space), лазер (Q)
- Пули применяют wrap-around и исчезают через 2 секунды
- Лимит пуль 5 штук — 6-я не создаётся
- Корабль применяет wrap-around
- При гибели: корабль скрывается, через 2с появляется с миганием неуязвимости 3с
</success_criteria>

<output>
После завершения создать `.planning/phases/03-core-mechanics/03-03-SUMMARY.md`
</output>
