---
phase: 03-core-mechanics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Utils/GameObjectPool.cs
  - Assets/Scripts/Utils/GameUtils.cs
  - Assets/Scripts/Utils/CoroutineResult.cs
  - Assets/Scripts/Configs/BaseGameEntityData.cs
  - Assets/Scripts/Configs/GameData.cs
  - Assets/Scripts/Configs/GunData.cs
  - Assets/Scripts/Configs/AsteroidData.cs
  - Assets/Scripts/Configs/UfoData.cs
  - Assets/Scripts/Model/ActionScheduler.cs
  - Assets/Scripts/Model/Model.cs
  - Assets/Scripts/Model/Components/IModelComponent.cs
  - Assets/Scripts/Model/Components/MoveComponent.cs
  - Assets/Scripts/Model/Components/RotateComponent.cs
  - Assets/Scripts/Model/Components/ThrustComponent.cs
  - Assets/Scripts/Model/Components/GunComponent.cs
  - Assets/Scripts/Model/Components/LaserComponent.cs
  - Assets/Scripts/Model/Components/LifeTimeComponent.cs
  - Assets/Scripts/Model/Components/MoveToComponent.cs
  - Assets/Scripts/Model/Components/ShootToComponent.cs
  - Assets/Scripts/Model/Entities/IGameEntityModel.cs
  - Assets/Scripts/Model/Entities/IGroupVisitor.cs
  - Assets/Scripts/Model/Systems/BaseModelSystem.cs
  - Assets/Input/PlayerActions.inputactions
autonomous: true
requirements:
  - SHIP-01
  - SHIP-02
  - SHIP-03
  - SHIP-04
  - SHIP-05
  - SHOT-01
  - SHOT-02
  - SHOT-03
  - SHOT-04
  - SHOT-06

must_haves:
  truths:
    - "Проект компилируется без ошибок после создания всех инфраструктурных файлов"
    - "GameData.cs содержит все вложенные structs: BulletData, LaserData, ShipData"
    - "PlayerActions.inputactions имеет Action Map с именем Player и 5 Actions"
    - "Model зарегистрирует системы в порядке: RotateSystem→ThrustSystem→MoveSystem→LifeTimeSystem→GunSystem→LaserSystem"
  artifacts:
    - path: "Assets/Scripts/Utils/GameObjectPool.cs"
      provides: "Stack-based пул GameObject по prefab-id"
    - path: "Assets/Scripts/Configs/GameData.cs"
      provides: "Главный ScriptableObject конфиг с BulletData/LaserData/ShipData structs"
    - path: "Assets/Scripts/Model/Model.cs"
      provides: "Реестр систем и сущностей, Update loop с ActionScheduler"
    - path: "Assets/Scripts/Model/Components/MoveComponent.cs"
      provides: "Position ObservableValue<Vector2>, Direction Vector2, скорость"
    - path: "Assets/Input/PlayerActions.inputactions"
      provides: "Unity Input System конфиг с Action Map Player"
  key_links:
    - from: "Assets/Scripts/Model/Model.cs"
      to: "Assets/Scripts/Model/ActionScheduler.cs"
      via: "Model.Update вызывает ActionScheduler.Update в начале кадра"
    - from: "Assets/Scripts/Model/Model.cs"
      to: "Assets/Scripts/Model/Systems/"
      via: "RegisterSystem в конструкторе, Update в цикле"
---

<objective>
Создать инфраструктурный слой Phase 3: утилиты, конфиги (C# классы), ядро модели (компоненты + интерфейсы + ActionScheduler + Model), и Input Actions asset.

Purpose: Все остальные планы Phase 3 зависят от этого фундамента. Без него нельзя скомпилировать ни Ship, ни Bullet.
Output: 20+ C# файлов + Input Actions asset. Компиляция Unity без ошибок.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-mechanics/3-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/DATA_SCHEMA.md

<interfaces>
<!-- Пакет com.shtl.mvvm (уже в PackageCache) предоставляет: -->
<!-- Namespace: Shtl.Mvvm -->
<!--
ObservableValue<T>         — field-like observable, supports multiple listeners via event Action
ReactiveValue<T>           — single-subscriber reactive; Connect() throws if already bound
EventBindingContext        — container for bindings; CleanUp() unsubscribes all
AbstractViewModel          — base class; constructor collects all IReactiveValue fields via reflection
AbstractWidgetView<TVM>    — base MonoBehaviour; has protected IEventBindingContext Bind, TViewModel ViewModel
-->

<!-- com.unity.inputsystem уже в Packages/manifest.json -->
<!-- Namespace: UnityEngine.InputSystem -->
<!--
InputActionAsset           — .inputactions file
PlayerInput C# generated class имя зависит от имени Asset (PlayerActions → PlayerActions class)
Action Map "Player" → generated property: asset.Player
-->

<!-- Существующий файл Assets/Input/PlayerActions.inputactions может содержать Action Map "PlayerControls" -->
<!-- ВАЖНО: переименовать в "Player" или создать новый. Имя Action Map = "Player" (D-10) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utils + Configs C# классы</name>
  <files>
    Assets/Scripts/Utils/GameObjectPool.cs
    Assets/Scripts/Utils/GameUtils.cs
    Assets/Scripts/Utils/CoroutineResult.cs
    Assets/Scripts/Configs/BaseGameEntityData.cs
    Assets/Scripts/Configs/GameData.cs
    Assets/Scripts/Configs/GunData.cs
    Assets/Scripts/Configs/AsteroidData.cs
    Assets/Scripts/Configs/UfoData.cs
  </files>
  <read_first>
    - .planning/codebase/DATA_SCHEMA.md (все поля GameData, GunData, AsteroidData, UfoData — источник истины)
    - .planning/codebase/CONVENTIONS.md (namespace, naming, code style)
    - .planning/codebase/STRUCTURE.md (расположение файлов, asmdef)
    - Assets/Scripts/Utils/ (если есть файлы — проверить текущее состояние)
    - Assets/Scripts/Configs/ (если есть файлы — проверить текущее состояние)
  </read_first>
  <action>
Создать файлы Utils и Configs. Все файлы в namespace `SelStrom.Asteroids` (Utils) и `SelStrom.Asteroids.Configs` (Configs).

**Utils/GameObjectPool.cs** — Stack-based пул по prefab id:
```csharp
namespace SelStrom.Asteroids
{
    public class GameObjectPool
    {
        private readonly Dictionary<int, Stack<GameObject>> _pool = new();

        public T Get<T>(GameObject prefab) where T : Component
        {
            var id = prefab.GetInstanceID();
            if (!_pool.ContainsKey(id)) { _pool[id] = new Stack<GameObject>(); }
            var go = _pool[id].Count > 0 ? _pool[id].Pop() : Object.Instantiate(prefab);
            go.SetActive(true);
            return go.GetComponent<T>();
        }

        public void Release(GameObject go, GameObject prefab)
        {
            var id = prefab.GetInstanceID();
            if (!_pool.ContainsKey(id)) { throw new Exception($"[GameObjectPool] Unknown prefab id {id}"); }
            go.SetActive(false);
            _pool[id].Push(go);
        }

        public void Register(GameObject prefab)
        {
            var id = prefab.GetInstanceID();
            if (!_pool.ContainsKey(id)) { _pool[id] = new Stack<GameObject>(); }
        }
    }
}
```

**Utils/GameUtils.cs** — вычисление позиций спауна:
```csharp
namespace SelStrom.Asteroids
{
    public static class GameUtils
    {
        public static Vector2 GetRandomPositionOutsideRadius(Vector2 center, float minRadius, Vector2 gameArea)
        {
            Vector2 pos;
            do {
                pos = new Vector2(
                    UnityEngine.Random.Range(-gameArea.x / 2f, gameArea.x / 2f),
                    UnityEngine.Random.Range(-gameArea.y / 2f, gameArea.y / 2f));
            } while (Vector2.Distance(pos, center) < minRadius);
            return pos;
        }
    }
}
```

**Utils/CoroutineResult.cs** — типизированный результат корутины:
```csharp
namespace SelStrom.Asteroids
{
    public class CoroutineResult
    {
        public bool IsSuccess { get; protected set; }
        public string Error { get; protected set; }
        public void SetSuccess() => IsSuccess = true;
        public void SetError(string error) { IsSuccess = false; Error = error; }
    }

    public class CoroutineResult<T> : CoroutineResult
    {
        public T Value { get; private set; }
        public void SetSuccess(T value) { IsSuccess = true; Value = value; }
    }
}
```

**Configs/BaseGameEntityData.cs**:
```csharp
namespace SelStrom.Asteroids.Configs
{
    public abstract class BaseGameEntityData : ScriptableObject
    {
        public int Score;
    }
}
```

**Configs/GunData.cs**:
```csharp
namespace SelStrom.Asteroids.Configs
{
    [CreateAssetMenu(menuName = "Gun data")]
    public class GunData : ScriptableObject
    {
        public int MaxShoots;
        public float ReloadDurationSec;
    }
}
```

**Configs/AsteroidData.cs**:
```csharp
namespace SelStrom.Asteroids.Configs
{
    [CreateAssetMenu(menuName = "Asteroid data")]
    public class AsteroidData : BaseGameEntityData
    {
        public GameObject Prefab;
        public Sprite[] SpriteVariants;
    }
}
```

**Configs/UfoData.cs** — поле ShootDurationSec добавить (DATA_SCHEMA §примечание):
```csharp
namespace SelStrom.Asteroids.Configs
{
    [CreateAssetMenu(menuName = "Ufo data")]
    public class UfoData : BaseGameEntityData
    {
        public GameObject Prefab;
        public float Speed;
        public GunData Gun;
        public float ShootDurationSec;
    }
}
```

**Configs/GameData.cs** — главный конфиг. Все числа из DATA_SCHEMA (D-07):
```csharp
namespace SelStrom.Asteroids.Configs
{
    [CreateAssetMenu(menuName = "Game data")]
    public class GameData : ScriptableObject
    {
        [Header("Gameplay")]
        public int AsteroidInitialCount = 10;
        public int SpawnAllowedRadius = 20;
        public float SpawnNewEnemyDurationSec = 25f;

        [Space]
        public GameObject VfxBlowPrefab; // null в Phase 3 (D-06)

        [Space]
        public UfoData UfoBig;
        public UfoData Ufo;
        public AsteroidData AsteroidBig;
        public AsteroidData AsteroidMedium;
        public AsteroidData AsteroidSmall;

        [Space]
        public BulletData Bullet;
        public LaserData Laser;
        public ShipData Ship;

        [Space]
        public string LeaderboardId = "asteroids_highscores";

        [Serializable]
        public struct BulletData
        {
            public GameObject Prefab;
            public GameObject EnemyPrefab;
            public int LifeTimeSeconds;  // int как в DATA_SCHEMA (Pitfall 7): значение 2
            public float Speed;          // 20 ед/с
        }

        [Serializable]
        public struct LaserData
        {
            public GameObject Prefab;
            public float BeamEffectLifetimeSec;   // 0.5 с
            public int LaserUpdateDurationSec;    // 10 с
            public int LaserMaxShoots;            // 3 заряда
        }

        [Serializable]
        public struct ShipData
        {
            public GameObject Prefab;
            public Sprite MainSprite;
            public Sprite ThrustSprite;
            public float ThrustUnitsPerSecond;   // 6 ед/с²
            public float MaxSpeed;               // 15 ед/с
            public GunData Gun;
        }
    }
}
```

Добавить `using System;` и `using UnityEngine;` в каждый файл по необходимости.
  </action>
  <verify>
    <automated>grep -r "class GameData" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Configs/ && grep "LifeTimeSeconds" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Configs/GameData.cs && grep "LaserMaxShoots" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Configs/GameData.cs && grep "class GameObjectPool" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Utils/GameObjectPool.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/Configs/GameData.cs содержит `struct BulletData` с полем `public int LifeTimeSeconds`
    - Assets/Scripts/Configs/GameData.cs содержит `struct LaserData` с полем `public int LaserMaxShoots`
    - Assets/Scripts/Configs/GameData.cs содержит `struct ShipData` с полями `ThrustUnitsPerSecond` и `MaxSpeed`
    - Assets/Scripts/Configs/GunData.cs содержит `public int MaxShoots` и `public float ReloadDurationSec`
    - Assets/Scripts/Utils/GameObjectPool.cs содержит `class GameObjectPool`
    - Assets/Scripts/Utils/CoroutineResult.cs содержит `class CoroutineResult<T>`
    - Все Configs файлы используют namespace `SelStrom.Asteroids.Configs`
    - Все Utils файлы используют namespace `SelStrom.Asteroids`
  </acceptance_criteria>
  <done>Все 8 файлов созданы; GameData содержит все вложенные structs с полями по DATA_SCHEMA.</done>
</task>

<task type="auto">
  <name>Task 2: Model core — компоненты, интерфейсы, ActionScheduler, Model.cs</name>
  <files>
    Assets/Scripts/Model/Components/IModelComponent.cs
    Assets/Scripts/Model/Components/MoveComponent.cs
    Assets/Scripts/Model/Components/RotateComponent.cs
    Assets/Scripts/Model/Components/ThrustComponent.cs
    Assets/Scripts/Model/Components/GunComponent.cs
    Assets/Scripts/Model/Components/LaserComponent.cs
    Assets/Scripts/Model/Components/LifeTimeComponent.cs
    Assets/Scripts/Model/Components/MoveToComponent.cs
    Assets/Scripts/Model/Components/ShootToComponent.cs
    Assets/Scripts/Model/Entities/IGameEntityModel.cs
    Assets/Scripts/Model/Entities/IGroupVisitor.cs
    Assets/Scripts/Model/Systems/BaseModelSystem.cs
    Assets/Scripts/Model/ActionScheduler.cs
    Assets/Scripts/Model/Model.cs
  </files>
  <read_first>
    - .planning/codebase/ARCHITECTURE.md (паттерны BaseModelSystem, ActionScheduler, IGroupVisitor, Visitor dispatch)
    - .planning/codebase/CONVENTIONS.md (namespace Model.Components, naming conventions)
    - .planning/phases/03-core-mechanics/03-RESEARCH.md §Pattern 1-5 (примеры кода компонентов и систем)
    - Assets/Scripts/Configs/GameData.cs (только что создан — нужны типы BulletData/ShipData)
    - Assets/Scripts/Model/ (если есть файлы — проверить состояние)
  </read_first>
  <action>
Создать ECS-ядро модели. Компоненты в namespace `Model.Components`, остальное в `SelStrom.Asteroids`.

**Components/IModelComponent.cs** — маркер-интерфейс:
```csharp
namespace Model.Components
{
    public interface IModelComponent { }
}
```

**Components/MoveComponent.cs**:
```csharp
using Shtl.Mvvm;
using UnityEngine;
namespace Model.Components
{
    public class MoveComponent : IModelComponent
    {
        public ObservableValue<Vector2> Position = new();
        public ObservableValue<float> Speed = new();
        public Vector2 Direction;
    }
}
```

**Components/RotateComponent.cs**:
```csharp
namespace Model.Components
{
    public class RotateComponent : IModelComponent
    {
        public float RotateDirection; // -1, 0, 1
        public float RotateSpeed = 180f; // градусов/сек
    }
}
```

**Components/ThrustComponent.cs**:
```csharp
using Shtl.Mvvm;
namespace Model.Components
{
    public class ThrustComponent : IModelComponent
    {
        public ObservableValue<bool> IsActive = new();
        public float ThrustUnitsPerSecond;
        public float MaxSpeed;

        public ThrustComponent(float thrustUnitsPerSecond, float maxSpeed)
        {
            ThrustUnitsPerSecond = thrustUnitsPerSecond;
            MaxSpeed = maxSpeed;
        }
    }
}
```

**Components/GunComponent.cs**:
```csharp
using System;
using SelStrom.Asteroids.Configs;
namespace Model.Components
{
    public class GunComponent : IModelComponent
    {
        public bool Shooting;
        public int CurrentShoots;
        public GunData Config;
        public Action<GunComponent> OnShooting;

        public GunComponent(GunData config)
        {
            Config = config;
        }
    }
}
```

**Components/LaserComponent.cs** — для HUD биндингов ObservableValue:
```csharp
using Shtl.Mvvm;
namespace Model.Components
{
    public class LaserComponent : IModelComponent
    {
        public bool IsLaserFiring;
        public ObservableValue<int> CurrentShoots = new();
        public ObservableValue<float> ReloadTimeLeft = new();
        public int MaxShoots;
        public int LaserUpdateDurationSec;
        public float BeamEffectLifetimeSec;
        public System.Action<LaserComponent> OnLaserFired;
    }
}
```

**Components/LifeTimeComponent.cs**:
```csharp
namespace Model.Components
{
    public class LifeTimeComponent : IModelComponent
    {
        public float TimeLeft;

        public LifeTimeComponent(float lifeTime)
        {
            TimeLeft = lifeTime;
        }
    }
}
```

**Components/MoveToComponent.cs** — заглушка Phase 4 (UFO движение к цели):
```csharp
using UnityEngine;
namespace Model.Components
{
    public class MoveToComponent : IModelComponent
    {
        public UnityEngine.Vector2 Target;
        public float Speed;
    }
}
```

**Components/ShootToComponent.cs** — заглушка Phase 4 (UFO стрельба в цель):
```csharp
namespace Model.Components
{
    public class ShootToComponent : IModelComponent
    {
        public float ShootInterval;
        public float Timer;
    }
}
```

**Entities/IGameEntityModel.cs**:
```csharp
namespace SelStrom.Asteroids
{
    public interface IGameEntityModel
    {
        bool IsDead();
        void Kill();
        void AcceptWith(IGroupVisitor visitor);
    }
}
```

**Entities/IGroupVisitor.cs** — Phase 3 знает о ShipModel и BulletModel; заглушки для Phase 4:
```csharp
namespace SelStrom.Asteroids
{
    public interface IGroupVisitor
    {
        void Visit(ShipModel model);
        void Visit(BulletModel model);
        void Visit(AsteroidModel model);   // Phase 4
        void Visit(UfoBigModel model);     // Phase 4-5
    }
}
```

**Model/ActionScheduler.cs** — планировщик таймеров без корутин:
```csharp
using System;
using System.Collections.Generic;
namespace SelStrom.Asteroids
{
    public class ActionScheduler
    {
        private struct ScheduledAction
        {
            public float TriggerTime;
            public Action Callback;
        }

        private readonly List<ScheduledAction> _actions = new();
        private float _time;
        private float _nextTrigger = float.MaxValue;

        public void Update(float deltaTime)
        {
            _time += deltaTime;
            if (_time < _nextTrigger) { return; }

            _nextTrigger = float.MaxValue;
            for (var i = _actions.Count - 1; i >= 0; i--)
            {
                if (_actions[i].TriggerTime <= _time)
                {
                    var action = _actions[i].Callback;
                    _actions.RemoveAt(i);
                    action?.Invoke();
                }
                else
                {
                    _nextTrigger = Math.Min(_nextTrigger, _actions[i].TriggerTime);
                }
            }
        }

        public void Schedule(float delay, Action callback)
        {
            var triggerTime = _time + delay;
            _actions.Add(new ScheduledAction { TriggerTime = triggerTime, Callback = callback });
            _nextTrigger = Math.Min(_nextTrigger, triggerTime);
        }

        public void CleanUp()
        {
            _actions.Clear();
            _nextTrigger = float.MaxValue;
        }
    }
}
```

**Systems/BaseModelSystem.cs** — базовый класс + IModelSystem интерфейс:
```csharp
using System.Collections.Generic;
namespace SelStrom.Asteroids
{
    public interface IModelSystem
    {
        void Update(float deltaTime);
        void Remove(IGameEntityModel entity);
        void CleanUp();
    }

    public abstract class BaseModelSystem<TNode> : IModelSystem
    {
        protected readonly Dictionary<IGameEntityModel, TNode> _nodes = new();

        public void Add(IGameEntityModel entity, TNode node)
        {
            _nodes[entity] = node;
        }

        public void Update(float deltaTime)
        {
            foreach (var pair in _nodes)
            {
                UpdateNode(pair.Key, pair.Value, deltaTime);
            }
        }

        protected abstract void UpdateNode(IGameEntityModel entity, TNode node, float deltaTime);

        public void Remove(IGameEntityModel entity)
        {
            _nodes.Remove(entity);
        }

        public void CleanUp()
        {
            _nodes.Clear();
        }
    }
}
```

**Model/Model.cs** — реестр систем и сущностей. КРИТИЧНО: порядок регистрации систем строго фиксирован (Pitfall 4):
```csharp
using System;
using System.Collections.Generic;
using UnityEngine;
namespace SelStrom.Asteroids
{
    public class Model
    {
        private readonly List<IModelSystem> _systems = new();
        private readonly Dictionary<Type, IModelSystem> _typeToSystem = new();
        private readonly List<IGameEntityModel> _entities = new();
        private readonly List<IGameEntityModel> _newEntities = new();

        public ActionScheduler ActionScheduler { get; } = new();
        public Vector2 GameArea { get; set; }
        public int Score { get; set; }

        private GroupCreator _groupCreator;

        public Model()
        {
            // Строгий порядок из ARCHITECTURE.md (Pitfall 4)
            RegisterSystem(new RotateSystem());
            RegisterSystem(new ThrustSystem());
            RegisterSystem(new MoveSystem());
            RegisterSystem(new LifeTimeSystem());
            RegisterSystem(new GunSystem());
            RegisterSystem(new LaserSystem());
            RegisterSystem(new ShootToSystem()); // Phase 4 заглушка
            RegisterSystem(new MoveToSystem());  // Phase 4 заглушка
            _groupCreator = new GroupCreator(this);
        }

        private void RegisterSystem(IModelSystem system)
        {
            _systems.Add(system);
            _typeToSystem[system.GetType()] = system;
        }

        public TSystem GetSystem<TSystem>() where TSystem : IModelSystem
        {
            return (TSystem)_typeToSystem[typeof(TSystem)];
        }

        public void AddEntity(IGameEntityModel entity)
        {
            _newEntities.Add(entity);
        }

        public event Action<IGameEntityModel> OnEntityDestroyed;

        public void Update(float deltaTime)
        {
            ActionScheduler.Update(deltaTime);

            if (_newEntities.Count > 0)
            {
                foreach (var entity in _newEntities)
                {
                    _entities.Add(entity);
                    entity.AcceptWith(_groupCreator);
                }
                _newEntities.Clear();
            }

            foreach (var system in _systems)
            {
                system.Update(deltaTime);
            }

            for (var i = _entities.Count - 1; i >= 0; i--)
            {
                if (_entities[i].IsDead())
                {
                    OnEntityDestroyed?.Invoke(_entities[i]);
                    foreach (var system in _systems) { system.Remove(_entities[i]); }
                    _entities.RemoveAt(i);
                }
            }
        }

        public void CleanUp()
        {
            foreach (var system in _systems) { system.CleanUp(); }
            _entities.Clear();
            _newEntities.Clear();
            ActionScheduler.CleanUp();
            Score = 0;
        }

        // GroupCreator — вложенный класс Visitor для регистрации сущностей в системах
        private class GroupCreator : IGroupVisitor
        {
            private readonly Model _owner;
            public GroupCreator(Model owner) { _owner = owner; }

            public void Visit(ShipModel model)
            {
                _owner.GetSystem<RotateSystem>().Add(model, model.Rotate);
                _owner.GetSystem<ThrustSystem>().Add(model, (model.Thrust, model.Move, model.Rotate));
                _owner.GetSystem<MoveSystem>().Add(model, model.Move);
                _owner.GetSystem<GunSystem>().Add(model, model.Gun);
                _owner.GetSystem<LaserSystem>().Add(model, model.Laser);
            }

            public void Visit(BulletModel model)
            {
                _owner.GetSystem<MoveSystem>().Add(model, model.Move);
                _owner.GetSystem<LifeTimeSystem>().Add(model, model.LifeTime);
            }

            public void Visit(AsteroidModel model) { /* Phase 4 */ }
            public void Visit(UfoBigModel model)   { /* Phase 4-5 */ }
        }
    }
}
```

ПРИМЕЧАНИЕ: `Model.cs` ссылается на классы `RotateSystem`, `ThrustSystem`, `MoveSystem` и др., которые создаются в Plan 02. Это нормально — Unity компилирует все файлы в сборке вместе. Добавить пустые заглушки систем в Plan 02 чтобы Plan 01 скомпилировался отдельно — НЕ нужно, Plan 01 и Plan 02 выполняются последовательно (02 зависит от 01).

ВАЖНО: Также нужны заглушки классов ShipModel, BulletModel, AsteroidModel, UfoBigModel — они создаются в Plan 02. Model.cs будет компилироваться только после Plan 02.
  </action>
  <verify>
    <automated>grep -r "class ActionScheduler" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/ && grep "interface IGameEntityModel" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Entities/IGameEntityModel.cs && grep "interface IGroupVisitor" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Entities/IGroupVisitor.cs && grep "class BaseModelSystem" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Systems/BaseModelSystem.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/Model/ActionScheduler.cs содержит `class ActionScheduler` с методом `Schedule(float delay, Action callback)`
    - Assets/Scripts/Model/Model.cs содержит `public ActionScheduler ActionScheduler` и `public Vector2 GameArea`
    - Assets/Scripts/Model/Systems/BaseModelSystem.cs содержит `interface IModelSystem` и `abstract class BaseModelSystem<TNode>`
    - Assets/Scripts/Model/Entities/IGameEntityModel.cs содержит `IsDead()`, `Kill()`, `AcceptWith(IGroupVisitor)`
    - Assets/Scripts/Model/Components/MoveComponent.cs использует `ObservableValue<Vector2>` из Shtl.Mvvm
    - Assets/Scripts/Model/Components/ThrustComponent.cs имеет конструктор с параметрами `(float thrustUnitsPerSecond, float maxSpeed)`
    - Assets/Scripts/Model/Components/GunComponent.cs содержит `public Action<GunComponent> OnShooting`
    - Все компоненты используют namespace `Model.Components`
  </acceptance_criteria>
  <done>14 файлов Model layer созданы; Model.cs регистрирует системы в строго правильном порядке; ActionScheduler работает без MonoBehaviour.</done>
</task>

<task type="auto">
  <name>Task 3: Input Actions asset</name>
  <files>
    Assets/Input/PlayerActions.inputactions
  </files>
  <read_first>
    - .planning/phases/03-core-mechanics/3-CONTEXT.md §D-10, D-11 (Action Map = "Player", Actions: Rotate/Thrust/Attack/Laser/Back)
    - .planning/phases/03-core-mechanics/03-RESEARCH.md §Pitfall 2 (Action Map имя PlayerControls → нужно Player)
    - Assets/Input/PlayerActions.inputactions (проверить существующий файл — есть ли уже Action Map)
  </read_first>
  <action>
Создать или обновить `Assets/Input/PlayerActions.inputactions` с правильным Action Map именем `Player`.

**ВАЖНО (Pitfall 2 из RESEARCH.md):** Существующий файл может иметь Action Map `PlayerControls`. Нужно `Player`.

Создать файл с содержимым JSON InputActionAsset. Action Map: `Player`. Actions:
- `Rotate` — Value, Vector2 composite (A=left -1, D=right +1 через 1D Axis)
- `Thrust` — Button, W или Arrow Up
- `Attack` — Button, Space + Mouse Left Button
- `Laser` — Button, Q
- `Back` — Button, Escape

Формат JSON для `.inputactions`:
```json
{
    "name": "PlayerActions",
    "maps": [
        {
            "name": "Player",
            "id": "a1b2c3d4-0000-0000-0000-000000000001",
            "actions": [
                {
                    "name": "Rotate",
                    "type": "Value",
                    "id": "a1b2c3d4-0000-0000-0000-000000000002",
                    "expectedControlType": "Axis",
                    "processors": "",
                    "interactions": "",
                    "initialStateCheck": false
                },
                {
                    "name": "Thrust",
                    "type": "Button",
                    "id": "a1b2c3d4-0000-0000-0000-000000000003",
                    "expectedControlType": "Button",
                    "processors": "",
                    "interactions": "",
                    "initialStateCheck": false
                },
                {
                    "name": "Attack",
                    "type": "Button",
                    "id": "a1b2c3d4-0000-0000-0000-000000000004",
                    "expectedControlType": "Button",
                    "processors": "",
                    "interactions": "",
                    "initialStateCheck": false
                },
                {
                    "name": "Laser",
                    "type": "Button",
                    "id": "a1b2c3d4-0000-0000-0000-000000000005",
                    "expectedControlType": "Button",
                    "processors": "",
                    "interactions": "",
                    "initialStateCheck": false
                },
                {
                    "name": "Back",
                    "type": "Button",
                    "id": "a1b2c3d4-0000-0000-0000-000000000006",
                    "expectedControlType": "Button",
                    "processors": "",
                    "interactions": "",
                    "initialStateCheck": false
                }
            ],
            "bindings": [
                {
                    "name": "1D Axis",
                    "id": "a1b2c3d4-0000-0000-0000-000000000010",
                    "path": "1DAxis",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Rotate",
                    "isComposite": true,
                    "isPartOfComposite": false
                },
                {
                    "name": "negative",
                    "id": "a1b2c3d4-0000-0000-0000-000000000011",
                    "path": "<Keyboard>/a",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Rotate",
                    "isComposite": false,
                    "isPartOfComposite": true
                },
                {
                    "name": "positive",
                    "id": "a1b2c3d4-0000-0000-0000-000000000012",
                    "path": "<Keyboard>/d",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Rotate",
                    "isComposite": false,
                    "isPartOfComposite": true
                },
                {
                    "name": "negative",
                    "id": "a1b2c3d4-0000-0000-0000-000000000013",
                    "path": "<Keyboard>/leftArrow",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Rotate",
                    "isComposite": false,
                    "isPartOfComposite": true
                },
                {
                    "name": "positive",
                    "id": "a1b2c3d4-0000-0000-0000-000000000014",
                    "path": "<Keyboard>/rightArrow",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Rotate",
                    "isComposite": false,
                    "isPartOfComposite": true
                },
                {
                    "name": "",
                    "id": "a1b2c3d4-0000-0000-0000-000000000020",
                    "path": "<Keyboard>/w",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Thrust",
                    "isComposite": false,
                    "isPartOfComposite": false
                },
                {
                    "name": "",
                    "id": "a1b2c3d4-0000-0000-0000-000000000021",
                    "path": "<Keyboard>/upArrow",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Thrust",
                    "isComposite": false,
                    "isPartOfComposite": false
                },
                {
                    "name": "",
                    "id": "a1b2c3d4-0000-0000-0000-000000000030",
                    "path": "<Keyboard>/space",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Attack",
                    "isComposite": false,
                    "isPartOfComposite": false
                },
                {
                    "name": "",
                    "id": "a1b2c3d4-0000-0000-0000-000000000031",
                    "path": "<Mouse>/leftButton",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Attack",
                    "isComposite": false,
                    "isPartOfComposite": false
                },
                {
                    "name": "",
                    "id": "a1b2c3d4-0000-0000-0000-000000000040",
                    "path": "<Keyboard>/q",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Laser",
                    "isComposite": false,
                    "isPartOfComposite": false
                },
                {
                    "name": "",
                    "id": "a1b2c3d4-0000-0000-0000-000000000050",
                    "path": "<Keyboard>/escape",
                    "interactions": "",
                    "processors": "",
                    "groups": "",
                    "action": "Back",
                    "isComposite": false,
                    "isPartOfComposite": false
                }
            ]
        }
    ],
    "controlSchemes": []
}
```

После создания файла Unity автоматически сгенерирует C# класс при `AssetDatabase.Refresh()`. Также создать ПУСТОЙ placeholder для `Assets/Scripts/Input/Generated/PlayerActions.cs` если генерации нет — но лучше позволить Unity сгенерировать.

Проверить настройки Generate C# Class в Unity Inspector для `.inputactions` — флаг `Generate C# Class` должен быть включён, путь генерации: `Assets/Scripts/Input/Generated/PlayerActions.cs`.

Поскольку включить Generate C# Class через JSON нельзя — это делается через Unity Editor — добавить в `.meta` файл или оставить на ручную проверку. **Вместо этого создать `Assets/Scripts/Input/Generated/PlayerActions.cs` вручную** с минимальной реализацией, совместимой с PlayerInput.cs (Plan 02).
  </action>
  <verify>
    <automated>grep -l '"name": "Player"' /Users/selstrom/work/projects/asteroids-specs/Assets/Input/PlayerActions.inputactions</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Input/PlayerActions.inputactions существует и содержит `"name": "Player"` (Action Map name)
    - Файл содержит 5 actions: Rotate, Thrust, Attack, Laser, Back
    - Действие Rotate привязано к клавишам A (negative) и D (positive), плюс стрелки
    - Действие Thrust привязано к W и стрелке вверх
    - Действие Attack привязано к Space и Mouse Left Button
    - Действие Laser привязано к Q
    - Действие Back привязано к Escape
  </acceptance_criteria>
  <done>PlayerActions.inputactions создан с Action Map "Player" и 5 actions с правильными bindings.</done>
</task>

</tasks>

<verification>
После выполнения всех задач:
1. `mcp__mcp-unity__compile` — компиляция успешна (Plan 01 не компилируется полностью без Plan 02, но структура файлов верна)
2. Все 20+ файлов существуют в правильных директориях
3. `grep -r "namespace SelStrom.Asteroids.Configs" Assets/Scripts/Configs/` — все конфиги в правильном namespace
4. `grep -r "namespace Model.Components" Assets/Scripts/Model/Components/` — все компоненты в правильном namespace
</verification>

<success_criteria>
- Assets/Scripts/Utils/ содержит 3 файла: GameObjectPool.cs, GameUtils.cs, CoroutineResult.cs
- Assets/Scripts/Configs/ содержит 5 файлов: BaseGameEntityData.cs, GameData.cs, GunData.cs, AsteroidData.cs, UfoData.cs
- Assets/Scripts/Model/ содержит ActionScheduler.cs, Model.cs
- Assets/Scripts/Model/Components/ содержит 9 файлов всех компонентов
- Assets/Scripts/Model/Entities/ содержит IGameEntityModel.cs, IGroupVisitor.cs
- Assets/Scripts/Model/Systems/ содержит BaseModelSystem.cs
- Assets/Input/PlayerActions.inputactions содержит Action Map "Player" с 5 actions
- Все числовые значения в GameData.cs соответствуют DATA_SCHEMA (BulletData.LifeTimeSeconds=int, LaserMaxShoots=3, Ship.MaxSpeed=15, Ship.ThrustUnitsPerSecond=6)
</success_criteria>

<output>
После завершения создать `.planning/phases/03-core-mechanics/03-01-SUMMARY.md`
</output>
