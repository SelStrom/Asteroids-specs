---
phase: 08-mcp-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts
  - Packages/com.shtl.mcp-unity/Editor~/Server/dist/index.js
autonomous: false
requirements:
  - MCP-07

must_haves:
  truths:
    - "MCP tool get_game_state доступен в Claude Code (отображается в списке tools)"
    - "В Play Mode get_game_state возвращает { score, wave, lives, isPlaying: true }"
    - "Вне Play Mode get_game_state возвращает { isPlaying: false } без ошибок"
  artifacts:
    - path: "Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts"
      provides: "TypeScript MCP tool get_game_state"
      contains: "get_game_state"
    - path: "Packages/com.shtl.mcp-unity/Editor~/Server/dist/index.js"
      provides: "Собранный JS с новым tool"
      contains: "get_game_state"
  key_links:
    - from: "index.ts (MCP tool)"
      to: "McpUnityBridge /get_game_state endpoint"
      via: "callBridge('/get_game_state')"
      pattern: "callBridge.*get_game_state"
---

<objective>
Добавить MCP tool get_game_state в TypeScript MCP-сервер, пересобрать dist/,
и верифицировать сквозную работу: TypeScript → HTTP bridge → RuntimeBridgeProxy → Game state.

Purpose: Замкнуть цепочку — Claude Code может вызвать get_game_state и получить
живое состояние игры во время Editor Play Mode.

Output: Обновлённый index.ts + пересобранный dist/index.js + человеческая верификация.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mcp-runtime/08-CONTEXT.md
@.planning/phases/08-mcp-runtime/08-RESEARCH.md
@.planning/phases/08-mcp-runtime/08-01-SUMMARY.md

<interfaces>
<!-- Ключевые паттерны из существующего index.ts -->

Из Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts (существующий стиль tools):
```typescript
// Паттерн для tool без параметров (аналог play/stop/compile):
server.tool("play", "Enter Unity Editor Play Mode (EditorApplication.isPlaying = true)", {}, async () => {
  const text = await callBridge("/play");
  return { content: [{ type: "text", text }] };
});

// callBridge сигнатура:
async function callBridge(endpoint: string, body?: object, timeoutMs = 15000): Promise<string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить get_game_state tool в index.ts и пересобрать</name>
  <files>Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts</files>
  <read_first>
    - Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts (весь файл — найти место для нового tool)
    - .planning/phases/08-mcp-runtime/08-RESEARCH.md (Pattern 4: TypeScript tool без параметров)
    - .planning/phases/08-mcp-runtime/08-CONTEXT.md (D-05: ожидаемый формат ответа)
  </read_first>
  <action>
**Шаг A: Добавить tool в index.ts**

Найти последний объявленный `server.tool(...)` в файле (вероятно set_scene_object_field или аналог).
После него добавить новый tool (per D-05 из CONTEXT.md и Pattern 4 из RESEARCH.md):

```typescript
// Инструмент get_game_state — получить живое состояние игры из Runtime (MCP-07)
server.tool(
  "get_game_state",
  "Get current game state: score, wave, lives, isPlaying. Returns { score, wave, lives, isPlaying }. Works only in Play Mode; returns isPlaying:false otherwise.",
  {},
  async () => {
    const text = await callBridge("/get_game_state");
    return { content: [{ type: "text", text }] };
  }
);
```

Без параметров (пустой объект `{}`) — аналогично play/stop/compile.

**Шаг B: Пересобрать TypeScript**

```bash
cd /Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor~/Server && npm run build
```

Если Node.js недоступен — отредактировать `dist/index.js` напрямую:
найти последний зарегистрированный tool (через поиск `"get_game_state"` или последний `server.tool`) и добавить аналогичный блок в compiled JS.

После сборки dist/index.js должен содержать строку `get_game_state`.
  </action>
  <verify>
    <automated>grep -n "get_game_state" /Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor~/Server/src/index.ts && grep -n "get_game_state" /Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor~/Server/dist/index.js</automated>
  </verify>
  <acceptance_criteria>
    - index.ts содержит строку `"get_game_state"` (регистрация tool)
    - index.ts содержит `callBridge("/get_game_state")`
    - dist/index.js содержит строку `get_game_state` (после rebuild)
    - npm run build завершается без ошибок (exit code 0) если Node.js доступен
  </acceptance_criteria>
  <done>TypeScript tool get_game_state добавлен, dist пересобран</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Верификация сквозной работы get_game_state</name>
  <what-built>
    - RuntimeBridgeProxy.cs — статический буфер состояния с #if UNITY_EDITOR guard (Plan 01, Task 1)
    - Game.IsRunning + Game.Wave — новые публичные свойства (Plan 01, Task 2)
    - Application.Score/Wave/Lives/IsRunning — делегирующие свойства (Plan 01, Task 2)
    - ApplicationEntry.Update() — обновляет буфер каждый кадр (Plan 01, Task 3)
    - McpUnityBridge /get_game_state endpoint — reflection к RuntimeBridgeProxy (Plan 01, Task 4)
    - index.ts get_game_state tool — вызывает /get_game_state через callBridge (Plan 02, Task 1)
  </what-built>
  <how-to-verify>
    **Проверка 1 — вне Play Mode (ожидаемый результат: isPlaying:false)**

    В Claude Code вызвать MCP tool get_game_state.
    Ожидаемый ответ:
    ```json
    {"success":true,"score":0,"wave":0,"lives":0,"isPlaying":false}
    ```

    **Проверка 2 — в Play Mode (ожидаемый результат: isPlaying:true)**

    1. Вызвать MCP tool `play` — Unity войдёт в Play Mode
    2. Подождать 2-3 секунды для запуска игры (TitleScreen)
    3. Вызвать MCP tool `get_game_state`
    Ожидаемый ответ (на TitleScreen, игра не запущена):
    ```json
    {"success":true,"score":0,"wave":0,"lives":0,"isPlaying":false}
    ```
    (isPlaying=false потому что Game._isRunning=false на TitleScreen — per D-01)

    4. В Unity Editor нажать кнопку Play в игре (или через UI кликнуть на сцене)
    5. Вызвать MCP tool `get_game_state` ещё раз
    Ожидаемый ответ (игра запущена):
    ```json
    {"success":true,"score":0,"wave":1,"lives":3,"isPlaying":true}
    ```

    **Проверка 3 — после остановки Play Mode**

    1. Вызвать MCP tool `stop`
    2. Вызвать MCP tool `get_game_state`
    Ожидаемый ответ:
    ```json
    {"success":true,"score":0,"wave":0,"lives":0,"isPlaying":false}
    ```

    **Если что-то не работает:**
    - Ошибка "Unknown endpoint" → McpUnityBridge не перезагрузился, перезапустить Unity или скомпилировать заново
    - Ошибка соединения → McpUnityBridge не запущен, проверить Unity Editor открыт
    - isPlaying всегда false → проверить GameEntry вызывает Game.Start() при нажатии Play в игре
  </how-to-verify>
  <files>N/A — checkpoint не создаёт файлы</files>
  <action>Человек выполняет верификацию согласно инструкциям в how-to-verify</action>
  <verify>Человек подтверждает результат через resume-signal</verify>
  <done>Все 3 проверки пройдены: вне Play Mode isPlaying:false, в Play Mode isPlaying:true, после Stop isPlaying:false</done>
  <resume-signal>Введите "approved" если все 3 проверки прошли успешно, или опишите проблему</resume-signal>
</task>

</tasks>

<verification>
Фаза 08 завершена когда:
1. MCP tool compile возвращает 0 ошибок
2. get_game_state вне Play Mode → { isPlaying: false }
3. get_game_state в Play Mode с запущенной игрой → { score: N, wave: N, lives: N, isPlaying: true }
4. get_game_state после Stop → { isPlaying: false } (Reset() сработал)
</verification>

<success_criteria>
- MCP-07: get_game_state возвращает { score, wave, lives, isPlaying } в Play Mode с запущенной игрой
- MCP-11: RuntimeBridgeProxy.cs компилируется без ошибок (WebGL guard через #if UNITY_EDITOR)
- Вне Play Mode get_game_state возвращает { isPlaying: false } без исключений
- Пакет com.shtl.mcp-unity не имеет compile-time зависимости от игрового кода
</success_criteria>

<output>
После завершения создать .planning/phases/08-mcp-runtime/08-02-SUMMARY.md
</output>
