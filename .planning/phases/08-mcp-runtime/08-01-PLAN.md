---
phase: 08-mcp-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Application/RuntimeBridgeProxy.cs
  - Assets/Scripts/Application/Game.cs
  - Assets/Scripts/Application/Application.cs
  - Assets/Scripts/Application/ApplicationEntry.cs
  - Packages/com.shtl.mcp-unity/Editor/McpUnityBridge.cs
autonomous: true
requirements:
  - MCP-11
  - MCP-07

must_haves:
  truths:
    - "RuntimeBridgeProxy.cs компилируется без ошибок под Editor и WebGL"
    - "McpUnityBridge отвечает на /get_game_state без исключений (в Edit Mode возвращает isPlaying:false)"
    - "ApplicationEntry обновляет буфер RuntimeBridgeProxy каждый кадр и сбрасывает при OnDestroy"
  artifacts:
    - path: "Assets/Scripts/Application/RuntimeBridgeProxy.cs"
      provides: "Статический буфер состояния игры с #if UNITY_EDITOR guard"
      contains: "public static class RuntimeBridgeProxy"
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "Публичные свойства IsRunning и Wave"
      exports: ["public bool IsRunning", "public int Wave"]
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "Делегирующие свойства Score/Wave/Lives/IsRunning для ApplicationEntry"
      exports: ["public int Score", "public int Wave", "public int Lives", "public bool IsRunning"]
    - path: "Packages/com.shtl.mcp-unity/Editor/McpUnityBridge.cs"
      provides: "Endpoint /get_game_state через reflection"
      contains: "case \"/get_game_state\":"
  key_links:
    - from: "Assets/Scripts/Application/ApplicationEntry.cs"
      to: "Assets/Scripts/Application/RuntimeBridgeProxy.cs"
      via: "UpdateState() в Update()"
      pattern: "RuntimeBridgeProxy\\.UpdateState"
    - from: "Packages/com.shtl.mcp-unity/Editor/McpUnityBridge.cs"
      to: "Assets/Scripts/Application/RuntimeBridgeProxy.cs"
      via: "C# Reflection — Type.GetType"
      pattern: "RuntimeBridgeProxy, Assembly-CSharp"
---

<objective>
Реализовать C# сторону MCP Runtime Bridge: создать RuntimeBridgeProxy статический буфер,
добавить публичные свойства в Game/Application, обновить ApplicationEntry для записи состояния
каждый кадр и добавить endpoint /get_game_state в McpUnityBridge.

Purpose: MCP tool get_game_state должен возвращать живое состояние игры без compile-time
зависимости пакета com.shtl.mcp-unity от игрового кода.

Output: 1 новый файл (RuntimeBridgeProxy.cs) + 4 изменённых C# файла, компилируются без ошибок.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-mcp-runtime/08-CONTEXT.md
@.planning/phases/08-mcp-runtime/08-RESEARCH.md

<interfaces>
<!-- Ключевые интерфейсы для исполнителя — извлечены из кодовой базы -->

Из Assets/Scripts/Application/Game.cs (строки 48-52 — уже существующие свойства):
```csharp
public int Score => _model.Score;
public int Lives => _lives;
public int HighScore => _highScore;
public int WaveNumber => _waveNumber;
// private bool _isRunning; — существует (строка 19), публичного свойства НЕТ
// private int _waveNumber; — существует (строка 24), нужен алиас Wave или использовать WaveNumber
```

Из Assets/Scripts/Application/Application.cs (строка 13):
```csharp
private Game _game; // приватное поле, нет публичных свойств Score/Wave/Lives/IsRunning
```

Из Assets/Scripts/Application/ApplicationEntry.cs (строки 49-61):
```csharp
private Application _application; // через него читаем состояние

private void Update()
{
    OnUpdate?.Invoke(Time.deltaTime);
    // Сюда добавить: RuntimeBridgeProxy.UpdateState(...)
}

private void OnDestroy()
{
    _application?.Dispose();
    // Сюда добавить: RuntimeBridgeProxy.Reset();
}
```

Из Packages/com.shtl.mcp-unity/Editor/McpUnityBridge.cs (строки 146-188):
```csharp
switch (path)
{
    case "/compile":  HandleCompile(context); break;
    case "/play":     HandlePlay(context); break;
    case "/stop":     HandleStop(context); break;
    case "/list_scenes": HandleListScenes(context); break;
    // ... другие cases ...
    default:
        context.Response.StatusCode = 404;
        SendJsonRaw(context, "{\"success\":false,\"message\":\"Unknown endpoint\"}");
        break;
}
// Новый case добавляется перед default:
// private static void SendJsonRaw(HttpListenerContext context, string json) — уже существует
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать RuntimeBridgeProxy.cs — статический буфер с #if UNITY_EDITOR guard</name>
  <files>Assets/Scripts/Application/RuntimeBridgeProxy.cs</files>
  <read_first>
    - Assets/Scripts/Application/Game.cs (текущие свойства для понимания типов)
    - Assets/Scripts/Application/ApplicationEntry.cs (куда будет вызываться UpdateState)
    - .planning/phases/08-mcp-runtime/08-CONTEXT.md (D-04: точная структура файла)
  </read_first>
  <action>
Создать новый файл Assets/Scripts/Application/RuntimeBridgeProxy.cs со следующим содержимым
(per D-04 из CONTEXT.md):

```csharp
namespace SelStrom.Asteroids
{
    /// <summary>
    /// Статический буфер состояния игры для MCP Runtime Bridge.
    /// В Editor-сборке хранит Score/Wave/Lives/IsRunning, обновляется каждый кадр.
    /// В WebGL-сборке компилируется в no-op заглушки — нет зависимости от Editor API.
    /// </summary>
    public static class RuntimeBridgeProxy
    {
#if UNITY_EDITOR
        public static int Score;
        public static int Wave;
        public static int Lives;
        public static bool IsRunning;

        public static void UpdateState(int score, int wave, int lives, bool isRunning)
        {
            Score = score;
            Wave = wave;
            Lives = lives;
            IsRunning = isRunning;
        }

        public static void Reset()
        {
            Score = 0;
            Wave = 0;
            Lives = 0;
            IsRunning = false;
        }
#else
        // WebGL no-op заглушки — компилируются без Editor API
        public static void UpdateState(int score, int wave, int lives, bool isRunning) { }
        public static void Reset() { }
#endif
    }
}
```

ВАЖНО: НЕ добавлять `using UnityEditor;` на уровне файла — это сломает WebGL-билд (Pitfall 1 из RESEARCH.md).
Все поля и методы, зависящие от Editor, строго внутри `#if UNITY_EDITOR`.
  </action>
  <verify>
    <automated>grep -n "public static class RuntimeBridgeProxy" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/RuntimeBridgeProxy.cs</automated>
  </verify>
  <acceptance_criteria>
    - Файл Assets/Scripts/Application/RuntimeBridgeProxy.cs существует
    - Файл содержит строку `public static class RuntimeBridgeProxy`
    - Файл содержит строку `#if UNITY_EDITOR`
    - Файл содержит строку `#else`
    - Файл НЕ содержит строку `using UnityEditor;` на верхнем уровне
    - Файл содержит `public static void UpdateState(int score, int wave, int lives, bool isRunning)`
    - Файл содержит `public static void Reset()`
    - Файл содержит поля `public static int Score`, `public static int Wave`, `public static int Lives`, `public static bool IsRunning` (внутри #if UNITY_EDITOR)
  </acceptance_criteria>
  <done>RuntimeBridgeProxy.cs создан, компилируется через MCP tool compile без ошибок</done>
</task>

<task type="auto">
  <name>Task 2: Добавить публичные свойства IsRunning и Wave в Game.cs, делегирующие свойства в Application.cs</name>
  <files>Assets/Scripts/Application/Game.cs, Assets/Scripts/Application/Application.cs</files>
  <read_first>
    - Assets/Scripts/Application/Game.cs (строки 1-55 — существующие свойства, поля _isRunning и _waveNumber)
    - Assets/Scripts/Application/Application.cs (весь файл — понять структуру, добавить делегирующие свойства)
    - .planning/phases/08-mcp-runtime/08-CONTEXT.md (D-06, D-07: имена свойств)
    - .planning/phases/08-mcp-runtime/08-RESEARCH.md (Pitfall 5: Game.Wave vs WaveNumber)
  </read_first>
  <action>
**Шаг A: Game.cs**

Найти блок публичных свойств (строки 48-52):
```csharp
public int Score => _model.Score;
public int Lives => _lives;
public int HighScore => _highScore;
public int WaveNumber => _waveNumber;
```

Добавить после `public int WaveNumber => _waveNumber;` новые свойства (per D-06 из CONTEXT.md):
```csharp
public bool IsRunning => _isRunning;
public int Wave => _waveNumber;
```

`_isRunning` уже существует (строка 19). `_waveNumber` уже существует (строка 24).
`Wave` — алиас для `_waveNumber`, нужен отдельно от `WaveNumber` т.к. CONTEXT.md D-07 использует именно `Wave`.

**Шаг B: Application.cs**

В классе Application добавить публичные свойства-делегаты к _game (после существующих полей или перед методом Connect):
```csharp
// Свойства для RuntimeBridgeProxy — делегируют к _game (per D-07 из CONTEXT.md)
public int Score => _game?.Score ?? 0;
public int Wave => _game?.Wave ?? 0;
public int Lives => _game?.Lives ?? 0;
public bool IsRunning => _game?.IsRunning ?? false;
```

`_game` может быть null до вызова Start() или после Dispose() — null-guard через `?.` с `?? 0` обязателен.
  </action>
  <verify>
    <automated>grep -n "public bool IsRunning" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs && grep -n "public int Wave =>" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs && grep -n "public bool IsRunning" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Application.cs</automated>
  </verify>
  <acceptance_criteria>
    - Game.cs содержит строку `public bool IsRunning => _isRunning;`
    - Game.cs содержит строку `public int Wave => _waveNumber;`
    - Application.cs содержит строку `public int Score => _game?.Score ?? 0;`
    - Application.cs содержит строку `public int Wave => _game?.Wave ?? 0;`
    - Application.cs содержит строку `public int Lives => _game?.Lives ?? 0;`
    - Application.cs содержит строку `public bool IsRunning => _game?.IsRunning ?? false;`
    - После компиляции через MCP tool compile — 0 ошибок
  </acceptance_criteria>
  <done>Game.cs и Application.cs содержат нужные свойства, компиляция без ошибок</done>
</task>

<task type="auto">
  <name>Task 3: Обновить ApplicationEntry.cs — UpdateState в Update(), Reset() в OnDestroy()</name>
  <files>Assets/Scripts/Application/ApplicationEntry.cs</files>
  <read_first>
    - Assets/Scripts/Application/ApplicationEntry.cs (весь файл — текущий Update() и OnDestroy())
    - Assets/Scripts/Application/RuntimeBridgeProxy.cs (созданный в Task 1 — интерфейс UpdateState/Reset)
    - .planning/phases/08-mcp-runtime/08-CONTEXT.md (D-02, D-07: точные сигнатуры вызовов)
    - .planning/phases/08-mcp-runtime/08-RESEARCH.md (Pitfall 3: ApplicationEntry не имеет прямого доступа к Game, Pitfall 4: OnDestroy vs OnDisable)
  </read_first>
  <action>
**Шаг A: метод Update()**

Найти существующий метод (строки 49-52):
```csharp
private void Update()
{
    OnUpdate?.Invoke(Time.deltaTime);
}
```

Изменить на (per D-07 из CONTEXT.md):
```csharp
private void Update()
{
    OnUpdate?.Invoke(Time.deltaTime);
    RuntimeBridgeProxy.UpdateState(
        _application?.Score ?? 0,
        _application?.Wave ?? 0,
        _application?.Lives ?? 0,
        _application?.IsRunning ?? false
    );
}
```

**Шаг B: метод OnDestroy()**

Найти существующий метод (строки 59-62):
```csharp
private void OnDestroy()
{
    _application?.Dispose();
}
```

Изменить на:
```csharp
private void OnDestroy()
{
    _application?.Dispose();
    RuntimeBridgeProxy.Reset();
}
```

При стандартных настройках Unity 2022.3 LTS (Domain Reload включён — по умолчанию) OnDestroy
вызывается при каждом Stop Play Mode, что достаточно для корректного сброса (Pitfall 4 из RESEARCH.md).

`_application` хранит ссылку на Application — публичные свойства Score/Wave/Lives/IsRunning
добавлены в Task 2. Null-guard `?.` обязателен — _application может быть null при OnDestroy.
  </action>
  <verify>
    <automated>grep -n "RuntimeBridgeProxy" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/ApplicationEntry.cs</automated>
  </verify>
  <acceptance_criteria>
    - ApplicationEntry.cs содержит строку `RuntimeBridgeProxy.UpdateState(`
    - ApplicationEntry.cs содержит `_application?.Score ?? 0` (или аналог с null-guard)
    - ApplicationEntry.cs содержит `_application?.Wave ?? 0`
    - ApplicationEntry.cs содержит `_application?.Lives ?? 0`
    - ApplicationEntry.cs содержит `_application?.IsRunning ?? false`
    - ApplicationEntry.cs содержит строку `RuntimeBridgeProxy.Reset();` в OnDestroy
    - После компиляции через MCP tool compile — 0 ошибок
  </acceptance_criteria>
  <done>ApplicationEntry.cs обновлён: UpdateState вызывается каждый кадр, Reset при OnDestroy</done>
</task>

<task type="auto">
  <name>Task 4: Добавить case /get_game_state и HandleGetGameState() в McpUnityBridge.cs</name>
  <files>Packages/com.shtl.mcp-unity/Editor/McpUnityBridge.cs</files>
  <read_first>
    - Packages/com.shtl.mcp-unity/Editor/McpUnityBridge.cs (строки 140-210 — switch в HandleRequest; строки 600-700 — SendJsonRaw для понимания сигнатуры)
    - .planning/phases/08-mcp-runtime/08-CONTEXT.md (D-03, D-05: reflection и структура JSON-ответа)
    - .planning/phases/08-mcp-runtime/08-RESEARCH.md (Pattern 2: полный код HandleGetGameState; Pitfall 2: null check type; Pitfall 6: bool в JSON)
  </read_first>
  <action>
**Шаг A: Добавить case в switch**

Найти в методе HandleRequest блок switch (строка ~146), перед строкой `default:` добавить:
```csharp
case "/get_game_state":
    HandleGetGameState(context);
    break;
```

**Шаг B: Добавить метод HandleGetGameState**

После метода HandleRunMenuItem (или перед методом SendJsonRaw) добавить новый private static метод
(per D-03, D-05 из CONTEXT.md):

```csharp
private static void HandleGetGameState(HttpListenerContext context)
{
    var type = System.Type.GetType("SelStrom.Asteroids.RuntimeBridgeProxy, Assembly-CSharp");

    if (type == null || !EditorApplication.isPlaying)
    {
        SendJsonRaw(context, "{\"success\":true,\"score\":0,\"wave\":0,\"lives\":0,\"isPlaying\":false}");
        return;
    }

    var score    = (int)type.GetField("Score").GetValue(null);
    var wave     = (int)type.GetField("Wave").GetValue(null);
    var lives    = (int)type.GetField("Lives").GetValue(null);
    var isRunning = (bool)type.GetField("IsRunning").GetValue(null);
    var isPlaying = EditorApplication.isPlaying && isRunning;

    var sb = new System.Text.StringBuilder();
    sb.Append("{\"success\":true,\"score\":");
    sb.Append(score);
    sb.Append(",\"wave\":");
    sb.Append(wave);
    sb.Append(",\"lives\":");
    sb.Append(lives);
    sb.Append(",\"isPlaying\":");
    sb.Append(isPlaying ? "true" : "false");
    sb.Append("}");

    SendJsonRaw(context, sb.ToString());
}
```

КРИТИЧНО:
- Проверять `type == null` перед любым reflection (Pitfall 2: type null вне Play Mode).
- `sb.Append(isPlaying ? "true" : "false")` — без кавычек, JSON boolean (Pitfall 6).
- `(int)` для Score/Wave/Lives — целые числа, не float (Pitfall 6 из RESEARCH.md).
- StringBuilder с ручной сериализацией — JsonUtility не поддерживает анонимные типы (паттерн проекта из STATE.md Phase 02).
- Метод использует уже существующий `SendJsonRaw(context, json)`.
  </action>
  <verify>
    <automated>grep -n "get_game_state\|HandleGetGameState" /Users/selstrom/work/projects/asteroids-specs/Packages/com.shtl.mcp-unity/Editor/McpUnityBridge.cs</automated>
  </verify>
  <acceptance_criteria>
    - McpUnityBridge.cs содержит строку `case "/get_game_state":`
    - McpUnityBridge.cs содержит строку `private static void HandleGetGameState(`
    - McpUnityBridge.cs содержит строку `"SelStrom.Asteroids.RuntimeBridgeProxy, Assembly-CSharp"`
    - McpUnityBridge.cs содержит `type == null` (null-check перед reflection)
    - McpUnityBridge.cs содержит `isPlaying ? "true" : "false"` (корректный JSON boolean)
    - McpUnityBridge.cs содержит `(int)type.GetField("Score").GetValue(null)` (int cast, не float)
    - После компиляции через MCP tool compile — 0 ошибок
  </acceptance_criteria>
  <done>McpUnityBridge.cs обновлён — endpoint /get_game_state работает, вне Play Mode возвращает isPlaying:false без исключений</done>
</task>

</tasks>

<verification>
После завершения всех 4 задач запустить через MCP tool compile:
1. Вызвать MCP tool `compile` — должен вернуть 0 ошибок компиляции
2. Если есть ошибки — исправить по тексту ошибки до перехода к Plan 02

Проверить через grep:
```
grep -rn "RuntimeBridgeProxy" Assets/Scripts/Application/
```
Должен найти файл в: RuntimeBridgeProxy.cs, ApplicationEntry.cs
</verification>

<success_criteria>
- Assets/Scripts/Application/RuntimeBridgeProxy.cs существует с #if UNITY_EDITOR структурой
- Game.cs содержит свойства IsRunning и Wave
- Application.cs содержит делегирующие свойства Score/Wave/Lives/IsRunning
- ApplicationEntry.cs вызывает RuntimeBridgeProxy.UpdateState в Update() и Reset() в OnDestroy()
- McpUnityBridge.cs содержит case "/get_game_state" и HandleGetGameState с reflection
- MCP tool compile возвращает 0 ошибок
</success_criteria>

<output>
После завершения создать .planning/phases/08-mcp-runtime/08-01-SUMMARY.md
</output>
