---
phase: 07-ugs-leaderboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Services/UgsService.cs
  - Assets/Scripts/Configs/GameData.cs
autonomous: true
requirements:
  - LEAD-01
  - LEAD-06

must_haves:
  truths:
    - "UgsService.InitializeAsync() выполняет guest sign-in через UGS Authentication SDK"
    - "Повторный вызов InitializeAsync() не бросает исключение (проверка IsSignedIn)"
    - "GameData.UgsProjectId — поле типа string, видно в Inspector"
    - "UgsService.SubmitScoreAsync(name, score) устанавливает имя игрока и отправляет счёт"
    - "UgsService.GetTopScoresAsync() возвращает LeaderboardScoresPage (до 10 записей)"
    - "UgsService.GetPlayerScoreAsync() возвращает null если игрок не в лидерборде (Pitfall 6)"
  artifacts:
    - path: "Assets/Scripts/Services/UgsService.cs"
      provides: "Plain C# UGS-сервис: InitializeAsync, SubmitScoreAsync, GetTopScoresAsync, GetPlayerScoreAsync"
      exports: ["UgsService"]
    - path: "Assets/Scripts/Configs/GameData.cs"
      provides: "GameData с полем UgsProjectId"
      contains: "string UgsProjectId"
  key_links:
    - from: "UgsService.InitializeAsync"
      to: "AuthenticationService.Instance.SignInAnonymouslyAsync"
      via: "проверка IsSignedIn перед вызовом"
      pattern: "IsSignedIn"
    - from: "UgsService.SubmitScoreAsync"
      to: "LeaderboardsService.Instance.AddPlayerScoreAsync"
      via: "UpdatePlayerNameAsync перед submit"
      pattern: "UpdatePlayerNameAsync"
---

<objective>
Создать UgsService — plain C# класс (паттерн AudioManager) с полным набором UGS-методов, и расширить GameData полем UgsProjectId.

Purpose: Фундамент для всей UGS-интеграции — все вызовы к UGS Authentication и Leaderboards SDK проходят через UgsService. GameData хранит конфигурацию (LEAD-06).
Output: Assets/Scripts/Services/UgsService.cs, обновлённый Assets/Scripts/Configs/GameData.cs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ugs-leaderboards/07-CONTEXT.md
@.planning/phases/07-ugs-leaderboards/07-RESEARCH.md

<interfaces>
<!-- Существующие интерфейсы, на которые опирается этот план -->

Из Assets/Scripts/Configs/GameData.cs:
```csharp
namespace SelStrom.Asteroids.Configs
{
    [CreateAssetMenu(menuName = "Game data")]
    public class GameData : ScriptableObject
    {
        // ...
        [Space]
        public string LeaderboardId = "asteroids_highscores";
        // Добавить рядом: public string UgsProjectId = "";
        [Space]
        public AudioData Audio;
    }
}
```

UGS SDK (уже в manifest.json):
- com.unity.services.authentication@3.6.0
- com.unity.services.leaderboards@2.3.3
- com.unity.services.core@1.16.0

Namespaces:
```csharp
using Unity.Services.Core;
using Unity.Services.Authentication;
using Unity.Services.Leaderboards;
using Unity.Services.Leaderboards.Models;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать UgsService</name>
  <files>Assets/Scripts/Services/UgsService.cs</files>
  <read_first>
    - Assets/Scripts/Configs/GameData.cs (для понимания namespace и паттерна)
    - .planning/phases/07-ugs-leaderboards/07-RESEARCH.md (Architecture Patterns, Pitfalls, Code Examples)
    - .planning/phases/07-ugs-leaderboards/07-CONTEXT.md (D-08, D-09, D-10)
  </read_first>
  <action>
Создать директорию Assets/Scripts/Services/ если не существует. Создать файл Assets/Scripts/Services/UgsService.cs со следующей точной реализацией:

```csharp
using System.Threading.Tasks;
using Unity.Services.Authentication;
using Unity.Services.Core;
using Unity.Services.Leaderboards;
using Unity.Services.Leaderboards.Models;

namespace SelStrom.Asteroids
{
    // UGS-сервис: plain C# класс без MonoBehaviour (паттерн AudioManager, D-08)
    // Все async операции через Task — оборачиваются в Coroutine в ApplicationEntry (D-09)
    public class UgsService
    {
        private readonly string _leaderboardId;

        public UgsService(string leaderboardId)
        {
            _leaderboardId = leaderboardId;
        }

        // LEAD-01: Guest sign-in. SignInAnonymouslyAsync автоматически использует кэшированный токен.
        // Pitfall 3: проверяем IsSignedIn перед вызовом.
        public async Task InitializeAsync()
        {
            await UnityServices.InitializeAsync();
            if (!AuthenticationService.Instance.IsSignedIn)
            {
                await AuthenticationService.Instance.SignInAnonymouslyAsync();
            }
        }

        // LEAD-02: установить имя ПЕРЕД отправкой — оно будет видно в лидерборде (Pitfall 3)
        public async Task SubmitScoreAsync(string playerName, int score)
        {
            await AuthenticationService.Instance.UpdatePlayerNameAsync(playerName);
            await LeaderboardsService.Instance.AddPlayerScoreAsync(_leaderboardId, (double)score);
        }

        // LEAD-03: Top-10 (Limit=10 по умолчанию, Offset=0)
        public async Task<LeaderboardScoresPage> GetTopScoresAsync()
        {
            return await LeaderboardsService.Instance.GetScoresAsync(_leaderboardId);
        }

        // LEAD-04: позиция текущего игрока. Pitfall 6: возвращает null если игрок не подавал счёт.
        public async Task<LeaderboardEntry> GetPlayerScoreAsync()
        {
            try
            {
                return await LeaderboardsService.Instance.GetPlayerScoreAsync(_leaderboardId);
            }
            catch (LeaderboardsException)
            {
                // Игрок не в лидерборде (NotFound) — вернуть null
                return null;
            }
        }
    }
}
```

Создать файл Assets/Scripts/Services.meta (если Unity его не создаст автоматически) — Unity создаёт .meta автоматически при обнаружении новой директории, вручную создавать не нужно.

Комментарии на русском языке. Фигурные скобки везде (K&R стиль).
  </action>
  <verify>
    <automated>grep -l "class UgsService" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Services/UgsService.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/Services/UgsService.cs существует
    - Файл содержит "public class UgsService"
    - Файл содержит "public async Task InitializeAsync()"
    - Файл содержит "public async Task SubmitScoreAsync(string playerName, int score)"
    - Файл содержит "public async Task&lt;LeaderboardScoresPage&gt; GetTopScoresAsync()"
    - Файл содержит "public async Task&lt;LeaderboardEntry&gt; GetPlayerScoreAsync()"
    - Файл содержит "IsSignedIn" (Pitfall 3 — проверка перед SignIn)
    - Файл содержит "UpdatePlayerNameAsync" (Pitfall 3 — имя до submit)
    - Файл содержит "catch (LeaderboardsException)" (Pitfall 6 — null при NotFound)
    - Namespace: "namespace SelStrom.Asteroids"
  </acceptance_criteria>
  <done>UgsService.cs создан, все 4 метода реализованы, Pitfalls 3 и 6 учтены.</done>
</task>

<task type="auto">
  <name>Task 2: Расширить GameData — добавить UgsProjectId</name>
  <files>Assets/Scripts/Configs/GameData.cs</files>
  <read_first>
    - Assets/Scripts/Configs/GameData.cs (ОБЯЗАТЕЛЬНО прочитать перед изменением — это существующий файл)
    - .planning/phases/07-ugs-leaderboards/07-CONTEXT.md (D-07)
    - .planning/phases/07-ugs-leaderboards/07-RESEARCH.md (Pitfall 1 — UgsProjectId только для документации)
  </read_first>
  <action>
Добавить поле `string UgsProjectId` в GameData.cs рядом с существующим `LeaderboardId`:

Найти блок:
```csharp
        [Space]
        public string LeaderboardId = "asteroids_highscores";
```

Заменить на:
```csharp
        [Space]
        public string LeaderboardId = "asteroids_highscores";
        // UGS Project ID (D-07, LEAD-06). Только документационное поле — реальный ID
        // задаётся в Edit → Project Settings → Services (Pitfall 1: SDK читает из Project Settings)
        public string UgsProjectId = "";
```

Больше никаких изменений в файле не делать. Остальные поля остаются нетронутыми.
  </action>
  <verify>
    <automated>grep -c "UgsProjectId" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Configs/GameData.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/Configs/GameData.cs содержит "public string UgsProjectId"
    - Поле находится рядом с "LeaderboardId" (в том же блоке [Space])
    - Существующие поля (LeaderboardId, Audio, AsteroidBig и т.д.) остались нетронутыми
    - Комментарий поясняет что это документационное поле (Pitfall 1)
  </acceptance_criteria>
  <done>GameData.UgsProjectId добавлен, поле видно в Inspector рядом с LeaderboardId. LEAD-06 выполнен.</done>
</task>

</tasks>

<verification>
После выполнения обоих задач:
1. grep -l "class UgsService" Assets/Scripts/Services/UgsService.cs — файл существует
2. grep "UgsProjectId" Assets/Scripts/Configs/GameData.cs — поле добавлено
3. MCP compile через Unity Editor — убедиться что нет ошибок компиляции
</verification>

<success_criteria>
- UgsService.cs компилируется без ошибок
- Все 4 метода присутствуют с правильными сигнатурами
- GameData.UgsProjectId видно в Inspector
- Pitfalls 3 и 6 обработаны в коде
</success_criteria>

<output>
После завершения создать .planning/phases/07-ugs-leaderboards/07-01-SUMMARY.md с результатами.
</output>
