---
phase: 06-audio-visual-polish
plan: "05"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - Assets/Editor/Phase6Setup.cs
  - Assets/Media/configs/AudioData.asset
  - Assets/Scenes/Main.unity
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-05
  - VIS-06
  - VIS-07
  - VIS-08
  - AUD-07

user_setup:
  - service: audio-clips
    why: "AudioClip .wav/.ogg файлы не существуют в репозитории — игра запустится без звука до их добавления"
    dashboard_config:
      - task: "После прохождения плана: назначить AudioClip файлы в Assets/Media/configs/AudioData.asset через Inspector"
        location: "Unity Editor → Project → Assets/Media/configs/AudioData.asset → Inspector"

must_haves:
  truths:
    - "Phase6Setup MenuItem создаёт AudioData.asset, AudioManager GameObject в сцене, VfxBlowPrefab prefab, LeaderboardScreen UI в Canvas"
    - "AudioManager в сцене содержит 9 дочерних AudioSource объектов с назначенными компонентами"
    - "VfxBlowPrefab prefab содержит EffectVisual компонент с ParticleSystem (stopAction=Callback)"
    - "TitleScreen Canvas содержит title text 'ASTEROIDS' и кнопку Leaderboard (disabled)"
    - "Camera.backgroundColor = black (VIS-02)"
    - "ApplicationEntry.cs в сцене имеет назначенные ссылки на AudioManager и LeaderboardView"
  artifacts:
    - path: "Assets/Editor/Phase6Setup.cs"
      provides: "Editor script для автоматической настройки всех Phase 6 Unity assets"
      contains: "SetupAll"
    - path: "Assets/Media/configs/AudioData.asset"
      provides: "ScriptableObject с AudioClip полями (пустыми до назначения пользователем)"
      contains: "AudioData"
    - path: "Assets/Scenes/Main.unity"
      provides: "Обновлённая сцена с AudioManager, VfxBlowPrefab, LeaderboardScreen"
  key_links:
    - from: "Assets/Editor/Phase6Setup.cs"
      to: "Assets/Scenes/Main.unity"
      via: "EditorSceneManager.OpenScene + добавление объектов в сцену"
      pattern: "EditorSceneManager"
    - from: "Assets/Scripts/Application/ApplicationEntry.cs"
      to: "Assets/Scripts/Application/AudioManager.cs"
      via: "SerializedField _audioManager назначен в Inspector через Phase6Setup"
      pattern: "AudioManager"
---

<objective>
Создать Phase6Setup.cs Editor script и запустить его для создания всех Unity assets Phase 6.

Purpose: Все предыдущие планы создали C# код. Этот план материализует assets в Unity: AudioData.asset, AudioManager GameObject в сцене, VfxBlowPrefab prefab, Leaderboard Canvas экран, обновления TitleScreen и Camera. Следует конвенции проекта (Phase3Setup, Phase4Setup, Phase5Setup).
Output: MenuItem "Asteroids/Setup Phase 6 Assets" создаёт и настраивает все assets одним кликом; сцена сохранена.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-audio-visual-polish/06-RESEARCH.md
@.planning/phases/06-audio-visual-polish/06-04-SUMMARY.md

<interfaces>
<!-- Phase5Setup.cs — образец для Phase6Setup (конвенция проекта) -->
<!-- Ключевые паттерны:
  - Resources.FindObjectsOfTypeAll<T>() для поиска неактивных объектов (Pitfall 5)
  - SerializedObject + FindProperty для назначения SerializedField
  - SaveOrReplacePrefab pattern
  - EditorSceneManager.OpenScene для сцены
  - AssetDatabase.LoadAllAssetsAtPath(atlasPath) для поиска спрайтов
-->

<!-- AtlasGuid = "cf467e92e508b5878eb24c5a126421e0" (из Phase5Setup.cs) -->
<!-- Спрайт bullet_particle — используется для ParticleSystem -->

<!-- ApplicationEntry.cs SerializedField поля (из Plan 04) -->
```csharp
[SerializeField] private AudioManager _audioManager;
[SerializeField] private LeaderboardView _leaderboardView;
[SerializeField] private GameObject _leaderboardGo;
```

<!-- TitleScreenView.cs SerializedField поля (из Plan 03) -->
```csharp
[SerializeField] private Button _leaderboardButton;
[SerializeField] private TextMeshProUGUI _titleText;
```

<!-- GameOverView.cs поля (из Phase 4, без изменений) -->
```csharp
[SerializeField] private Button _submitScoreButton;
[SerializeField] private Button _leaderboardButton;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать Phase6Setup.cs Editor script</name>
  <read_first>
    - Assets/Editor/Phase5Setup.cs
    - Assets/Editor/Phase4Setup.cs
    - Assets/Scripts/Application/ApplicationEntry.cs
    - Assets/Scripts/View/TitleScreenView.cs
    - Assets/Scripts/View/LeaderboardView.cs
    - Assets/Scripts/Application/Screens/LeaderboardScreen.cs
    - Assets/Scripts/Application/AudioManager.cs
    - Assets/Scripts/Configs/AudioData.cs
    - Assets/Scripts/Configs/GameData.cs
  </read_first>
  <files>Assets/Editor/Phase6Setup.cs</files>
  <action>
Создать Assets/Editor/Phase6Setup.cs. Структура аналогична Phase5Setup.cs. Реализовать SetupAll() с вызовом:
1. CreateAudioDataAsset()
2. CreateVfxBlowPrefab()
3. SetupAudioManagerInScene()
4. SetupLeaderboardScreen()
5. UpdateTitleScreen()
6. UpdateCamera()
7. UpdateGameData()
8. AssetDatabase.SaveAssets() + AssetDatabase.Refresh()

**CreateAudioDataAsset()**: Создать Assets/Media/configs/AudioData.asset если не существует. Поля AudioClip оставить null — пользователь назначит вручную. `EditorUtility.SetDirty(audioData)`.

**CreateVfxBlowPrefab()**: Создать Assets/Media/prefabs/vfx_blow.prefab:
- GameObject "vfx_blow", layer 0
- Добавить ParticleSystem компонент:
  - `main.loop = false`
  - `main.stopAction = ParticleSystemStopAction.Callback` (критично для OnParticleSystemStopped)
  - `main.startLifetime = 0.5f`
  - `main.startSpeed = 3f`
  - `main.startSize = 0.3f`
  - `main.maxParticles = 20`
  - `emission.SetBurst(0, new ParticleSystem.Burst(0f, 15))` — burst: 15 частиц при старте
  - Назначить спрайт bullet_particle через TextureSheetAnimationModule или Renderer материал
- Добавить EffectVisual компонент
- Назначить ParticleSystem в SerializedField `_particles` через SerializedObject
- Сохранить через SaveOrReplacePrefab

**SetupAudioManagerInScene()**: Открыть сцену Main.unity. Найти ApplicationEntry через Resources.FindObjectsOfTypeAll<ApplicationEntry>(). Создать GameObject "AudioManager" как дочерний к root сцены. Добавить AudioManager компонент. Создать 9 дочерних GameObject для AudioSource (shoot, thrust, explodeShip, explodeAsteroidBig, explodeAsteroidMedium, explodeAsteroidSmall, ufoTone, beatLow, beatHigh). На каждом добавить AudioSource компонент. Для thrust и ufoTone: `audioSource.loop = true`. Назначить все AudioSource в AudioManager через SerializedObject + FindProperty. Назначить AudioData.asset в поле `_data`. Назначить AudioManager ссылку в ApplicationEntry через SerializedObject (`_audioManager`).

**SetupLeaderboardScreen()**: В сцене найти Canvas (или создать дочерний к нему). Создать GameObject "LeaderboardScreen" с RectTransform (fullscreen: anchor min(0,0) max(1,1), offsetMin/Max = 0). Добавить Image компонент (цвет черный, alpha 0.9). Добавить дочерние элементы:
- "title_text": TextMeshProUGUI, "LEADERBOARD", fontSize=64, anchor center-top
- "placeholder_text": TextMeshProUGUI, "— Доступно в Phase 7 —", fontSize=32, anchor center
- "back_button": Button + TextMeshProUGUI "< BACK", anchor bottom-center
Добавить LeaderboardView компонент на root объект. Назначить `_titleText`, `_placeholderText`, `_backButton` через SerializedObject. Скрыть экран по умолчанию: `leaderboardGo.SetActive(false)`. Назначить LeaderboardView и leaderboardGo в ApplicationEntry через SerializedObject.

**UpdateTitleScreen()**: Найти TitleScreenView через Resources.FindObjectsOfTypeAll<TitleScreenView>(). Найти или создать дочерние элементы на TitleScreen GameObject:
- "title_text": TextMeshProUGUI "ASTEROIDS", fontSize=96, anchor center
- "leaderboard_button": Button + TextMeshProUGUI "LEADERBOARD", anchor под Play кнопкой
Назначить `_titleText` и `_leaderboardButton` через SerializedObject.

**UpdateCamera()**: Найти Camera.main. `cam.backgroundColor = Color.black`. `cam.clearFlags = CameraClearFlags.SolidColor`. Пометить как dirty.

**UpdateGameData()**: Загрузить GameData.asset. Назначить AudioData.asset в поле `Audio` через SerializedObject.

Везде использовать Resources.FindObjectsOfTypeAll<T>() с фильтрацией по scene (Pitfall 5 из RESEARCH.md).
Сохранить сцену в конце: EditorSceneManager.MarkSceneDirty + SaveScene.
  </action>
  <verify>
    <automated>grep -n "SetupAll" /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase6Setup.cs && grep -n "MenuItem" /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase6Setup.cs</automated>
  </verify>
  <acceptance_criteria>
    - Файл существует: `ls Assets/Editor/Phase6Setup.cs`
    - `grep -n "MenuItem" Assets/Editor/Phase6Setup.cs` — `[MenuItem("Asteroids/Setup Phase 6 Assets")]`
    - `grep -n "ParticleSystemStopAction.Callback" Assets/Editor/Phase6Setup.cs` — критический параметр присутствует
    - `grep -n "CreateAudioDataAsset" Assets/Editor/Phase6Setup.cs` — метод найден
    - `grep -n "SetupAudioManagerInScene" Assets/Editor/Phase6Setup.cs` — метод найден
    - `grep -n "SetupLeaderboardScreen" Assets/Editor/Phase6Setup.cs` — метод найден
    - `grep -n "FindObjectsOfTypeAll" Assets/Editor/Phase6Setup.cs` — используется (не GameObject.Find)
    - MCP compile: 0 ошибок
  </acceptance_criteria>
  <done>Phase6Setup.cs создан с 7 методами настройки; компилируется; MenuItem "Asteroids/Setup Phase 6 Assets" доступен</done>
</task>

<task type="auto">
  <name>Task 2: Запустить Phase6Setup через MCP</name>
  <read_first>
    - Assets/Editor/Phase6Setup.cs (только что созданный)
  </read_first>
  <files>Assets/Media/configs/AudioData.asset, Assets/Scenes/Main.unity</files>
  <action>
Выполнить через MCP:

1. mcp__mcp-unity__compile — убедиться что 0 ошибок
2. mcp__mcp-unity__execute_menu_item с путём "Asteroids/Setup Phase 6 Assets"
3. mcp__mcp-unity__compile — проверить что после выполнения setup нет ошибок
4. Проверить создание assets:
   - AudioData.asset существует: Assets/Media/configs/AudioData.asset
   - vfx_blow.prefab существует: Assets/Media/prefabs/vfx_blow.prefab
5. Если execute_menu_item недоступен в MCP — использовать mcp__mcp-unity__play + stop как альтернативную проверку что сцена загружается без ошибок

ПРИМЕЧАНИЕ: AudioClip поля в AudioData.asset будут пустыми (null) — это ожидаемое поведение. Звуки появятся когда пользователь назначит .wav/.ogg файлы.
  </action>
  <verify>
    <automated>ls /Users/selstrom/work/projects/asteroids-specs/Assets/Media/configs/AudioData.asset 2>/dev/null && echo "AudioData.asset EXISTS" || echo "AudioData.asset MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - AudioData.asset существует в Assets/Media/configs/
    - vfx_blow.prefab существует в Assets/Media/prefabs/
    - MCP compile после setup: 0 ошибок
    - Play Mode запускается без NullReferenceException в Console (AudioManager null-guard защищает от пустых clips)
  </acceptance_criteria>
  <done>Assets созданы; сцена сохранена; Play Mode работает без crash при пустых AudioClip полях</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 6 полная интеграция:
- EffectVisual с ParticleSystem взрывами (VIS-03, VIS-04)
- AudioManager с 9 AudioSource (AUD-07)
- AudioData.asset (заполнить clips вручную для звука)
- TitleScreen с "ASTEROIDS" заголовком и кнопкой Leaderboard (VIS-06)
- LeaderboardScreen stub экран с кнопкой Back (VIS-08)
- Чёрный фон камеры (VIS-02)
- Audio callbacks подключены к игровой логике
  </what-built>
  <how-to-verify>
1. Unity Editor → "Asteroids/Setup Phase 6 Assets" (если ещё не выполнено)
2. Нажать Play
3. Проверить:
   а. TitleScreen: виден заголовок "ASTEROIDS" и две кнопки (Play, Leaderboard)
   б. Нажать Play → игра начинается, чёрный фон (VIS-02)
   в. HUD виден с счётом и жизнями (VIS-05)
   г. Врезаться в астероид → виден взрыв частиц (VIS-03, VIS-04)
   д. Уничтожить астероид → виден взрыв частиц соответствующего размера (VIS-04)
   е. Умереть 3 раза → Game Over экран с кнопками (VIS-07)
   ж. На TitleScreen нажать Leaderboard → открывается LeaderboardScreen с "LEADERBOARD" и кнопкой Back (VIS-08) — ЕСЛИ кнопка была включена в Phase6Setup
4. Console: нет NullReferenceException
5. Звуки будут тихими пока AudioClip не назначены (ожидаемо)
  </how-to-verify>
  <resume-signal>Написать "approved" если визуальные проверки пройдены, или описать проблемы для gap closure</resume-signal>
</task>

</tasks>

<verification>
mcp__mcp-unity__compile → 0 ошибок.
ls Assets/Media/configs/AudioData.asset → файл существует.
ls Assets/Media/prefabs/vfx_blow.prefab → файл существует.
Play Mode → взрывы видны, TitleScreen с "ASTEROIDS", LeaderboardScreen доступен.
</verification>

<success_criteria>
- Phase6Setup.cs создан и работает без ошибок
- AudioData.asset создан (clips null — ожидаемо)
- vfx_blow.prefab создан с ParticleSystem (stopAction=Callback)
- AudioManager GameObject в сцене с 9 AudioSource
- TitleScreen содержит "ASTEROIDS" и кнопку Leaderboard
- LeaderboardScreen: "LEADERBOARD" title + placeholder + Back кнопка
- Камера: чёрный фон
- ApplicationEntry назначены все ссылки
- Play Mode запускается без crash
</success_criteria>

<output>
После завершения создать `.planning/phases/06-audio-visual-polish/06-05-SUMMARY.md`
</output>
