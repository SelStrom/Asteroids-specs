---
phase: 06-audio-visual-polish
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/View/TitleScreenView.cs
  - Assets/Scripts/View/LeaderboardView.cs
  - Assets/Scripts/Application/Screens/LeaderboardScreen.cs
autonomous: true
requirements:
  - VIS-02
  - VIS-05
  - VIS-06
  - VIS-07
  - VIS-08

must_haves:
  truths:
    - "TitleScreenView содержит поле для title-текста и кнопку Leaderboard (disabled до Phase 7)"
    - "LeaderboardView показывает заголовок, список-заглушку и кнопку Back"
    - "LeaderboardScreen управляет Show/Hide LeaderboardView и подключает OnBackClicked"
  artifacts:
    - path: "Assets/Scripts/View/TitleScreenView.cs"
      provides: "TitleScreen с title text и кнопкой Leaderboard"
      contains: "_leaderboardButton"
    - path: "Assets/Scripts/View/LeaderboardView.cs"
      provides: "Stub экран лидерборда"
      exports: ["LeaderboardView", "LeaderboardViewModel"]
      contains: "AbstractWidgetView"
    - path: "Assets/Scripts/Application/Screens/LeaderboardScreen.cs"
      provides: "Screen-контроллер для лидерборда"
      exports: ["LeaderboardScreen"]
  key_links:
    - from: "Assets/Scripts/Application/Screens/LeaderboardScreen.cs"
      to: "Assets/Scripts/View/LeaderboardView.cs"
      via: "LeaderboardScreen.Connect(view, onBack)"
      pattern: "LeaderboardView"
---

<objective>
Расширить TitleScreenView для VIS-06 и создать LeaderboardView/LeaderboardScreen stub для VIS-08. Также подтвердить VIS-02 (чёрный фон) и VIS-05 (HUD layout — уже реализован).

Purpose: VIS-06 требует Main Menu с названием игры и кнопкой Leaderboard; VIS-08 требует экран лидерборда с Top-10 (stub) и кнопкой Back. VIS-02 и VIS-05 — уже выполнены кодом из Phase 4, нужна документация факта.
Output: Расширенный TitleScreenView, новые LeaderboardView и LeaderboardScreen классы — готовы к подключению в Application (Plan 04) и Setup (Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-audio-visual-polish/06-RESEARCH.md

<interfaces>
<!-- Существующий TitleScreenView.cs -->
```csharp
public class TitleScreenViewModel : AbstractViewModel
{
    public Action OnPlayClicked;
}

public class TitleScreenView : AbstractWidgetView<TitleScreenViewModel>
{
    [SerializeField] private Button _playButton;

    protected override void OnConnected()
    {
        _playButton.onClick.AddListener(() => ViewModel?.OnPlayClicked?.Invoke());
    }

    protected override void OnDisposed()
    {
        _playButton.onClick.RemoveAllListeners();
    }
}
```

<!-- Паттерн AbstractWidgetView из com.shtl.mvvm -->
```csharp
public abstract class AbstractWidgetView<TViewModel> : MonoBehaviour
    where TViewModel : AbstractViewModel
{
    public TViewModel ViewModel { get; private set; }
    protected abstract void OnConnected();
    protected abstract void OnDisposed();
}
```

<!-- Паттерн AbstractScreen (GameOverScreen как пример) -->
```csharp
public class GameOverScreen
{
    private GameOverView _view;
    private GameOverViewModel _viewModel;

    public void Connect(GameOverView view, Action onPlayAgain) { ... }
    public void Show(int score, int highScore) { ... }
    public void Hide() { ... }
}
```

<!-- GameOverView.cs — пример структуры для reference -->
// Assets/Scripts/View/GameOverView.cs — см. контекст
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить TitleScreenView — добавить title text и кнопку Leaderboard</name>
  <read_first>
    - Assets/Scripts/View/TitleScreenView.cs
    - Assets/Scripts/Application/Screens/TitleScreen.cs
  </read_first>
  <files>Assets/Scripts/View/TitleScreenView.cs</files>
  <action>
Расширить существующий TitleScreenView.cs:

1. Добавить в TitleScreenViewModel поле `OnLeaderboardClicked`:
```csharp
public class TitleScreenViewModel : AbstractViewModel
{
    public Action OnPlayClicked;
    public Action OnLeaderboardClicked; // VIS-06: новая кнопка Leaderboard
}
```

2. Добавить в TitleScreenView поля `_titleText` (TMP) и `_leaderboardButton`, расширить OnConnected/OnDisposed:
```csharp
public class TitleScreenView : AbstractWidgetView<TitleScreenViewModel>
{
    [SerializeField] private Button _playButton;
    [SerializeField] private Button _leaderboardButton;    // disabled до Phase 7 (D-11)
    [SerializeField] private TextMeshProUGUI _titleText;   // "ASTEROIDS"

    protected override void OnConnected()
    {
        if (_titleText != null) { _titleText.text = "ASTEROIDS"; }
        if (_playButton != null)
        {
            _playButton.onClick.AddListener(() => ViewModel?.OnPlayClicked?.Invoke());
        }
        if (_leaderboardButton != null)
        {
            _leaderboardButton.interactable = false; // disabled до Phase 7
            _leaderboardButton.onClick.AddListener(() => ViewModel?.OnLeaderboardClicked?.Invoke());
        }
    }

    protected override void OnDisposed()
    {
        if (_playButton != null) { _playButton.onClick.RemoveAllListeners(); }
        if (_leaderboardButton != null) { _leaderboardButton.onClick.RemoveAllListeners(); }
    }
}
```

Добавить using TMPro в начале файла. Сохранить обратную совместимость: _playButton nullable guard.
  </action>
  <verify>
    <automated>grep -n "_leaderboardButton" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/TitleScreenView.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_leaderboardButton" Assets/Scripts/View/TitleScreenView.cs` — найдено
    - `grep -n "_titleText" Assets/Scripts/View/TitleScreenView.cs` — найдено
    - `grep -n "OnLeaderboardClicked" Assets/Scripts/View/TitleScreenView.cs` — найдено
    - `grep -n "interactable = false" Assets/Scripts/View/TitleScreenView.cs` — disabled до Phase 7
    - MCP compile: 0 ошибок
  </acceptance_criteria>
  <done>TitleScreenView содержит _titleText ("ASTEROIDS"), _leaderboardButton (disabled), OnLeaderboardClicked callback</done>
</task>

<task type="auto">
  <name>Task 2: Создать LeaderboardView и LeaderboardScreen (stub)</name>
  <read_first>
    - Assets/Scripts/View/GameOverView.cs
    - Assets/Scripts/Application/Screens/GameOverScreen.cs
    - Assets/Scripts/Application/Screens/AbstractScreen.cs
  </read_first>
  <files>Assets/Scripts/View/LeaderboardView.cs, Assets/Scripts/Application/Screens/LeaderboardScreen.cs</files>
  <action>
**Файл 1: Assets/Scripts/View/LeaderboardView.cs**

```csharp
using System;
using Shtl.Mvvm;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

namespace SelStrom.Asteroids
{
    public class LeaderboardViewModel : AbstractViewModel
    {
        public Action OnBackClicked;
        // Данные Top-10 — заглушки до Phase 7
        public string[] EntryNames = new string[0];
        public int[] EntryScores = new int[0];
    }

    public class LeaderboardView : AbstractWidgetView<LeaderboardViewModel>
    {
        [SerializeField] private TextMeshProUGUI _titleText;      // "LEADERBOARD"
        [SerializeField] private TextMeshProUGUI _placeholderText; // "Загрузка..." stub
        [SerializeField] private Button _backButton;

        protected override void OnConnected()
        {
            if (_titleText != null) { _titleText.text = "LEADERBOARD"; }
            if (_placeholderText != null) { _placeholderText.text = "— Доступно в Phase 7 —"; }
            if (_backButton != null)
            {
                _backButton.onClick.AddListener(() => ViewModel?.OnBackClicked?.Invoke());
            }
        }

        protected override void OnDisposed()
        {
            if (_backButton != null) { _backButton.onClick.RemoveAllListeners(); }
        }
    }
}
```

**Файл 2: Assets/Scripts/Application/Screens/LeaderboardScreen.cs**

```csharp
using System;
using UnityEngine;

namespace SelStrom.Asteroids
{
    public class LeaderboardScreen
    {
        private LeaderboardView _view;
        private LeaderboardViewModel _viewModel;

        public void Connect(LeaderboardView view, Action onBack)
        {
            _view = view;
            _viewModel = new LeaderboardViewModel();
            _viewModel.OnBackClicked = onBack;
        }

        public void Show()
        {
            if (_view == null) { return; }
            _view.gameObject.SetActive(true);
            _view.Connect(_viewModel);
        }

        public void Hide()
        {
            if (_view == null) { return; }
            _view.Disconnect();
            _view.gameObject.SetActive(false);
        }
    }
}
```

Оба файла создаются с нуля. Паттерн идентичен GameOverView/GameOverScreen.
  </action>
  <verify>
    <automated>grep -n "LeaderboardView" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/LeaderboardView.cs && grep -n "LeaderboardScreen" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Screens/LeaderboardScreen.cs</automated>
  </verify>
  <acceptance_criteria>
    - Файл LeaderboardView.cs существует: `ls Assets/Scripts/View/LeaderboardView.cs`
    - Файл LeaderboardScreen.cs существует: `ls Assets/Scripts/Application/Screens/LeaderboardScreen.cs`
    - `grep -n "OnBackClicked" Assets/Scripts/View/LeaderboardView.cs` — найдено
    - `grep -n "public void Show" Assets/Scripts/Application/Screens/LeaderboardScreen.cs` — найдено
    - `grep -n "public void Hide" Assets/Scripts/Application/Screens/LeaderboardScreen.cs` — найдено
    - MCP compile: 0 ошибок
  </acceptance_criteria>
  <done>LeaderboardView (с _backButton, _titleText) и LeaderboardScreen (Show/Hide) созданы по паттерну GameOver экрана</done>
</task>

</tasks>

<verification>
mcp__mcp-unity__compile → 0 ошибок.
grep -n "_leaderboardButton" Assets/Scripts/View/TitleScreenView.cs → найдено.
grep -n "OnBackClicked" Assets/Scripts/View/LeaderboardView.cs → найдено.
</verification>

<success_criteria>
- TitleScreenView: _titleText ("ASTEROIDS"), _leaderboardButton (interactable=false), OnLeaderboardClicked
- LeaderboardView: stub с _backButton, _titleText, _placeholderText
- LeaderboardScreen: Connect(view, onBack), Show(), Hide()
- Нет компиляционных ошибок
- VIS-02 (чёрный фон): реализован Camera.backgroundColor в сцене — будет проверен в Plan 05 Setup
- VIS-05 (HUD layout): реализован в Phase 4, не требует изменений
- VIS-07 (GameOver экран): реализован в Phase 4, не требует изменений
</success_criteria>

<output>
После завершения создать `.planning/phases/06-audio-visual-polish/06-03-SUMMARY.md`
</output>
