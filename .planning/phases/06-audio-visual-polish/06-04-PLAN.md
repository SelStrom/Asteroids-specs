---
phase: 06-audio-visual-polish
plan: "04"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - Assets/Scripts/Application/Game.cs
  - Assets/Scripts/Application/Application.cs
  - Assets/Scripts/Application/ApplicationEntry.cs
  - Assets/Scripts/Configs/GameData.cs
autonomous: true
requirements:
  - VIS-01
  - AUD-01
  - AUD-02
  - AUD-03
  - AUD-04
  - AUD-05
  - AUD-06

must_haves:
  truths:
    - "Game.cs вызывает audio callbacks при выстреле, тяге, взрыве корабля, взрыве астероида, появлении/исчезновении UFO"
    - "Game.cs вызывает catalog.SpawnEffect() при уничтожении корабля и астероидов"
    - "Application.cs останавливает AudioManager при GameOver и Restart"
    - "ApplicationEntry.cs содержит поле AudioManager _audioManager"
    - "GameData.cs содержит поле AudioData Audio"
  artifacts:
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "Audio callback wire-up и SpawnEffect вызовы"
      contains: "_onShoot"
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "AudioManager подключение к Game events"
      contains: "_audioManager"
    - path: "Assets/Scripts/Application/ApplicationEntry.cs"
      provides: "SerializedField AudioManager"
      contains: "AudioManager _audioManager"
    - path: "Assets/Scripts/Configs/GameData.cs"
      provides: "AudioData поле"
      contains: "AudioData Audio"
  key_links:
    - from: "Assets/Scripts/Application/Game.cs"
      to: "Assets/Scripts/Application/EntitiesCatalog.cs"
      via: "_catalog.SpawnEffect(position, scale) при Kill"
      pattern: "SpawnEffect"
    - from: "Assets/Scripts/Application/Application.cs"
      to: "Assets/Scripts/Application/AudioManager.cs"
      via: "_audioManager.StartBeat() / StopAll()"
      pattern: "_audioManager"
---

<objective>
Подключить AudioManager и SpawnEffect к игровой логике — wire-up audio callbacks в Game.cs и Application.cs.

Purpose: Без этого плана все аудио классы из Plan 02 и эффекты из Plan 01 существуют, но не вызываются. Этот план соединяет слои.
Output: Game.cs вызывает audio при игровых событиях; Application.cs управляет AudioManager lifecycle; ApplicationEntry.cs имеет SerializedField для AudioManager.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-audio-visual-polish/06-RESEARCH.md
@.planning/phases/06-audio-visual-polish/06-01-SUMMARY.md
@.planning/phases/06-audio-visual-polish/06-02-SUMMARY.md
@.planning/phases/06-audio-visual-polish/06-03-SUMMARY.md

<interfaces>
<!-- Game.Connect() текущая сигнатура -->
```csharp
public void Connect(GameData configs, EntitiesCatalog catalog, Model model,
                    PlayerInput input, Action onGameOver, Action<int, int> onScoreChanged = null,
                    Action<int> onWaveBannerShow = null, Action onWaveBannerHide = null)
```

<!-- Application.cs текущий OnGameStart() -->
```csharp
private void OnGameStart()
{
    _game.Connect(_configs, _catalog, _model, _input, OnGameOver, OnScoreChanged,
        onWaveBannerShow: wave => _hudVisual?.ShowWaveBanner(wave),
        onWaveBannerHide: () => _hudVisual?.HideWaveBanner());
    _game.Start();
}
```

<!-- ApplicationEntry.cs текущие поля -->
```csharp
[SerializeField] private GameData _configs;
[SerializeField] private HudVisual _hudVisual;
// ... другие SerializedField
```

<!-- GameData.cs текущая структура -->
```csharp
[CreateAssetMenu(menuName = "Game data")]
public class GameData : ScriptableObject
{
    public GameObject VfxBlowPrefab; // существующее поле
    // ...
}
```

<!-- AudioManager public API (из Plan 02) -->
```csharp
public void PlayShoot()
public void PlayThrust(bool isActive)
public void PlayExplodeShip()
public void PlayExplodeAsteroid(int size)
public void SetUfoTone(bool active)
public void StartBeat()
public void StopBeat()
public void SetAsteroidCount(int count)
public void StopAll()
```

<!-- EntitiesCatalog.SpawnEffect (из Plan 01) -->
```csharp
public void SpawnEffect(Vector2 position, float scale = 1f)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить AudioData в GameData и audio callbacks в Game.cs</name>
  <read_first>
    - Assets/Scripts/Configs/GameData.cs
    - Assets/Scripts/Application/Game.cs
  </read_first>
  <files>Assets/Scripts/Configs/GameData.cs, Assets/Scripts/Application/Game.cs</files>
  <action>
**GameData.cs**: Добавить поле AudioData в конец класса (после `LeaderboardId`):
```csharp
[Space]
public AudioData Audio; // Phase 6: ссылка на AudioData ScriptableObject
```
Добавить using: `using SelStrom.Asteroids.Configs;` — уже есть в namespace. AudioData в том же namespace.

**Game.cs** — добавить audio callbacks:

1. Добавить приватные поля для audio callbacks рядом с `_onGameOver`:
```csharp
private Action<bool> _onThrust;        // AUD-02
private Action _onShoot;               // AUD-01
private Action _onExplodeShip;         // AUD-03
private Action<int> _onExplodeAsteroid; // AUD-04 (int = asteroid size)
private Action<bool> _onUfoTone;       // AUD-05
private Action<int> _onAsteroidCount;  // AUD-06 (для пульса)
```

2. Расширить Connect() — добавить опциональные audio параметры в конец сигнатуры:
```csharp
public void Connect(GameData configs, EntitiesCatalog catalog, Model model,
                    PlayerInput input, Action onGameOver, Action<int, int> onScoreChanged = null,
                    Action<int> onWaveBannerShow = null, Action onWaveBannerHide = null,
                    Action<bool> onThrust = null, Action onShoot = null,
                    Action onExplodeShip = null, Action<int> onExplodeAsteroid = null,
                    Action<bool> onUfoTone = null, Action<int> onAsteroidCount = null)
{
    // существующие присваивания...
    _onThrust = onThrust;
    _onShoot = onShoot;
    _onExplodeShip = onExplodeShip;
    _onExplodeAsteroid = onExplodeAsteroid;
    _onUfoTone = onUfoTone;
    _onAsteroidCount = onAsteroidCount;
}
```

3. Добавить вызовы callbacks в существующих методах:

В `OnThrust(bool isActive)`:
```csharp
_onThrust?.Invoke(isActive); // AUD-02
```

В `OnUserGunShooting(GunComponent gun)` — после создания пули:
```csharp
_onShoot?.Invoke(); // AUD-01
```

В `OnEntityDestroyed(IGameEntityModel model)`:
- После `if (model is ShipModel)` — перед `_lives--`:
```csharp
_onExplodeShip?.Invoke(); // AUD-03
_catalog.SpawnEffect(_ship.Move.Position.Value, 1.5f); // VIS-03
```
- После `if (model is AsteroidModel asteroid)` — перед `SpawnFragments`:
```csharp
_onExplodeAsteroid?.Invoke(asteroid.Size); // AUD-04
_catalog.SpawnEffect(asteroid.Move.Position.Value, asteroid.Size * 0.5f); // VIS-04
_onAsteroidCount?.Invoke(_asteroidCount - 1); // AUD-06 (передаём до декремента _asteroidCount--)
```
- После `if (model is UfoBigModel ufoModel)` — перед `_ufoActive = false`:
```csharp
_onUfoTone?.Invoke(false); // AUD-05: UFO уничтожен
```

4. В `SpawnUfoBig()` и `SpawnUfoSmall()` — после `_ufoActive = true`:
```csharp
_onUfoTone?.Invoke(true); // AUD-05: UFO появился
```

5. В `StartWave()` — после `_asteroidCount++` (в цикле) или после цикла:
```csharp
_onAsteroidCount?.Invoke(_asteroidCount); // AUD-06: начальное число
```

6. В `CheckUfoExit()` — после `_activeUfo.Kill()`:
```csharp
_onUfoTone?.Invoke(false); // AUD-05: UFO ушёл за край (Pitfall 6 из RESEARCH)
```

ВАЖНО: SpawnEffect при уничтожении ShipModel — позицию брать до Kill(): `var shipPos = (model as ShipModel) != null && _ship != null ? _ship.Move.Position.Value : Vector2.zero;`
  </action>
  <verify>
    <automated>grep -n "_onShoot" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_onShoot" Assets/Scripts/Application/Game.cs` — поле и вызов найдены
    - `grep -n "_onUfoTone" Assets/Scripts/Application/Game.cs` — найдено
    - `grep -n "SpawnEffect" Assets/Scripts/Application/Game.cs` — найдено (≥2 строки: Ship и Asteroid)
    - `grep -n "AudioData Audio" Assets/Scripts/Configs/GameData.cs` — найдено
    - `grep -n "_onAsteroidCount" Assets/Scripts/Application/Game.cs` — найдено
    - MCP compile: 0 ошибок
  </acceptance_criteria>
  <done>Game.cs содержит 6 audio callback полей; все вызовы на правильных событиях; SpawnEffect для корабля и астероидов; GameData.Audio поле добавлено</done>
</task>

<task type="auto">
  <name>Task 2: Подключить AudioManager в Application.cs и ApplicationEntry.cs</name>
  <read_first>
    - Assets/Scripts/Application/Application.cs
    - Assets/Scripts/Application/ApplicationEntry.cs
    - Assets/Scripts/Application/Screens/LeaderboardScreen.cs
  </read_first>
  <files>Assets/Scripts/Application/Application.cs, Assets/Scripts/Application/ApplicationEntry.cs</files>
  <action>
**Application.cs**:

1. Добавить поле `_audioManager`:
```csharp
private AudioManager _audioManager;
private LeaderboardScreen _leaderboardScreen; // VIS-08: stub экран
private LeaderboardView _leaderboardView;
private GameObject _leaderboardGo;
```

2. Расширить Connect() сигнатуру — добавить параметры в конец:
```csharp
public void Connect(IApplicationComponent entry, GameData configs, HudVisual hudVisual,
    TitleScreenView titleScreenView, GameObject titleScreenGo, GameObject hudGo,
    GameOverView gameOverView, GameObject gameOverGo,
    AudioManager audioManager = null,
    LeaderboardView leaderboardView = null, GameObject leaderboardGo = null)
{
    // существующие присваивания...
    _audioManager = audioManager;
    _leaderboardView = leaderboardView;
    _leaderboardGo = leaderboardGo;
}
```

3. В конце Connect() — инициализация LeaderboardScreen:
```csharp
if (_leaderboardGo != null) { _leaderboardGo.SetActive(false); }
_leaderboardScreen = new LeaderboardScreen();
if (_leaderboardView != null)
{
    _leaderboardScreen.Connect(_leaderboardView, OnLeaderboardBack);
}
```

4. Расширить `OnGameStart()` — добавить после `_game.Start()`:
```csharp
if (_audioManager != null)
{
    _audioManager.StopAll(); // сброс состояния при повторном старте
    _audioManager.SetAsteroidCount(12);
    _audioManager.StartBeat();
}
```

5. Расширить `_game.Connect(...)` вызов — добавить audio callbacks:
```csharp
_game.Connect(_configs, _catalog, _model, _input, OnGameOver, OnScoreChanged,
    onWaveBannerShow: wave => _hudVisual?.ShowWaveBanner(wave),
    onWaveBannerHide: () => _hudVisual?.HideWaveBanner(),
    onThrust: isActive => _audioManager?.PlayThrust(isActive),
    onShoot: () => _audioManager?.PlayShoot(),
    onExplodeShip: () => _audioManager?.PlayExplodeShip(),
    onExplodeAsteroid: size => _audioManager?.PlayExplodeAsteroid(size),
    onUfoTone: active => _audioManager?.SetUfoTone(active),
    onAsteroidCount: count => _audioManager?.SetAsteroidCount(count));
```

6. Расширить `OnGameOver()`:
```csharp
private void OnGameOver()
{
    if (_audioManager != null)
    {
        _audioManager.StopBeat();  // AUD-06: пульс стоп
        _audioManager.StopAll();   // AUD-02/05: тяга/UFO стоп (Pitfall 4)
    }
    // существующий код Game Over...
    if (_gameOverGo != null) { _gameOverGo.SetActive(true); }
    _gameOverScreen.Show(_game.Score, _game.HighScore);
}
```

7. Расширить `OnPlayAgain()` — добавить перед `_game.Restart()`:
```csharp
_audioManager?.StopAll(); // Pitfall 2: thrust loop стоп при Restart
```

8. Добавить OnLeaderboardBack метод:
```csharp
private void OnLeaderboardBack()
{
    _leaderboardScreen?.Hide();
    if (_titleScreenGo != null) { _titleScreenGo.SetActive(true); }
}
```

**ApplicationEntry.cs**:

Добавить новые SerializedField поля:
```csharp
[SerializeField] private AudioManager _audioManager;
[SerializeField] private LeaderboardView _leaderboardView;
[SerializeField] private GameObject _leaderboardGo;
```

Расширить Awake() — обновить вызов Connect():
```csharp
_application.Connect(this, _configs, _hudVisual, _titleScreenView, _titleScreenGo, _hudGo,
    _gameOverView, _gameOverGo,
    _audioManager, _leaderboardView, _leaderboardGo);
```
  </action>
  <verify>
    <automated>grep -n "_audioManager" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Application.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_audioManager" Assets/Scripts/Application/Application.cs` — ≥5 строк (поле + StartBeat + StopAll + коллбэки)
    - `grep -n "StartBeat" Assets/Scripts/Application/Application.cs` — найдено в OnGameStart
    - `grep -n "StopAll" Assets/Scripts/Application/Application.cs` — найдено в OnGameOver и OnPlayAgain (Pitfall 4 и Pitfall 2)
    - `grep -n "AudioManager _audioManager" Assets/Scripts/Application/ApplicationEntry.cs` — найдено
    - `grep -n "LeaderboardView _leaderboardView" Assets/Scripts/Application/ApplicationEntry.cs` — найдено
    - `grep -n "_leaderboardScreen" Assets/Scripts/Application/Application.cs` — найдено
    - MCP compile: 0 ошибок
  </acceptance_criteria>
  <done>Application.cs: AudioManager lifecycle (StartBeat/StopAll/StopBeat); audio callbacks переданы в Game.Connect; LeaderboardScreen подключён; ApplicationEntry.cs имеет SerializedField для AudioManager и Leaderboard</done>
</task>

</tasks>

<verification>
mcp__mcp-unity__compile → 0 ошибок.
grep -n "_onShoot" Assets/Scripts/Application/Game.cs → найдено.
grep -n "StartBeat" Assets/Scripts/Application/Application.cs → найдено в OnGameStart.
grep -n "StopAll" Assets/Scripts/Application/Application.cs → найдено в OnGameOver.
</verification>

<success_criteria>
- Game.cs: 6 audio callback полей, все вызываются на правильных событиях
- SpawnEffect вызывается при уничтожении корабля (scale=1.5) и астероида (scale=size*0.5)
- Application.cs: StartBeat при старте, StopAll при GameOver и PlayAgain
- Все audio callbacks переданы через Game.Connect параметры
- ApplicationEntry.cs: SerializedField AudioManager, LeaderboardView, GameObject leaderboardGo
- Нет компиляционных ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/06-audio-visual-polish/06-04-SUMMARY.md`
</output>
