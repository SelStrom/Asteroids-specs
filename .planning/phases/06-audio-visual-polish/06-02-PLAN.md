---
phase: 06-audio-visual-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Configs/AudioData.cs
  - Assets/Scripts/Application/AudioManager.cs
autonomous: true
requirements:
  - AUD-01
  - AUD-02
  - AUD-03
  - AUD-04
  - AUD-05
  - AUD-06
  - AUD-07

must_haves:
  truths:
    - "AudioData ScriptableObject содержит поля для всех 9 AudioClip (shoot, thrust, explodeShip, explodeAsteroidBig/Medium/Small, ufoTone, beatLow, beatHigh)"
    - "AudioManager.PlayShoot() воспроизводит выстрел через PlayOneShot с null-guard"
    - "AudioManager.PlayThrust(bool) включает/выключает loop-источник тяги"
    - "AudioManager.SetUfoTone(bool) включает/выключает loop-источник UFO"
    - "AudioManager.BeatLoop корутина чередует beatLow/beatHigh с интервалом, обратно пропорциональным числу астероидов"
    - "AudioManager.StopAll() останавливает все AudioSource (для Restart)"
  artifacts:
    - path: "Assets/Scripts/Configs/AudioData.cs"
      provides: "ScriptableObject с AudioClip полями"
      exports: ["AudioData"]
      contains: "CreateAssetMenu"
    - path: "Assets/Scripts/Application/AudioManager.cs"
      provides: "Централизованный аудио менеджер"
      exports: ["AudioManager"]
      contains: "BeatLoop"
  key_links:
    - from: "Assets/Scripts/Application/AudioManager.cs"
      to: "Assets/Scripts/Configs/AudioData.cs"
      via: "[SerializeField] AudioData _data"
      pattern: "AudioData _data"
---

<objective>
Создать AudioData ScriptableObject и AudioManager MonoBehaviour — полная реализация всей аудио логики.

Purpose: AUD-01..07 требуют централизованного аудио менеджера с PlayOneShot для разовых звуков, loop для тяги/UFO, корутину для фонового пульса с нарастающим темпом.
Output: AudioData.cs и AudioManager.cs компилируются; все public методы готовы к wire-up в Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-audio-visual-polish/06-RESEARCH.md

<interfaces>
<!-- Конвенция ScriptableObject в проекте (пример AsteroidData.cs) -->
```csharp
[CreateAssetMenu(menuName = "Asteroid data")]
public class AsteroidData : ScriptableObject { ... }
```

<!-- Паттерн AudioManager из RESEARCH.md — использовать точно этот паттерн -->
<!-- AudioManager принимает SetAsteroidCount(int) для расчёта темпа пульса -->
<!-- BeatLoop: интервал = Lerp(0.25f, 1.0f, asteroidCount/12f) -->
<!-- StopAll() вызывается из Application при Restart и GameOver -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать AudioData ScriptableObject</name>
  <read_first>
    - Assets/Scripts/Configs/GameData.cs
    - Assets/Scripts/Configs/AsteroidData.cs
  </read_first>
  <files>Assets/Scripts/Configs/AudioData.cs</files>
  <action>
Создать новый файл Assets/Scripts/Configs/AudioData.cs:

```csharp
using UnityEngine;

namespace SelStrom.Asteroids.Configs
{
    [CreateAssetMenu(menuName = "Audio data")]
    public class AudioData : ScriptableObject
    {
        public AudioClip Shoot;
        public AudioClip Thrust;
        public AudioClip ExplodeShip;
        public AudioClip ExplodeAsteroidBig;
        public AudioClip ExplodeAsteroidMedium;
        public AudioClip ExplodeAsteroidSmall;
        public AudioClip UfoTone;
        public AudioClip BeatLow;
        public AudioClip BeatHigh;
    }
}
```

Файл создаётся в папке Configs рядом с GameData.cs. Namespace: SelStrom.Asteroids.Configs (конвенция проекта).
  </action>
  <verify>
    <automated>grep -n "CreateAssetMenu" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Configs/AudioData.cs</automated>
  </verify>
  <acceptance_criteria>
    - Файл существует: `ls Assets/Scripts/Configs/AudioData.cs`
    - `grep -n "AudioClip Shoot" Assets/Scripts/Configs/AudioData.cs` — найдено
    - `grep -n "AudioClip BeatHigh" Assets/Scripts/Configs/AudioData.cs` — найдено (все 9 полей)
    - `grep -n "CreateAssetMenu" Assets/Scripts/Configs/AudioData.cs` — найдено
  </acceptance_criteria>
  <done>AudioData.cs создан, содержит 9 AudioClip полей, [CreateAssetMenu] атрибут присутствует</done>
</task>

<task type="auto">
  <name>Task 2: Создать AudioManager MonoBehaviour</name>
  <read_first>
    - Assets/Scripts/Configs/AudioData.cs (только что созданный)
    - Assets/Scripts/Application/Application.cs
  </read_first>
  <files>Assets/Scripts/Application/AudioManager.cs</files>
  <action>
Создать новый файл Assets/Scripts/Application/AudioManager.cs:

```csharp
using SelStrom.Asteroids.Configs;
using UnityEngine;

namespace SelStrom.Asteroids
{
    public class AudioManager : MonoBehaviour
    {
        [SerializeField] private AudioData _data;

        [Header("AudioSources")]
        [SerializeField] private AudioSource _shoot;
        [SerializeField] private AudioSource _thrust;
        [SerializeField] private AudioSource _explodeShip;
        [SerializeField] private AudioSource _explodeAsteroidBig;
        [SerializeField] private AudioSource _explodeAsteroidMedium;
        [SerializeField] private AudioSource _explodeAsteroidSmall;
        [SerializeField] private AudioSource _ufoTone;
        [SerializeField] private AudioSource _beatLow;
        [SerializeField] private AudioSource _beatHigh;

        private int _asteroidCount = 12;
        private bool _beatRunning;

        // AUD-01: Звук выстрела корабля
        public void PlayShoot()
        {
            PlayOneShot(_shoot);
        }

        // AUD-02: Звук тяги (зациклен пока зажата кнопка)
        public void PlayThrust(bool isActive)
        {
            if (_thrust == null) { return; }
            if (isActive && !_thrust.isPlaying)
            {
                _thrust.Play();
            }
            else if (!isActive && _thrust.isPlaying)
            {
                _thrust.Stop();
            }
        }

        // AUD-03: Взрыв корабля
        public void PlayExplodeShip()
        {
            PlayOneShot(_explodeShip);
        }

        // AUD-04: Взрыв астероида по размеру (size: 3=big, 2=medium, 1=small)
        public void PlayExplodeAsteroid(int size)
        {
            var src = size == 3 ? _explodeAsteroidBig
                    : size == 2 ? _explodeAsteroidMedium
                    : _explodeAsteroidSmall;
            PlayOneShot(src);
        }

        // AUD-05: Тон UFO (зациклен пока UFO на экране)
        public void SetUfoTone(bool active)
        {
            if (_ufoTone == null) { return; }
            if (active && !_ufoTone.isPlaying)
            {
                _ufoTone.Play();
            }
            else if (!active)
            {
                _ufoTone.Stop();
            }
        }

        // AUD-06: Запустить фоновый пульс
        public void StartBeat()
        {
            if (_beatRunning) { return; }
            _beatRunning = true;
            StartCoroutine(BeatLoop());
        }

        // AUD-06: Остановить фоновый пульс
        public void StopBeat()
        {
            _beatRunning = false;
            StopAllCoroutines();
        }

        // AUD-06: Обновить число астероидов для расчёта темпа
        public void SetAsteroidCount(int count)
        {
            _asteroidCount = Mathf.Max(count, 0);
        }

        // Остановить все звуки (для Restart и GameOver)
        public void StopAll()
        {
            StopBeat();
            if (_thrust != null && _thrust.isPlaying) { _thrust.Stop(); }
            if (_ufoTone != null && _ufoTone.isPlaying) { _ufoTone.Stop(); }
        }

        // Назначить AudioClip из AudioData (вызывается из Phase6Setup или Awake)
        public void ApplyAudioData()
        {
            if (_data == null) { return; }
            SetClip(_shoot, _data.Shoot);
            SetClipLoop(_thrust, _data.Thrust);
            SetClip(_explodeShip, _data.ExplodeShip);
            SetClip(_explodeAsteroidBig, _data.ExplodeAsteroidBig);
            SetClip(_explodeAsteroidMedium, _data.ExplodeAsteroidMedium);
            SetClip(_explodeAsteroidSmall, _data.ExplodeAsteroidSmall);
            SetClipLoop(_ufoTone, _data.UfoTone);
            SetClip(_beatLow, _data.BeatLow);
            SetClip(_beatHigh, _data.BeatHigh);
        }

        private void Awake()
        {
            ApplyAudioData();
        }

        private System.Collections.IEnumerator BeatLoop()
        {
            var useLow = true;
            while (_beatRunning)
            {
                // Интервал: от 1.0s (12 астероидов) до 0.25s (0 астероидов)
                var t = _asteroidCount / 12f;
                var interval = Mathf.Lerp(0.25f, 1.0f, t);
                var src = useLow ? _beatLow : _beatHigh;
                PlayOneShot(src);
                useLow = !useLow;
                yield return new WaitForSeconds(interval);
            }
        }

        private void PlayOneShot(AudioSource src)
        {
            if (src == null || src.clip == null)
            {
                // null guard — нет crash при отсутствии AudioClip (см. Pitfall 1 в RESEARCH.md)
                return;
            }
            src.PlayOneShot(src.clip);
        }

        private static void SetClip(AudioSource src, AudioClip clip)
        {
            if (src != null) { src.clip = clip; }
        }

        private static void SetClipLoop(AudioSource src, AudioClip clip)
        {
            if (src == null) { return; }
            src.clip = clip;
            src.loop = true;
        }
    }
}
```

Важно: AudioSource поля будут назначены в сцене через Phase6Setup.cs. AudioData.asset — через Inspector после Phase6Setup.
  </action>
  <verify>
    <automated>grep -n "BeatLoop" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/AudioManager.cs</automated>
  </verify>
  <acceptance_criteria>
    - Файл существует: `ls Assets/Scripts/Application/AudioManager.cs`
    - `grep -n "public void PlayShoot" Assets/Scripts/Application/AudioManager.cs` — найдено (AUD-01)
    - `grep -n "PlayThrust" Assets/Scripts/Application/AudioManager.cs` — найдено (AUD-02)
    - `grep -n "SetUfoTone" Assets/Scripts/Application/AudioManager.cs` — найдено (AUD-05)
    - `grep -n "BeatLoop" Assets/Scripts/Application/AudioManager.cs` — найдено (AUD-06)
    - `grep -n "StopAll" Assets/Scripts/Application/AudioManager.cs` — найдено
    - `grep -n "src.clip == null" Assets/Scripts/Application/AudioManager.cs` — null-guard присутствует (Pitfall 1)
    - MCP compile: 0 ошибок
  </acceptance_criteria>
  <done>AudioManager.cs создан со всеми методами AUD-01..07, null-guard на PlayOneShot, StopAll() для Restart</done>
</task>

</tasks>

<verification>
mcp__mcp-unity__compile → 0 ошибок.
grep -rn "AudioManager" Assets/Scripts/Application/AudioManager.cs → класс найден.
grep -n "CreateAssetMenu" Assets/Scripts/Configs/AudioData.cs → атрибут найден.
</verification>

<success_criteria>
- AudioData.cs: 9 AudioClip полей, [CreateAssetMenu(menuName = "Audio data")]
- AudioManager.cs: PlayShoot, PlayThrust, PlayExplodeShip, PlayExplodeAsteroid, SetUfoTone, StartBeat, StopBeat, SetAsteroidCount, StopAll
- BeatLoop корутина с динамическим интервалом Lerp(0.25f, 1.0f, count/12f)
- null-guard в PlayOneShot против крашей при отсутствии AudioClip
- Нет компиляционных ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/06-audio-visual-polish/06-02-SUMMARY.md`
</output>
