---
phase: 06-audio-visual-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/View/EffectVisual.cs
  - Assets/Scripts/Application/EntitiesCatalog.cs
autonomous: true
requirements:
  - VIS-03
  - VIS-04

must_haves:
  truths:
    - "EffectVisual.Play(position, scale) запускает ParticleSystem с переданным масштабом"
    - "EffectVisual возвращается в пул только после завершения ParticleSystem (не мгновенно)"
    - "EntitiesCatalog.SpawnEffect(position, scale) берёт EffectVisual из пула и проигрывает взрыв"
  artifacts:
    - path: "Assets/Scripts/View/EffectVisual.cs"
      provides: "Реализация particle effect взрывов"
      exports: ["EffectVisual"]
      contains: "ParticleSystem"
    - path: "Assets/Scripts/Application/EntitiesCatalog.cs"
      provides: "SpawnEffect метод для создания взрывов из пула"
      contains: "SpawnEffect"
  key_links:
    - from: "Assets/Scripts/Application/EntitiesCatalog.cs"
      to: "Assets/Scripts/View/EffectVisual.cs"
      via: "пул EffectVisual, вызов Play(position, scale)"
      pattern: "EffectVisual.*Play"
---

<objective>
Реализовать particle effect взрывов для корабля и астероидов.

Purpose: VIS-03 требует взрыв корабля (4+ фреймов/частиц), VIS-04 — взрывы астероидов масштабируются по размеру. Единственный доступный визуальный ресурс — спрайт bullet_particle.
Output: EffectVisual MonoBehaviour с ParticleSystem логикой; EntitiesCatalog.SpawnEffect(); компилируется без ошибок.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-visual-polish/06-RESEARCH.md

<interfaces>
<!-- Текущий EffectVisual.cs — заглушка без логики -->
<!-- Assets/Scripts/View/EffectVisual.cs -->
```csharp
public class EffectVisual : MonoBehaviour, IEntityView
{
    private int _prefabInstanceId;
    public int PrefabInstanceId => _prefabInstanceId;
    public void SetPrefabId(int id) { _prefabInstanceId = id; }
}
```

<!-- IEntityView интерфейс (из View/Base/) -->
```csharp
public interface IEntityView
{
    int PrefabInstanceId { get; }
    void SetPrefabId(int id);
}
```

<!-- EntitiesCatalog: существующий метод Release -->
```csharp
// Возвращает view GameObject в пул (pool.Release(view.PrefabInstanceId, go))
public void Release(IGameEntityModel model) { ... }
```

<!-- GameObjectPool — уже есть в проекте -->
```csharp
public class GameObjectPool
{
    public GameObject Get(int prefabId, GameObject prefab) { ... }
    public void Release(int prefabId, GameObject go) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить EffectVisual — ParticleSystem с delayed pool return</name>
  <read_first>
    - Assets/Scripts/View/EffectVisual.cs
    - Assets/Scripts/View/Base/ (все файлы интерфейсов)
    - Assets/Scripts/Application/EntitiesCatalog.cs
  </read_first>
  <files>Assets/Scripts/View/EffectVisual.cs</files>
  <action>
Расширить EffectVisual.cs. Заменить заглушку полной реализацией:

```csharp
using System;
using UnityEngine;

namespace SelStrom.Asteroids
{
    public class EffectVisual : MonoBehaviour, IEntityView
    {
        [SerializeField] private ParticleSystem _particles;

        private int _prefabInstanceId;
        private Action<EffectVisual> _onFinished; // callback для возврата в пул

        public int PrefabInstanceId => _prefabInstanceId;
        public void SetPrefabId(int id) { _prefabInstanceId = id; }

        public void Initialize(Action<EffectVisual> onFinished)
        {
            _onFinished = onFinished;
        }

        public void Play(Vector2 position, float scale = 1f)
        {
            transform.position = new Vector3(position.x, position.y, 0f);
            if (_particles == null)
            {
                Debug.LogWarning("[EffectVisual] ParticleSystem не назначен");
                _onFinished?.Invoke(this);
                return;
            }
            var main = _particles.main;
            main.startSizeMultiplier = scale;
            _particles.Play();
        }

        // Вызывается Unity когда ParticleSystem завершился (StopAction = Callback)
        private void OnParticleSystemStopped()
        {
            _onFinished?.Invoke(this);
        }
    }
}
```

Критично: ParticleSystem.main.stopAction должен быть установлен в `ParticleSystemStopAction.Callback` через Inspector (будет сделано в Phase6Setup). Без этого OnParticleSystemStopped не вызывается.
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids-specs && node /Users/selstrom/.claude/get-shit-done/bin/gsd-tools.cjs mcp-compile 2>/dev/null || echo "compile via MCP"</automated>
  </verify>
  <acceptance_criteria>
    - Файл содержит `private ParticleSystem _particles` (grep-проверка: `grep -n "ParticleSystem _particles" Assets/Scripts/View/EffectVisual.cs`)
    - Метод `Play(Vector2 position, float scale = 1f)` присутствует (`grep -n "public void Play" Assets/Scripts/View/EffectVisual.cs`)
    - Метод `OnParticleSystemStopped` присутствует (`grep -n "OnParticleSystemStopped" Assets/Scripts/View/EffectVisual.cs`)
    - Метод `Initialize(Action<EffectVisual> onFinished)` присутствует
  </acceptance_criteria>
  <done>EffectVisual компилируется, содержит ParticleSystem поле, Play() и OnParticleSystemStopped()</done>
</task>

<task type="auto">
  <name>Task 2: Добавить SpawnEffect в EntitiesCatalog</name>
  <read_first>
    - Assets/Scripts/Application/EntitiesCatalog.cs
    - Assets/Scripts/Configs/GameData.cs
  </read_first>
  <files>Assets/Scripts/Application/EntitiesCatalog.cs</files>
  <action>
Добавить в EntitiesCatalog:

1. Поле для хранения EffectVisual prefab id:
```csharp
private int _effectPrefabId = -1;
```

2. В методе Connect() (или отдельным методом) — инициализация prefab id для VfxBlowPrefab:
```csharp
// В Connect() после существующего кода:
if (_configs.VfxBlowPrefab != null)
{
    _effectPrefabId = _configs.VfxBlowPrefab.GetInstanceID();
}
```

3. Новый публичный метод SpawnEffect:
```csharp
public void SpawnEffect(Vector2 position, float scale = 1f)
{
    if (_configs.VfxBlowPrefab == null)
    {
        Debug.LogWarning("[EntitiesCatalog] VfxBlowPrefab не назначен в GameData");
        return;
    }
    var go = _pool.Get(_effectPrefabId, _configs.VfxBlowPrefab);
    var effect = go.GetComponent<EffectVisual>();
    if (effect == null)
    {
        Debug.LogWarning("[EntitiesCatalog] EffectVisual компонент не найден на VfxBlowPrefab");
        return;
    }
    effect.SetPrefabId(_effectPrefabId);
    effect.Initialize(ReturnEffect);
    effect.Play(position, scale);
    go.SetActive(true);
}

private void ReturnEffect(EffectVisual effect)
{
    effect.gameObject.SetActive(false);
    _pool.Release(effect.PrefabInstanceId, effect.gameObject);
}
```

Не изменять существующие методы Connect(), CreateShip(), Release() и т.д.
  </action>
  <verify>
    <automated>grep -n "SpawnEffect" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/EntitiesCatalog.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SpawnEffect" Assets/Scripts/Application/EntitiesCatalog.cs` — метод найден
    - `grep -n "ReturnEffect" Assets/Scripts/Application/EntitiesCatalog.cs` — callback найден
    - `grep -n "VfxBlowPrefab" Assets/Scripts/Application/EntitiesCatalog.cs` — null-check присутствует
    - MCP compile: 0 ошибок
  </acceptance_criteria>
  <done>EntitiesCatalog.SpawnEffect(position, scale) компилируется; корректно берёт из пула, вызывает Play(), возвращает в пул по OnParticleSystemStopped</done>
</task>

</tasks>

<verification>
mcp__mcp-unity__compile → 0 ошибок компиляции.
grep -n "SpawnEffect" Assets/Scripts/Application/EntitiesCatalog.cs → строка найдена.
grep -n "OnParticleSystemStopped" Assets/Scripts/View/EffectVisual.cs → строка найдена.
</verification>

<success_criteria>
- EffectVisual.cs: Play(), Initialize(), OnParticleSystemStopped() реализованы
- EntitiesCatalog.SpawnEffect() берёт EffectVisual из пула, вызывает Play(position, scale)
- ReturnEffect() возвращает в пул после завершения ParticleSystem
- Нет компиляционных ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/06-audio-visual-polish/06-01-SUMMARY.md`
</output>
