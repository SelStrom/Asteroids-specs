---
phase: 05-ufo-progression-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Editor/Phase5Setup.cs
  - Assets/Media/prefabs/ufo_big.prefab
  - Assets/Media/prefabs/ufo_small.prefab
  - Assets/Media/configs/UfoBigData.asset
  - Assets/Media/configs/UfoSmallData.asset
  - Assets/Media/configs/GameData.asset
  - Assets/Scenes/Main.unity
autonomous: true
requirements: [UFO-01, UFO-02, PROG-06]

must_haves:
  truths:
    - "UFO prefabs (ufo_big, ufo_small) существуют в Assets/Media/prefabs/ с UfoVisual + CircleCollider2D + Rigidbody2D Kinematic"
    - "UfoBigData.asset и UfoSmallData.asset существуют как ScriptableObject UfoData с заполненными Speed, Gun, Prefab"
    - "GameData.asset содержит ссылки на UfoBig и Ufo (UfoData assets)"
    - "HUD в Main.unity содержит wave_banner_text (TextMeshProUGUI, inactive по умолчанию)"
    - "HudVisual.cs содержит поле _waveBannerText и методы ShowWaveBanner/HideWaveBanner"
  artifacts:
    - path: "Assets/Editor/Phase5Setup.cs"
      provides: "Editor скрипт для автоматического создания UFO prefabs, configs и HUD баннера"
    - path: "Assets/Media/prefabs/ufo_big.prefab"
      provides: "Large UFO prefab с UfoVisual"
    - path: "Assets/Media/prefabs/ufo_small.prefab"
      provides: "Small UFO prefab с UfoVisual"
    - path: "Assets/Media/configs/UfoBigData.asset"
      provides: "Large UFO конфиг: Score=200, Speed=8, Gun.MaxShoots=1, Gun.ReloadDurationSec=1.5"
    - path: "Assets/Media/configs/UfoSmallData.asset"
      provides: "Small UFO конфиг: Score=1000, Speed=10, ShootDurationSec=2"
    - path: "Assets/Scripts/View/HudVisual.cs"
      provides: "ShowWaveBanner(int) и HideWaveBanner() методы"
  key_links:
    - from: "Assets/Editor/Phase5Setup.cs"
      to: "Assets/Media/configs/GameData.asset"
      via: "SerializedObject.FindProperty(UfoBig/Ufo).objectReferenceValue = ufoData"
      pattern: "FindProperty.*Ufo"
    - from: "Assets/Scripts/View/HudVisual.cs"
      to: "Assets/Scenes/Main.unity"
      via: "_waveBannerText сериализованное поле назначается Phase5Setup через SerializedObject"
      pattern: "_waveBannerText"
---

<objective>
Создать Editor setup скрипт Phase5Setup.cs который автоматически генерирует UFO prefabs, конфиги и HUD wave banner. Расширить HudVisual методами для баннера волны.

Purpose: Все Unity assets (prefabs, ScriptableObjects, scene elements) должны быть созданы через автоматизированный Editor script — как в Phase4Setup. Ручное создание в Editor запрещено (правило проекта).

Output: Phase5Setup.cs с MenuItem "Asteroids/Setup Phase 5 Assets", UFO prefabs/configs в Assets/Media/, wave banner в HUD сцены.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ufo-progression-polish/05-RESEARCH.md
@.planning/STATE.md

<interfaces>
<!-- Phase4Setup.cs — эталонный паттерн для Editor скриптов -->
Ключевые паттерны из Phase4Setup.cs:
- const string AtlasGuid = "cf467e92e508b5878eb24c5a126421e0" — GUID атласа asteroids.png
- LoadSprite(spriteName) — загружает sub-sprite по имени из GUID
- CreateOrUpdateAsteroidPrefab — создаёт GO, добавляет компоненты, сохраняет через PrefabUtility.SaveAsPrefabAsset
- SerializedObject + FindProperty для назначения ссылок в ScriptableObjects
- Resources.FindObjectsOfTypeAll<T>() для поиска MonoBehaviour в неактивных объектах сцены

<!-- GameData.cs — структура для UFO полей -->
```csharp
public class GameData : ScriptableObject
{
    public UfoData UfoBig;   // Large UFO
    public UfoData Ufo;      // Small UFO
    // ...
}
```

<!-- UfoData.cs — структура конфига -->
```csharp
public class UfoData : BaseGameEntityData
{
    public GameObject Prefab;
    public float Speed;
    public GunData Gun;         // ScriptableObject
    public float ShootDurationSec;
}
```

<!-- GunData.cs — структура оружия -->
```csharp
public class GunData : ScriptableObject
{
    public int MaxShoots;
    public float ReloadDurationSec;
}
```

<!-- BaseGameEntityData.cs — базовый класс с полем Score -->
```csharp
public class BaseGameEntityData : ScriptableObject
{
    public int Score;
}
```

<!-- HudVisual.cs — текущие поля (нужно добавить _waveBannerText) -->
```csharp
public class HudVisual : MonoBehaviour
{
    [SerializeField] private TextMeshProUGUI _scoreText;
    [SerializeField] private TextMeshProUGUI _highScoreText;
    [SerializeField] private Transform _livesContainer;
    [SerializeField] private Sprite _lifeIconSprite;
    // ДОБАВИТЬ: [SerializeField] private TextMeshProUGUI _waveBannerText;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить HudVisual — добавить wave banner поля и методы</name>
  <files>Assets/Scripts/View/HudVisual.cs</files>
  <read_first>
    - Assets/Scripts/View/HudVisual.cs (текущее состояние — ОБЯЗАТЕЛЬНО прочитать перед изменением)
  </read_first>
  <action>
Добавить в HudVisual.cs сериализованное поле _waveBannerText и два публичных метода:

1. Добавить поле (рядом с существующими [SerializeField] полями):
```csharp
[SerializeField] private TextMeshProUGUI _waveBannerText;
```

2. Добавить публичные методы (после UpdateHud или в конце класса, до закрывающей скобки класса):
```csharp
public void ShowWaveBanner(int wave)
{
    if (_waveBannerText == null) { return; }
    _waveBannerText.text = $"WAVE {wave}";
    _waveBannerText.gameObject.SetActive(true);
}

public void HideWaveBanner()
{
    if (_waveBannerText == null) { return; }
    _waveBannerText.gameObject.SetActive(false);
}
```

Правила:
- Не изменять существующий код (только добавить)
- K&R стиль скобок
- Использовать null-guard pattern (if == null { return; }) как в существующих методах
  </action>
  <verify>
    <automated>grep "_waveBannerText" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/HudVisual.cs && grep "ShowWaveBanner" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/HudVisual.cs && grep "HideWaveBanner" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/HudVisual.cs</automated>
  </verify>
  <acceptance_criteria>
    - HudVisual.cs содержит "[SerializeField] private TextMeshProUGUI _waveBannerText"
    - HudVisual.cs содержит "public void ShowWaveBanner(int wave)"
    - HudVisual.cs содержит "public void HideWaveBanner()"
    - HudVisual.cs содержит "$\"WAVE {wave}\""
    - Существующие поля _scoreText, _highScoreText, _livesContainer, _lifeIconSprite сохранены
  </acceptance_criteria>
  <done>HudVisual расширен методами баннера волны. Проект компилируется.</done>
</task>

<task type="auto">
  <name>Task 2: Создать Phase5Setup.cs — UFO prefabs, configs, HUD wave banner</name>
  <files>Assets/Editor/Phase5Setup.cs</files>
  <read_first>
    - Assets/Editor/Phase4Setup.cs (эталонный паттерн — строго следовать структуре)
    - Assets/Scripts/Configs/UfoData.cs (поля конфига)
    - Assets/Scripts/Configs/GameData.cs (поля UfoBig, Ufo)
    - Assets/Scripts/View/HudVisual.cs (после Task 1 — нужен FindProperty("_waveBannerText"))
  </read_first>
  <action>
Создать Assets/Editor/Phase5Setup.cs по точному паттерну Phase4Setup.cs:

```csharp
#if UNITY_EDITOR
using SelStrom.Asteroids;
using SelStrom.Asteroids.Configs;
using TMPro;
using UnityEditor;
using UnityEngine;

namespace SelStrom.Asteroids.Editor
{
    public static class Phase5Setup
    {
        private const string AtlasGuid = "cf467e92e508b5878eb24c5a126421e0";

        [MenuItem("Asteroids/Setup Phase 5 Assets")]
        public static void SetupAll()
        {
            CreateUfoGunConfigs();
            CreateUfoConfigs();
            CreateUfoPrefabs();
            UpdateGameData();
            SetupWaveBanner();
            AssetDatabase.SaveAssets();
            AssetDatabase.Refresh();
            UnityEngine.Debug.Log("[Phase5Setup] Готово. Все Phase 5 assets созданы.");
        }

        private static Sprite LoadSprite(string spriteName)
        {
            var atlasPath = AssetDatabase.GUIDToAssetPath(AtlasGuid);
            var allSprites = AssetDatabase.LoadAllAssetsAtPath(atlasPath);
            foreach (var obj in allSprites)
            {
                if (obj is Sprite sprite && sprite.name == spriteName)
                {
                    return sprite;
                }
            }
            UnityEngine.Debug.LogWarning($"[Phase5Setup] Спрайт не найден: {spriteName}");
            return null;
        }

        private static void CreateUfoGunConfigs()
        {
            // UfoBigGunData.asset — Large UFO пушка: 1 выстрел, перезарядка 2 сек
            var bigGunPath = "Assets/Media/configs/UfoBigGunData.asset";
            var bigGun = AssetDatabase.LoadAssetAtPath<GunData>(bigGunPath);
            if (bigGun == null)
            {
                bigGun = ScriptableObject.CreateInstance<GunData>();
                AssetDatabase.CreateAsset(bigGun, bigGunPath);
            }
            bigGun.MaxShoots = 1;
            bigGun.ReloadDurationSec = 2f;
            EditorUtility.SetDirty(bigGun);

            // UfoSmallGunData.asset — Small UFO пушка: 1 выстрел, перезарядка уже в ShootToSystem
            var smallGunPath = "Assets/Media/configs/UfoSmallGunData.asset";
            var smallGun = AssetDatabase.LoadAssetAtPath<GunData>(smallGunPath);
            if (smallGun == null)
            {
                smallGun = ScriptableObject.CreateInstance<GunData>();
                AssetDatabase.CreateAsset(smallGun, smallGunPath);
            }
            smallGun.MaxShoots = 1;
            smallGun.ReloadDurationSec = 0.1f;
            EditorUtility.SetDirty(smallGun);

            UnityEngine.Debug.Log("[Phase5Setup] UFO GunData конфиги созданы/обновлены.");
        }

        private static void CreateUfoConfigs()
        {
            var bigGun = AssetDatabase.LoadAssetAtPath<GunData>("Assets/Media/configs/UfoBigGunData.asset");
            var smallGun = AssetDatabase.LoadAssetAtPath<GunData>("Assets/Media/configs/UfoSmallGunData.asset");

            // UfoBigData.asset — Large UFO: Score=200, Speed=8
            var bigPath = "Assets/Media/configs/UfoBigData.asset";
            var bigData = AssetDatabase.LoadAssetAtPath<UfoData>(bigPath);
            if (bigData == null)
            {
                bigData = ScriptableObject.CreateInstance<UfoData>();
                AssetDatabase.CreateAsset(bigData, bigPath);
            }
            bigData.Score = 200;
            bigData.Speed = 8f;
            bigData.ShootDurationSec = 1.5f;
            bigData.Gun = bigGun;
            EditorUtility.SetDirty(bigData);

            // UfoSmallData.asset — Small UFO: Score=1000, Speed=10
            var smallPath = "Assets/Media/configs/UfoSmallData.asset";
            var smallData = AssetDatabase.LoadAssetAtPath<UfoData>(smallPath);
            if (smallData == null)
            {
                smallData = ScriptableObject.CreateInstance<UfoData>();
                AssetDatabase.CreateAsset(smallData, smallPath);
            }
            smallData.Score = 1000;
            smallData.Speed = 10f;
            smallData.ShootDurationSec = 2f;
            smallData.Gun = smallGun;
            EditorUtility.SetDirty(smallData);

            UnityEngine.Debug.Log("[Phase5Setup] UfoData конфиги созданы/обновлены.");
        }

        private static void CreateUfoPrefabs()
        {
            // ufo_big.prefab — layer 8 (Enemies), collider radius 0.75
            CreateOrUpdateUfoPrefab("ufo_big", "Assets/Media/prefabs/ufo_big.prefab",
                layer: 8, colliderRadius: 0.75f, spriteName: "ufo_big");

            // ufo_small.prefab — layer 8 (Enemies), collider radius 0.4
            CreateOrUpdateUfoPrefab("ufo_small", "Assets/Media/prefabs/ufo_small.prefab",
                layer: 8, colliderRadius: 0.4f, spriteName: "ufo_small");

            // Назначить prefabs в UfoData configs
            var bigData = AssetDatabase.LoadAssetAtPath<UfoData>("Assets/Media/configs/UfoBigData.asset");
            var smallData = AssetDatabase.LoadAssetAtPath<UfoData>("Assets/Media/configs/UfoSmallData.asset");
            var bigPrefab = AssetDatabase.LoadAssetAtPath<GameObject>("Assets/Media/prefabs/ufo_big.prefab");
            var smallPrefab = AssetDatabase.LoadAssetAtPath<GameObject>("Assets/Media/prefabs/ufo_small.prefab");

            if (bigData != null && bigPrefab != null) { bigData.Prefab = bigPrefab; EditorUtility.SetDirty(bigData); }
            if (smallData != null && smallPrefab != null) { smallData.Prefab = smallPrefab; EditorUtility.SetDirty(smallData); }

            UnityEngine.Debug.Log("[Phase5Setup] UFO prefabs созданы/обновлены.");
        }

        private static void CreateOrUpdateUfoPrefab(string name, string path, int layer, float colliderRadius, string spriteName)
        {
            var go = new GameObject(name);
            go.layer = layer;

            var sr = go.AddComponent<SpriteRenderer>();
            var sprite = LoadSprite(spriteName);
            if (sprite != null) { sr.sprite = sprite; }

            var rb = go.AddComponent<Rigidbody2D>();
            rb.bodyType = RigidbodyType2D.Kinematic;
            rb.gravityScale = 0f;
            rb.useFullKinematicContacts = true;

            var col = go.AddComponent<CircleCollider2D>();
            col.radius = colliderRadius;

            var ufoVisual = go.AddComponent<UfoVisual>();
            // Назначить _spriteRenderer сериализованно
            var so = new SerializedObject(ufoVisual);
            var srProp = so.FindProperty("_spriteRenderer");
            if (srProp != null) { srProp.objectReferenceValue = sr; so.ApplyModifiedProperties(); }

            SaveOrReplacePrefab(go, path);
            Object.DestroyImmediate(go);
        }

        private static void UpdateGameData()
        {
            var gameData = AssetDatabase.LoadAssetAtPath<GameData>("Assets/Media/configs/GameData.asset");
            if (gameData == null)
            {
                UnityEngine.Debug.LogError("[Phase5Setup] GameData.asset не найден!");
                return;
            }

            var bigData = AssetDatabase.LoadAssetAtPath<UfoData>("Assets/Media/configs/UfoBigData.asset");
            var smallData = AssetDatabase.LoadAssetAtPath<UfoData>("Assets/Media/configs/UfoSmallData.asset");

            var serializedGameData = new SerializedObject(gameData);
            if (bigData != null)
            {
                serializedGameData.FindProperty("UfoBig").objectReferenceValue = bigData;
            }
            if (smallData != null)
            {
                serializedGameData.FindProperty("Ufo").objectReferenceValue = smallData;
            }
            serializedGameData.ApplyModifiedProperties();

            UnityEngine.Debug.Log("[Phase5Setup] GameData.asset обновлён с UFO конфигами.");
        }

        private static void SetupWaveBanner()
        {
            var scene = UnityEditor.SceneManagement.EditorSceneManager.OpenScene("Assets/Scenes/Main.unity");

            // Найти HudVisual через Resources (поддерживает неактивные объекты)
            var hudVisuals = Resources.FindObjectsOfTypeAll<HudVisual>();
            var hudVisual = System.Array.Find(hudVisuals, v => v.gameObject.scene == scene);
            if (hudVisual == null)
            {
                UnityEngine.Debug.LogError("[Phase5Setup] HudVisual не найден в сцене!");
                return;
            }

            var hudGo = hudVisual.gameObject;

            // Удалить существующий wave_banner_text если есть
            var existing = hudGo.transform.Find("wave_banner_text");
            if (existing != null) { Object.DestroyImmediate(existing.gameObject); }

            // Wave Banner — по центру экрана, скрыт по умолчанию
            var bannerGo = new GameObject("wave_banner_text");
            bannerGo.transform.SetParent(hudGo.transform, false);
            var bannerRect = bannerGo.AddComponent<RectTransform>();
            bannerRect.pivot = new Vector2(0.5f, 0.5f);
            bannerRect.anchorMin = new Vector2(0.5f, 0.5f);
            bannerRect.anchorMax = new Vector2(0.5f, 0.5f);
            bannerRect.anchoredPosition = new Vector2(0f, 60f);
            bannerRect.sizeDelta = new Vector2(400f, 60f);
            var bannerTmp = bannerGo.AddComponent<TextMeshProUGUI>();
            bannerTmp.text = "WAVE 1";
            bannerTmp.fontSize = 48f;
            bannerTmp.fontStyle = FontStyles.Bold;
            bannerTmp.alignment = TextAlignmentOptions.Center;
            bannerTmp.color = Color.white;
            bannerGo.SetActive(false); // скрыт по умолчанию

            // Назначить _waveBannerText в HudVisual
            var serializedHud = new SerializedObject(hudVisual);
            serializedHud.FindProperty("_waveBannerText").objectReferenceValue = bannerTmp;
            serializedHud.ApplyModifiedProperties();

            // Сохранить сцену
            UnityEditor.SceneManagement.EditorSceneManager.MarkSceneDirty(scene);
            UnityEditor.SceneManagement.EditorSceneManager.SaveScene(scene);

            UnityEngine.Debug.Log("[Phase5Setup] Wave banner добавлен в HUD.");
        }

        private static void SaveOrReplacePrefab(GameObject go, string path)
        {
            var existing = AssetDatabase.LoadAssetAtPath<GameObject>(path);
            if (existing != null) { AssetDatabase.DeleteAsset(path); }
            PrefabUtility.SaveAsPrefabAsset(go, path);
        }
    }
}
#endif
```

Требования к реализации:
- `#if UNITY_EDITOR` обёртка (не попадает в build)
- MenuItem: "Asteroids/Setup Phase 5 Assets"
- AtlasGuid = "cf467e92e508b5878eb24c5a126421e0" (тот же атлас что в Phase4Setup)
- Спрайты UFO искать по именам "ufo_big" и "ufo_small" в атласе
- Если спрайт не найден — LogWarning (не ошибка), prefab создаётся без спрайта
- Коллизионный слой layer=8 (Enemies — тот же что у астероидов)
- UFO prefab НЕ должен иметь RotateSystem компонент (астероиды вращаются, UFO нет)
- После SaveOrReplacePrefab → Object.DestroyImmediate(go) (как в Phase4Setup)
- namespace: SelStrom.Asteroids.Editor
  </action>
  <verify>
    <automated>grep -l "Phase5Setup" /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase5Setup.cs && grep "MenuItem.*Setup Phase 5" /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase5Setup.cs && grep "UfoBigData" /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase5Setup.cs && grep "wave_banner_text" /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase5Setup.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Editor/Phase5Setup.cs существует
    - Содержит MenuItem "Asteroids/Setup Phase 5 Assets"
    - Содержит "#if UNITY_EDITOR" и "#endif"
    - Содержит "UfoBigData" и "UfoSmallData"
    - Содержит "wave_banner_text"
    - Содержит "AtlasGuid = \"cf467e92e508b5878eb24c5a126421e0\""
    - Содержит CreateOrUpdateUfoPrefab метод с UfoVisual компонентом
    - Содержит UpdateGameData с FindProperty("UfoBig") и FindProperty("Ufo")
    - mcp__mcp-unity__compile завершается без ошибок
  </acceptance_criteria>
  <done>Phase5Setup.cs создан и компилируется. Меню "Asteroids/Setup Phase 5 Assets" доступно в Unity Editor.</done>
</task>

</tasks>

<verification>
1. mcp__mcp-unity__compile — проект компилируется без ошибок
2. grep проверки по всем файлам согласно acceptance_criteria
3. После выполнения Setup скрипта (отдельный шаг в Plan 03): Assets/Media/prefabs/ufo_big.prefab и ufo_small.prefab существуют
</verification>

<success_criteria>
- Phase5Setup.cs создан с полной реализацией всех методов
- HudVisual расширен ShowWaveBanner/HideWaveBanner
- Проект компилируется
- Phase5Setup создаёт assets при запуске из Editor меню
</success_criteria>

<output>
После выполнения создать `.planning/phases/05-ufo-progression-polish/05-02-SUMMARY.md`
</output>
