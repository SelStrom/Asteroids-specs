---
phase: 05-ufo-progression-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/View/UfoVisual.cs
  - Assets/Scripts/Model/Components/ShootToComponent.cs
autonomous: true
requirements: [UFO-03, UFO-04]

must_haves:
  truths:
    - "UfoVisual компонент существует и биндит Position через ReactiveValue (аналогично AsteroidVisual)"
    - "ShootToComponent содержит Action<UfoModel> OnShoot для callback стрельбы Small UFO"
    - "Проект компилируется без ошибок после добавления файлов"
  artifacts:
    - path: "Assets/Scripts/View/UfoVisual.cs"
      provides: "UfoViewModel + UfoVisual MonoBehaviour с SpriteRenderer биндингом и OnCollisionEnter2D"
      exports: ["UfoViewModel", "UfoVisual"]
    - path: "Assets/Scripts/Model/Components/ShootToComponent.cs"
      provides: "ShootToComponent с таймером и OnShoot callback"
      contains: "Action<UfoModel> OnShoot"
  key_links:
    - from: "Assets/Scripts/View/UfoVisual.cs"
      to: "Assets/Scripts/View/AsteroidVisual.cs"
      via: "одинаковый паттерн AbstractWidgetView + IEntityView"
      pattern: "AbstractWidgetView<UfoViewModel>"
    - from: "Assets/Scripts/Model/Components/ShootToComponent.cs"
      to: "Assets/Scripts/Model/Systems/ShootToSystem.cs"
      via: "OnShoot callback вызывается из ShootToSystem.UpdateNode"
      pattern: "OnShoot\\.Invoke"
---

<objective>
Создать View-слой для UFO (UfoViewModel + UfoVisual) и расширить ShootToComponent для поддержки callback стрельбы.

Purpose: Без UfoVisual невозможно создавать UFO через EntitiesCatalog (нет MonoBehaviour для пула). Без OnShoot в ShootToComponent нет способа нотифицировать Game.cs о выстреле Small UFO.

Output: UfoVisual.cs (новый файл), обновлённый ShootToComponent.cs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ufo-progression-polish/05-RESEARCH.md

<interfaces>
<!-- Паттерн из AsteroidVisual.cs — строго следовать -->
```csharp
// AsteroidViewModel : AbstractViewModel
public class AsteroidViewModel : AbstractViewModel
{
    public ReactiveValue<Vector2> Position = new();
    public ReactiveValue<Sprite> Sprite = new();
    public Action<Collision2D> OnCollision;
}

// AsteroidVisual : AbstractWidgetView<AsteroidViewModel>, IEntityView
public class AsteroidVisual : AbstractWidgetView<AsteroidViewModel>, IEntityView
{
    [SerializeField] private SpriteRenderer _spriteRenderer;
    private int _prefabInstanceId;

    public int PrefabInstanceId => _prefabInstanceId;
    public void SetPrefabId(int id) { _prefabInstanceId = id; }

    protected override void OnConnected()
    {
        if (_spriteRenderer == null) { _spriteRenderer = GetComponent<SpriteRenderer>(); }
        ViewModel.Position.Connect(val =>
            transform.position = new Vector3(val.x, val.y, transform.position.z));
        ViewModel.Sprite.Connect(val => {
            if (_spriteRenderer != null && val != null) { _spriteRenderer.sprite = val; }
        });
    }

    private void OnCollisionEnter2D(Collision2D col) { ViewModel?.OnCollision?.Invoke(col); }
    public new void Dispose() { base.Dispose(); }
}

// IEntityView
public interface IEntityView { int PrefabInstanceId { get; }; void SetPrefabId(int id); }
```

<!-- ShootToComponent текущее состояние -->
```csharp
// Assets/Scripts/Model/Components/ShootToComponent.cs
namespace Model.Components
{
    public class ShootToComponent : IModelComponent
    {
        public float ShootInterval;
        public float Timer;
    }
}
```

<!-- UfoBigModel/UfoModel — используются в OnShoot callback -->
```csharp
// Assets/Scripts/Model/Entities/UfoBigModel.cs
public class UfoBigModel : IGameEntityModel { ... }
public class UfoModel : UfoBigModel
{
    public MoveToComponent MoveTo { get; } = new();
    public ShootToComponent ShootTo { get; } = new();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать UfoVisual.cs (UfoViewModel + UfoVisual)</name>
  <files>Assets/Scripts/View/UfoVisual.cs</files>
  <read_first>
    - Assets/Scripts/View/AsteroidVisual.cs (эталонный паттерн — строго следовать структуре)
    - Assets/Scripts/View/BulletVisual.cs (дополнительный пример AbstractWidgetView)
    - Assets/Scripts/View/Base/ (AbstractWidgetView базовый класс, если есть)
  </read_first>
  <action>
Создать файл Assets/Scripts/View/UfoVisual.cs по точному паттерну AsteroidVisual.cs:

```csharp
using System;
using Shtl.Mvvm;
using UnityEngine;

namespace SelStrom.Asteroids
{
    public class UfoViewModel : AbstractViewModel
    {
        public ReactiveValue<Vector2> Position = new();
        public ReactiveValue<Sprite> Sprite = new();
        public Action<Collision2D> OnCollision;
    }

    public class UfoVisual : AbstractWidgetView<UfoViewModel>, IEntityView
    {
        [SerializeField] private SpriteRenderer _spriteRenderer;
        private int _prefabInstanceId;

        private void Awake()
        {
            if (_spriteRenderer == null) { _spriteRenderer = GetComponent<SpriteRenderer>(); }
        }

        public int PrefabInstanceId => _prefabInstanceId;

        public void SetPrefabId(int id)
        {
            _prefabInstanceId = id;
        }

        protected override void OnConnected()
        {
            if (_spriteRenderer == null) { _spriteRenderer = GetComponent<SpriteRenderer>(); }
            ViewModel.Position.Connect(val =>
                transform.position = new Vector3(val.x, val.y, transform.position.z));
            ViewModel.Sprite.Connect(val => {
                if (_spriteRenderer != null && val != null) { _spriteRenderer.sprite = val; }
            });
        }

        private void OnCollisionEnter2D(Collision2D col)
        {
            ViewModel?.OnCollision?.Invoke(col);
        }

        public new void Dispose()
        {
            base.Dispose();
        }
    }
}
```

Требования:
- namespace: SelStrom.Asteroids
- using: System, Shtl.Mvvm, UnityEngine
- UfoViewModel наследует AbstractViewModel
- UfoVisual наследует AbstractWidgetView<UfoViewModel> И реализует IEntityView
- SpriteRenderer биндинг через ViewModel.Sprite.Connect (аналогично AsteroidVisual)
- Position биндинг через ViewModel.Position.Connect
- OnCollisionEnter2D пробрасывает в ViewModel.OnCollision
- Awake защита: если _spriteRenderer == null → GetComponent
- K&R стиль скобок (открывающая на той же строке)
  </action>
  <verify>
    <automated>grep -l "class UfoVisual" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/UfoVisual.cs && grep "class UfoViewModel" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/UfoVisual.cs && grep "IEntityView" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/UfoVisual.cs && grep "Action<Collision2D> OnCollision" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/View/UfoVisual.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/View/UfoVisual.cs существует
    - Файл содержит "class UfoViewModel : AbstractViewModel"
    - Файл содержит "class UfoVisual : AbstractWidgetView<UfoViewModel>, IEntityView"
    - Файл содержит "Action<Collision2D> OnCollision"
    - Файл содержит "ReactiveValue<Vector2> Position"
    - Файл содержит "ReactiveValue<Sprite> Sprite"
    - Файл содержит "OnCollisionEnter2D"
    - Файл содержит "public int PrefabInstanceId"
  </acceptance_criteria>
  <done>UfoVisual.cs создан, компилируется вместе с проектом без ошибок.</done>
</task>

<task type="auto">
  <name>Task 2: Расширить ShootToComponent — добавить Action&lt;UfoModel&gt; OnShoot</name>
  <files>Assets/Scripts/Model/Components/ShootToComponent.cs</files>
  <read_first>
    - Assets/Scripts/Model/Components/ShootToComponent.cs (текущее состояние)
    - Assets/Scripts/Model/Components/GunComponent.cs (паттерн Action callback в компоненте)
    - Assets/Scripts/Model/Entities/UfoBigModel.cs (чтобы знать тип UfoModel для Action)
  </read_first>
  <action>
Обновить ShootToComponent.cs — добавить `Action<UfoModel> OnShoot` и using System:

```csharp
using System;

namespace Model.Components
{
    public class ShootToComponent : IModelComponent
    {
        public float ShootInterval;
        public float Timer;
        public Action<UfoModel> OnShoot;
    }
}
```

Важно:
- namespace остаётся Model.Components (не менять)
- Добавить `using System;` для Action<T>
- Тип параметра: `UfoModel` (подкласс UfoBigModel, используется для Small UFO)
- Не добавлять using SelStrom.Asteroids — UfoModel находится в том же проекте и доступен через глобальное пространство имён Unity Assembly. Если возникнет конфликт — добавить `using SelStrom.Asteroids;`
  </action>
  <verify>
    <automated>grep "Action<UfoModel> OnShoot" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Components/ShootToComponent.cs && grep "using System" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Components/ShootToComponent.cs</automated>
  </verify>
  <acceptance_criteria>
    - ShootToComponent.cs содержит "Action<UfoModel> OnShoot"
    - ShootToComponent.cs содержит "using System"
    - ShootToComponent.cs сохраняет поля "public float ShootInterval" и "public float Timer"
    - namespace остаётся "Model.Components"
  </acceptance_criteria>
  <done>ShootToComponent расширен, компилируется. GunComponent.OnShooting остаётся нетронутым.</done>
</task>

</tasks>

<verification>
После выполнения Tasks 1 и 2:
- `mcp__mcp-unity__compile` должен завершиться без ошибок компиляции
- grep показывает UfoVisual, UfoViewModel и Action<UfoModel> OnShoot в соответствующих файлах
</verification>

<success_criteria>
- UfoVisual.cs существует с правильной структурой (AbstractWidgetView + IEntityView + коллизии)
- ShootToComponent.cs содержит Action<UfoModel> OnShoot
- Проект компилируется без ошибок
</success_criteria>

<output>
После выполнения создать `.planning/phases/05-ufo-progression-polish/05-01-SUMMARY.md`
</output>
