---
phase: 05-ufo-progression-polish
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - Assets/Scripts/Model/Systems/MoveToSystem.cs
  - Assets/Scripts/Model/Systems/ShootToSystem.cs
  - Assets/Scripts/Model/Model.cs
  - Assets/Scripts/Application/EntitiesCatalog.cs
autonomous: true
requirements: [UFO-01, UFO-02, UFO-03, UFO-04, UFO-06]

must_haves:
  truths:
    - "GroupCreator.Visit(UfoBigModel) регистрирует сущность в MoveSystem и GunSystem"
    - "GroupCreator.Visit(UfoBigModel) для UfoModel дополнительно регистрирует в ShootToSystem"
    - "MoveToSystem.UpdateNode обновляет позицию Small UFO в направлении цели (корабля)"
    - "ShootToSystem.UpdateNode тикает таймер и вызывает OnShoot когда Timer <= 0"
    - "EntitiesCatalog.CreateUfoBig() и CreateUfoSmall() создают модели по паттерну CreateAsteroid"
    - "UFO prefabs зарегистрированы в пуле через EntitiesCatalog.Connect()"
  artifacts:
    - path: "Assets/Scripts/Model/Systems/MoveToSystem.cs"
      provides: "Обновление позиции UfoModel к Target (Vector2.MoveTowards)"
    - path: "Assets/Scripts/Model/Systems/ShootToSystem.cs"
      provides: "Таймер стрельбы + OnShoot callback для Small UFO"
    - path: "Assets/Scripts/Model/Model.cs"
      provides: "GroupCreator.Visit(UfoBigModel) с регистрацией в MoveSystem+GunSystem+ShootToSystem"
    - path: "Assets/Scripts/Application/EntitiesCatalog.cs"
      provides: "CreateUfoBig(pos, velocity), CreateUfoSmall(pos, target), Release для UfoVisual"
  key_links:
    - from: "Assets/Scripts/Model/Systems/ShootToSystem.cs"
      to: "Assets/Scripts/Model/Components/ShootToComponent.cs"
      via: "shootTo.OnShoot?.Invoke(ufo) — callback к Game.cs"
      pattern: "OnShoot\\.Invoke"
    - from: "Assets/Scripts/Application/EntitiesCatalog.cs"
      to: "Assets/Scripts/View/UfoVisual.cs"
      via: "_pool.Get<UfoVisual>(prefab) — создание view из пула"
      pattern: "Get<UfoVisual>"
    - from: "Assets/Scripts/Application/EntitiesCatalog.cs"
      to: "Assets/Scripts/Model/Model.cs"
      via: "_modelFactory.Create<UfoBigModel>() → Model.AddEntity → GroupCreator.Visit"
      pattern: "Create<UfoBigModel>"
---

<objective>
Реализовать Model-слой UFO: активировать системы (MoveToSystem, ShootToSystem), заполнить GroupCreator.Visit(UfoBigModel) и добавить CreateUfoBig/CreateUfoSmall в EntitiesCatalog.

Purpose: Plan 01 создал View-слой, Plan 02 — Unity assets. Этот план подключает UFO к ECS-системам и EntitiesCatalog, завершая инфраструктуру. После этого Game.cs (Plan 04) может создавать и управлять UFO.

Output: Полностью функциональная модельная инфраструктура UFO (без Game-логики спауна).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ufo-progression-polish/05-RESEARCH.md
@.planning/phases/05-ufo-progression-polish/05-01-SUMMARY.md

<interfaces>
<!-- Паттерн BaseModelSystem — как реализовать UpdateNode -->
```csharp
// Assets/Scripts/Model/Systems/BaseModelSystem.cs
public abstract class BaseModelSystem<TComponent> : IModelSystem
{
    // Add(entity, component), Remove(entity), CleanUp()
    protected abstract void UpdateNode(IGameEntityModel entity, TComponent component, float deltaTime);
}
```

<!-- GunSystem.cs — паттерн UpdateNode с таймером -->
```csharp
protected override void UpdateNode(IGameEntityModel entity, GunComponent gun, float deltaTime)
{
    if (gun.Config == null) { return; }
    if (gun.IsReloading) { gun.ReloadTimer -= deltaTime; ... return; }
    if (gun.Shooting && gun.CurrentShoots < gun.Config.MaxShoots)
    {
        gun.CurrentShoots++;
        gun.OnShooting?.Invoke(gun);
        ...
    }
    gun.Shooting = false;
}
```

<!-- MoveToComponent.cs -->
```csharp
public class MoveToComponent : IModelComponent
{
    public Vector2 Target;   // позиция цели (корабль)
    public float Speed;      // скорость движения к цели
}
```

<!-- ShootToComponent.cs после Plan 01 -->
```csharp
public class ShootToComponent : IModelComponent
{
    public float ShootInterval;  // секунды между выстрелами
    public float Timer;
    public Action<UfoModel> OnShoot;
}
```

<!-- UfoBigModel / UfoModel -->
```csharp
public class UfoBigModel : IGameEntityModel
{
    public MoveComponent Move { get; } = new();
    public GunComponent Gun { get; private set; }
    public bool IsDead() => _killed;
    public void Kill() { _killed = true; }
    public virtual void AcceptWith(IGroupVisitor visitor) { visitor.Visit(this); }
}

public class UfoModel : UfoBigModel
{
    public MoveToComponent MoveTo { get; } = new();
    public ShootToComponent ShootTo { get; } = new();
}
```

<!-- GroupCreator текущее состояние в Model.cs -->
```csharp
public void Visit(UfoBigModel model) { /* Phase 4-5 */ }
```

<!-- EntitiesCatalog.CreateAsteroid — эталонный паттерн -->
```csharp
public AsteroidModel CreateAsteroid(AsteroidData data, Vector2 position, Vector2 velocity)
{
    // 1. Model
    var model = _modelFactory.Create<AsteroidModel>();
    // 2. View — _pool.Get<AsteroidVisual>(data.Prefab)
    // 3. ViewModel + Bindings
    var vm = new AsteroidViewModel();
    var bind = new EventBindingContext();
    bind.From(model.Move.Position).To(vm.Position);
    vm.Sprite.Value = sprite;
    view.Connect(vm);
    bind.InvokeAll();
    // 4. Register все 4 словаря
    _modelToView[model] = view;
    _modelToBind[model] = bind;
    _modelToGo[model] = view.gameObject;
    _goToModel[view.gameObject] = model;
    return model;
}
```

<!-- EntitiesCatalog.Release — нужно добавить UfoVisual в ветку Dispose -->
```csharp
if (view is ShipVisual sv) { sv.Dispose(); }
else if (view is BulletVisual bv) { bv.Dispose(); }
else if (view is AsteroidVisual av) { av.Dispose(); }
// ДОБАВИТЬ: else if (view is UfoVisual uv) { uv.Dispose(); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Реализовать MoveToSystem и ShootToSystem</name>
  <files>
    Assets/Scripts/Model/Systems/MoveToSystem.cs
    Assets/Scripts/Model/Systems/ShootToSystem.cs
  </files>
  <read_first>
    - Assets/Scripts/Model/Systems/MoveToSystem.cs (текущая заглушка)
    - Assets/Scripts/Model/Systems/ShootToSystem.cs (текущая заглушка)
    - Assets/Scripts/Model/Systems/GunSystem.cs (паттерн UpdateNode с таймером)
    - Assets/Scripts/Model/Components/MoveToComponent.cs (поля Target, Speed)
    - Assets/Scripts/Model/Components/ShootToComponent.cs (после Plan 01: ShootInterval, Timer, OnShoot)
    - Assets/Scripts/Model/Entities/UfoBigModel.cs (UfoModel тип для OnShoot callback)
  </read_first>
  <action>
**MoveToSystem.cs** — реализовать движение Small UFO к цели (кораблю):

```csharp
using Model.Components;
using UnityEngine;

namespace SelStrom.Asteroids
{
    public class MoveToSystem : BaseModelSystem<MoveToComponent>
    {
        protected override void UpdateNode(IGameEntityModel entity, MoveToComponent moveTo, float deltaTime)
        {
            if (!(entity is UfoModel ufo)) { return; }
            // Двигаться от текущей позиции к Target
            var currentPos = ufo.Move.Position.Value;
            var direction = (moveTo.Target - currentPos).normalized;
            var distance = moveTo.Speed * deltaTime;
            ufo.Move.Position.Value = currentPos + direction * distance;
        }
    }
}
```

**ShootToSystem.cs** — реализовать таймер стрельбы Small UFO:

```csharp
using Model.Components;

namespace SelStrom.Asteroids
{
    public class ShootToSystem : BaseModelSystem<ShootToComponent>
    {
        protected override void UpdateNode(IGameEntityModel entity, ShootToComponent shootTo, float deltaTime)
        {
            shootTo.Timer -= deltaTime;
            if (shootTo.Timer > 0f) { return; }
            shootTo.Timer = shootTo.ShootInterval;

            if (!(entity is UfoModel ufo)) { return; }
            shootTo.OnShoot?.Invoke(ufo);
        }
    }
}
```

Требования:
- MoveToSystem использует `ufo.Move.Position.Value` для чтения и записи позиции (не MoveComponent)
- ShootToSystem сбрасывает Timer в ShootInterval ПЕРЕД проверкой — избегает пропуска первого выстрела
- OnShoot?.Invoke — null-safe (OnShoot может быть не назначен до регистрации callback)
- K&R скобки, namespace SelStrom.Asteroids
  </action>
  <verify>
    <automated>grep "Vector2.MoveTowards\|direction \* distance\|MoveTowards" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Systems/MoveToSystem.cs; grep "OnShoot" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Systems/ShootToSystem.cs && grep "ShootInterval" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Systems/ShootToSystem.cs</automated>
  </verify>
  <acceptance_criteria>
    - MoveToSystem.cs НЕ содержит "empty" комментарий
    - MoveToSystem.cs содержит "ufo.Move.Position.Value"
    - MoveToSystem.cs содержит "moveTo.Speed"
    - ShootToSystem.cs НЕ содержит "empty" комментарий
    - ShootToSystem.cs содержит "shootTo.Timer -= deltaTime"
    - ShootToSystem.cs содержит "shootTo.OnShoot?.Invoke(ufo)"
    - ShootToSystem.cs содержит "shootTo.ShootInterval"
  </acceptance_criteria>
  <done>Обе системы реализованы. GunSystem для Large UFO остаётся нетронутым.</done>
</task>

<task type="auto">
  <name>Task 2: GroupCreator.Visit(UfoBigModel) + EntitiesCatalog.CreateUfoBig/CreateUfoSmall</name>
  <files>
    Assets/Scripts/Model/Model.cs
    Assets/Scripts/Application/EntitiesCatalog.cs
  </files>
  <read_first>
    - Assets/Scripts/Model/Model.cs (GroupCreator.Visit(UfoBigModel) — заглушка)
    - Assets/Scripts/Application/EntitiesCatalog.cs (CreateAsteroid паттерн, Connect метод, Release метод)
    - Assets/Scripts/View/UfoVisual.cs (после Plan 01: UfoViewModel, UfoVisual)
    - Assets/Scripts/Configs/UfoData.cs (Speed, Gun, Prefab поля)
    - Assets/Scripts/Configs/GameData.cs (UfoBig, Ufo поля)
  </read_first>
  <action>
**Model.cs** — заменить заглушку Visit(UfoBigModel):

Найти в GroupCreator:
```csharp
public void Visit(UfoBigModel model) { /* Phase 4-5 */ }
```

Заменить на:
```csharp
public void Visit(UfoBigModel model)
{
    _owner.GetSystem<MoveSystem>().Add(model, model.Move);
    _owner.GetSystem<GunSystem>().Add(model, model.Gun);
    // UfoModel (Small) дополнительно подключает ShootToSystem и MoveToSystem
    if (model is UfoModel ufo)
    {
        _owner.GetSystem<ShootToSystem>().Add(ufo, ufo.ShootTo);
        _owner.GetSystem<MoveToSystem>().Add(ufo, ufo.MoveTo);
    }
}
```

**EntitiesCatalog.cs** — три изменения:

1. В методе Connect() добавить регистрацию UFO prefabs (ПОСЛЕ регистрации asteroid prefabs):
```csharp
RegisterPrefab(_configs.UfoBig.Prefab);
RegisterPrefab(_configs.Ufo.Prefab);
```

2. Добавить метод CreateUfoBig() после CreateAsteroid():
```csharp
public UfoBigModel CreateUfoBig(Vector2 position, Vector2 direction, float speed)
{
    if (_configs.UfoBig == null || _configs.UfoBig.Prefab == null)
    {
        UnityEngine.Debug.LogError("[EntitiesCatalog] UfoBig config или prefab не назначен!");
        return null;
    }

    // 1. Model
    var model = _modelFactory.Create<UfoBigModel>();
    model.SetGun(new GunComponent(_configs.UfoBig.Gun));
    model.Move.Position.Value = position;
    model.Move.Direction.Value = direction;
    model.Move.Speed.Value = speed;

    // 2. View
    var view = _pool.Get<UfoVisual>(_configs.UfoBig.Prefab);
    view.SetPrefabId(_configs.UfoBig.Prefab.GetInstanceID());

    // 3. ViewModel + Bindings
    var vm = new UfoViewModel();
    var bind = new EventBindingContext();
    bind.From(model.Move.Position).To(vm.Position);
    if (_configs.UfoBig.Prefab.GetComponent<SpriteRenderer>()?.sprite != null)
    {
        vm.Sprite.Value = _configs.UfoBig.Prefab.GetComponent<SpriteRenderer>().sprite;
    }
    view.Connect(vm);
    bind.InvokeAll();

    // 4. Register
    _modelToView[model] = view;
    _modelToBind[model] = bind;
    _modelToGo[model] = view.gameObject;
    _goToModel[view.gameObject] = model;

    return model;
}
```

3. Добавить метод CreateUfoSmall() после CreateUfoBig():
```csharp
public UfoModel CreateUfoSmall(Vector2 position, Vector2 direction, float speed)
{
    if (_configs.Ufo == null || _configs.Ufo.Prefab == null)
    {
        UnityEngine.Debug.LogError("[EntitiesCatalog] Ufo (Small) config или prefab не назначен!");
        return null;
    }

    // 1. Model
    var model = _modelFactory.Create<UfoModel>();
    model.SetGun(new GunComponent(_configs.Ufo.Gun));
    model.Move.Position.Value = position;
    model.Move.Direction.Value = direction;
    model.Move.Speed.Value = speed;
    model.ShootTo.ShootInterval = _configs.Ufo.ShootDurationSec;
    model.ShootTo.Timer = _configs.Ufo.ShootDurationSec;

    // 2. View
    var view = _pool.Get<UfoVisual>(_configs.Ufo.Prefab);
    view.SetPrefabId(_configs.Ufo.Prefab.GetInstanceID());

    // 3. ViewModel + Bindings
    var vm = new UfoViewModel();
    var bind = new EventBindingContext();
    bind.From(model.Move.Position).To(vm.Position);
    view.Connect(vm);
    bind.InvokeAll();

    // 4. Register
    _modelToView[model] = view;
    _modelToBind[model] = bind;
    _modelToGo[model] = view.gameObject;
    _goToModel[view.gameObject] = model;

    return model;
}
```

4. В методе Release() добавить UfoVisual ветку (после AsteroidVisual ветки):
```csharp
else if (view is UfoVisual uv) { uv.Dispose(); }
```

ВАЖНО: Для `model.SetGun(...)` — если в UfoBigModel нет метода SetGun, использовать reflection или добавить метод в UfoBigModel. Проверить UfoBigModel.cs: если `Gun { get; private set; }`, добавить публичный метод:
```csharp
public void SetGun(GunComponent gun) { Gun = gun; }
```
Если уже есть — не дублировать.

Ключевые решения:
- Large UFO: только MoveSystem + GunSystem (через GroupCreator.Visit)
- Small UFO: MoveSystem + GunSystem + ShootToSystem + MoveToSystem (через is UfoModel check)
- GunComponent создаётся через конструктор с config: `new GunComponent(_configs.UfoBig.Gun)`
- gun.Config null → GunSystem пропустит обновление (защита Pitfall 1 из RESEARCH.md)
  </action>
  <verify>
    <automated>grep "GetSystem<GunSystem>.*Add" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Model.cs && grep "GetSystem<ShootToSystem>" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Model/Model.cs && grep "CreateUfoBig\|CreateUfoSmall" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/EntitiesCatalog.cs && grep "RegisterPrefab.*UfoBig\|_configs.UfoBig.Prefab" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/EntitiesCatalog.cs</automated>
  </verify>
  <acceptance_criteria>
    - Model.cs: Visit(UfoBigModel) НЕ содержит "/* Phase 4-5 */"
    - Model.cs: содержит "GetSystem<GunSystem>().Add(model, model.Gun)"
    - Model.cs: содержит "GetSystem<ShootToSystem>().Add"
    - Model.cs: содержит "if (model is UfoModel ufo)"
    - EntitiesCatalog.cs: содержит "public UfoBigModel CreateUfoBig("
    - EntitiesCatalog.cs: содержит "public UfoModel CreateUfoSmall("
    - EntitiesCatalog.cs: Connect() содержит "RegisterPrefab(_configs.UfoBig.Prefab)"
    - EntitiesCatalog.cs: Release() содержит "UfoVisual"
    - mcp__mcp-unity__compile завершается без ошибок
  </acceptance_criteria>
  <done>UFO полностью интегрирован в ECS. EntitiesCatalog умеет создавать и удалять UFO.</done>
</task>

</tasks>

<verification>
- mcp__mcp-unity__compile — компиляция без ошибок
- grep проверки по acceptance_criteria всех задач
</verification>

<success_criteria>
- MoveToSystem и ShootToSystem реализованы (не заглушки)
- GroupCreator.Visit(UfoBigModel) регистрирует UFO в системах
- EntitiesCatalog создаёт и освобождает UFO по паттерну астероидов
- Проект компилируется
</success_criteria>

<output>
После выполнения создать `.planning/phases/05-ufo-progression-polish/05-03-SUMMARY.md`
</output>
