---
phase: 05-ufo-progression-polish
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - Assets/Scripts/Application/Game.cs
  - Assets/Scripts/Application/Application.cs
autonomous: true
requirements: [UFO-01, UFO-02, UFO-03, UFO-04, UFO-05, UFO-06, PROG-06]

must_haves:
  truths:
    - "Large UFO спаунится случайно каждые 25-40 сек с левого или правого края экрана"
    - "Small UFO спаунится после набора 10000 очков (вместо Large или как замена)"
    - "Не более одного UFO одновременно (_ufoActive флаг)"
    - "UFO исчезает после пересечения всего экрана (UFO-05) — не ходит по кругу вечно"
    - "Large UFO стреляет в случайном направлении через GunSystem (UFO-03)"
    - "Small UFO стреляет точно в корабль через ShootToSystem + OnShoot callback (UFO-02)"
    - "Баннер WAVE N появляется на 2.5 секунды при каждом старте волны (PROG-06)"
    - "Application передаёт ShowWaveBanner/HideWaveBanner от Game до HudVisual"
  artifacts:
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "ScheduleUfoSpawn, SpawnUfoBig, SpawnUfoSmall, CheckUfoExit, OnUfoCollided, ShowWaveBanner, Update(dt)"
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "OnUpdate вызывает _game.Update(dt), передаёт wave banner callbacks"
  key_links:
    - from: "Assets/Scripts/Application/Game.cs"
      to: "Assets/Scripts/Application/EntitiesCatalog.cs"
      via: "_catalog.CreateUfoBig() / CreateUfoSmall()"
      pattern: "_catalog\\.CreateUfo"
    - from: "Assets/Scripts/Application/Game.cs"
      to: "Assets/Scripts/View/HudVisual.cs"
      via: "_onWaveBannerShow?.Invoke(wave) → Application → _hudVisual.ShowWaveBanner(wave)"
      pattern: "ShowWaveBanner"
    - from: "Assets/Scripts/Application/Application.cs"
      to: "Assets/Scripts/Application/Game.cs"
      via: "_game.Update(deltaTime) в OnUpdate"
      pattern: "_game\\.Update"
---

<objective>
Реализовать всю Game-логику UFO и HUD баннера волны: спаун/управление UFO в Game.cs, передача Update и callbacks через Application.cs.

Purpose: Plan 03 создал инфраструктуру. Этот план — финальная wire-up: Game.cs получает логику спауна, стрельбы, уничтожения UFO и показа баннера волны.

Output: Полностью играбельные UFO. Баннер волны виден при каждом StartWave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ufo-progression-polish/05-RESEARCH.md
@.planning/phases/05-ufo-progression-polish/05-03-SUMMARY.md

<interfaces>
<!-- Game.cs текущее состояние — ключевые методы -->
```csharp
public class Game
{
    private GameData _configs;
    private EntitiesCatalog _catalog;
    private Model _model;
    private PlayerInput _input;
    private Action _onGameOver;
    private Action<int, int> _onScoreChanged;

    private ShipModel _ship;
    private ShipViewModel _shipViewModel;
    private bool _isRunning;

    // Поля прогрессии
    private int _waveNumber;
    private int _lives;
    private int _highScore;
    private int _asteroidCount;
    private int _nextBonusLifeScore;

    public void Connect(GameData configs, EntitiesCatalog catalog, Model model,
                        PlayerInput input, Action onGameOver, Action<int, int> onScoreChanged = null) { ... }
    public void Start() { ... } // вызывает StartWave()
    public void Stop() { ... }
    public void Restart() { ... }
    private void StartWave() { _waveNumber++; /* спаун астероидов */ }
    private void OnEntityDestroyed(IGameEntityModel model) { ... }
    private void RespawnShip() { ... }
    public void Dispose() { ... }
}
```

<!-- Application.cs — OnUpdate и OnGameStart -->
```csharp
private void OnUpdate(float deltaTime)
{
    _model?.Update(deltaTime);
    // ДОБАВИТЬ: _game?.Update(deltaTime);
}

private void OnGameStart()
{
    // ...
    _game.Connect(_configs, _catalog, _model, _input, OnGameOver, OnScoreChanged);
    _game.Start();
    // ДОБАВИТЬ: передача wave banner callbacks
}
```

<!-- EntitiesCatalog новые методы (после Plan 03) -->
```csharp
public UfoBigModel CreateUfoBig(Vector2 position, Vector2 direction, float speed) { ... }
public UfoModel CreateUfoSmall(Vector2 position, Vector2 direction, float speed) { ... }
```

<!-- ActionScheduler паттерн из Game.cs -->
```csharp
_model.ActionScheduler.Schedule(2f, RespawnShip);
_model.ActionScheduler.Schedule(delay, callback);
```

<!-- GunComponent.OnShooting — паттерн для Large UFO стрельбы -->
```csharp
_ship.Gun.OnShooting = OnUserGunShooting; // в Game.Start()
// По аналогии для UFO:
ufo.Gun.OnShooting = OnUfoGunShooting;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Реализовать UFO логику в Game.cs</name>
  <files>Assets/Scripts/Application/Game.cs</files>
  <read_first>
    - Assets/Scripts/Application/Game.cs (ПОЛНЫЙ файл — критично прочитать текущее состояние)
    - Assets/Scripts/Application/EntitiesCatalog.cs (сигнатуры CreateUfoBig/CreateUfoSmall)
    - Assets/Scripts/View/UfoVisual.cs (UfoViewModel.OnCollision для подписки)
    - Assets/Scripts/Configs/GameData.cs (GameData.UfoBig.Speed, GameData.Ufo.Speed)
  </read_first>
  <action>
Добавить в Game.cs следующие поля и методы. Не удалять существующий код — только добавлять.

**1. Новые поля (в секцию приватных полей):**
```csharp
// UFO состояние (UFO-06: не более одного одновременно)
private bool _ufoActive;
private UfoBigModel _activeUfo;
private bool _ufoStartedFromLeft;
private bool _activeUfoWrapped;

// Wave banner callbacks (PROG-06)
private Action<int> _onWaveBannerShow;  // вызывается с номером волны
private Action _onWaveBannerHide;
```

**2. Расширить метод Connect() — добавить wave banner callbacks в параметры:**

Изменить сигнатуру (добавить два последних параметра):
```csharp
public void Connect(GameData configs, EntitiesCatalog catalog, Model model,
                    PlayerInput input, Action onGameOver, Action<int, int> onScoreChanged = null,
                    Action<int> onWaveBannerShow = null, Action onWaveBannerHide = null)
{
    // существующий код ...
    _onWaveBannerShow = onWaveBannerShow;
    _onWaveBannerHide = onWaveBannerHide;
}
```

**3. Расширить метод Start() — добавить ScheduleUfoSpawn после StartWave():**
```csharp
// В конце Start(), после _onScoreChanged?.Invoke:
ScheduleUfoSpawn();
```

**4. Расширить метод Stop() — добавить сброс UFO флагов:**
```csharp
// В конце Stop():
_ufoActive = false;
_activeUfo = null;
```

**5. Расширить метод StartWave() — добавить ShowWaveBanner:**
```csharp
// В начале StartWave(), после _waveNumber++:
ShowWaveBanner();
```

**6. Расширить метод OnEntityDestroyed() — добавить обработку UFO:**
```csharp
// В методе OnEntityDestroyed, после блока if (model is ShipModel):
if (model is UfoBigModel ufoModel)
{
    var data = (ufoModel is UfoModel) ? _configs.Ufo : _configs.UfoBig;
    _model.Score += data.Score;

    // Проверить экстра-жизнь
    while (_model.Score >= _nextBonusLifeScore && _lives < 6)
    {
        _lives++;
        _nextBonusLifeScore += 10000;
    }

    _ufoActive = false;
    _activeUfo = null;
    _onScoreChanged?.Invoke(_model.Score, _lives);
}
```

**7. Добавить новый публичный метод Update(float dt):**
```csharp
public void Update(float dt)
{
    if (!_isRunning) { return; }
    CheckUfoExit();
    // Small UFO: обновлять цель (MoveTo.Target = позиция корабля)
    UpdateUfoTarget();
}
```

**8. Добавить приватные UFO методы:**

```csharp
private void ScheduleUfoSpawn()
{
    var delay = UnityEngine.Random.Range(25f, 40f);
    _model.ActionScheduler.Schedule(delay, TrySpawnUfo);
}

private void TrySpawnUfo()
{
    if (!_isRunning || _ufoActive) { return; }
    // Small UFO появляется при score >= 10000 (UFO-02)
    if (_model.Score >= 10000)
    {
        SpawnUfoSmall();
    }
    else
    {
        SpawnUfoBig();
    }
    ScheduleUfoSpawn(); // запланировать следующий спаун
}

private void SpawnUfoBig()
{
    var gameArea = _model.GameArea;
    var fromLeft = UnityEngine.Random.value > 0.5f;
    var x = fromLeft ? -gameArea.x / 2f : gameArea.x / 2f;
    var y = UnityEngine.Random.Range(-gameArea.y / 2f, gameArea.y / 2f);
    var pos = new Vector2(x, y);
    var dir = fromLeft ? Vector2.right : Vector2.left;

    var ufo = _catalog.CreateUfoBig(pos, dir, _configs.UfoBig.Speed);
    if (ufo == null) { return; }

    ufo.Gun.OnShooting = OnUfoGunShooting;
    // Инициализировать случайную стрельбу через GunSystem: установить Shooting периодически
    // GunSystem проверяет gun.Shooting каждый кадр — устанавливаем через ActionScheduler
    ScheduleUfoShoot(ufo);

    _activeUfo = ufo;
    _ufoStartedFromLeft = fromLeft;
    _activeUfoWrapped = false;
    _ufoActive = true;

    BindUfoCollision(ufo);
}

private void SpawnUfoSmall()
{
    var gameArea = _model.GameArea;
    var fromLeft = UnityEngine.Random.value > 0.5f;
    var x = fromLeft ? -gameArea.x / 2f : gameArea.x / 2f;
    var y = UnityEngine.Random.Range(-gameArea.y / 2f, gameArea.y / 2f);
    var pos = new Vector2(x, y);
    var dir = fromLeft ? Vector2.right : Vector2.left;

    var ufo = _catalog.CreateUfoSmall(pos, dir, _configs.Ufo.Speed);
    if (ufo == null) { return; }

    // ShootToSystem вызовет OnShoot — назначить callback
    ufo.ShootTo.OnShoot = OnUfoSmallShoot;

    _activeUfo = ufo;
    _ufoStartedFromLeft = fromLeft;
    _activeUfoWrapped = false;
    _ufoActive = true;

    BindUfoCollision(ufo);
}

private void BindUfoCollision(UfoBigModel ufo)
{
    if (_catalog.GetViewByModel(ufo) is UfoVisual ufoVisual)
    {
        ufoVisual.ViewModel.OnCollision = col => OnUfoCollided(ufo, col);
    }
}

private void ScheduleUfoShoot(UfoBigModel ufo)
{
    // Случайная стрельба Large UFO через GunSystem
    var interval = _configs.UfoBig.ShootDurationSec > 0f ? _configs.UfoBig.ShootDurationSec : 1.5f;
    _model.ActionScheduler.Schedule(interval, () => {
        if (ufo == null || ufo.IsDead() || !_ufoActive) { return; }
        ufo.Gun.Shooting = true;
        ScheduleUfoShoot(ufo);
    });
}

private void OnUfoGunShooting(GunComponent gun)
{
    // Пуля Large UFO в случайном направлении (UFO-03)
    if (_activeUfo == null || _activeUfo.IsDead()) { return; }
    var pos = _activeUfo.Move.Position.Value;
    var randomDir = UnityEngine.Random.insideUnitCircle.normalized;
    var velocity = randomDir * _configs.Bullet.Speed;
    _catalog.CreateBullet(pos, velocity, isEnemy: true);
}

private void OnUfoSmallShoot(UfoModel ufo)
{
    // Пуля Small UFO точно в корабль (UFO-02)
    if (ufo == null || ufo.IsDead() || _ship == null || _ship.IsDead()) { return; }
    var ufoPos = ufo.Move.Position.Value;
    var shipPos = _ship.Move.Position.Value;
    var dir = (shipPos - ufoPos).normalized;
    var velocity = dir * _configs.Bullet.Speed;
    _catalog.CreateBullet(ufoPos, velocity, isEnemy: true);
}

private void OnUfoCollided(UfoBigModel ufo, Collision2D col)
{
    if (!_isRunning || ufo.IsDead()) { return; }
    // UFO уничтожается только от пули игрока
    var hitModel = _catalog.GetModelByGo(col.gameObject);
    if (hitModel is not BulletModel hitBullet || hitBullet.IsEnemy) { return; }

    hitBullet.Kill();
    ufo.Kill();
    // Очки и UFO cleanup — обрабатываются в OnEntityDestroyed
}

private void CheckUfoExit()
{
    if (_activeUfo == null || _activeUfo.IsDead()) { return; }
    var pos = _activeUfo.Move.Position.Value;
    var halfW = _model.GameArea.x / 2f;

    // Отслеживать первый wrap-around (UFO пересёк экран и оказался с другой стороны)
    if (!_activeUfoWrapped)
    {
        // Если UFO вышел за противоположный от старта край → wrap произошёл
        var crossedOpposite = _ufoStartedFromLeft ? pos.x >= halfW - 0.1f : pos.x <= -halfW + 0.1f;
        if (crossedOpposite)
        {
            _activeUfoWrapped = true;
        }
        return;
    }

    // После wrap: UFO дошёл до края с которого стартовал → уничтожить без очков (UFO-05)
    var reachedStart = _ufoStartedFromLeft ? pos.x <= -halfW + 0.5f : pos.x >= halfW - 0.5f;
    if (reachedStart)
    {
        _activeUfo.Kill();
        _ufoActive = false;
        _activeUfo = null;
    }
}

private void UpdateUfoTarget()
{
    // Обновить Target для Small UFO (MoveToSystem будет использовать его)
    if (_activeUfo is UfoModel ufoSmall && !ufoSmall.IsDead() && _ship != null && !_ship.IsDead())
    {
        ufoSmall.MoveTo.Target = _ship.Move.Position.Value;
    }
}

private void ShowWaveBanner()
{
    _onWaveBannerShow?.Invoke(_waveNumber);
    _model.ActionScheduler.Schedule(2.5f, HideWaveBanner);
}

private void HideWaveBanner()
{
    if (!_isRunning) { return; }
    _onWaveBannerHide?.Invoke();
}
```

Итого изменения в Game.cs:
- 5 новых приватных полей (_ufoActive, _activeUfo, _ufoStartedFromLeft, _activeUfoWrapped, _onWaveBannerShow, _onWaveBannerHide)
- Расширена сигнатура Connect() (2 опциональных параметра)
- Расширен Start() — вызов ScheduleUfoSpawn() и ShowWaveBanner() из StartWave()
- Расширен Stop() — сброс UFO флагов
- Расширен StartWave() — ShowWaveBanner()
- Расширен OnEntityDestroyed() — UFO ветка
- Новый публичный Update(float dt)
- 10 новых приватных методов
  </action>
  <verify>
    <automated>grep "ScheduleUfoSpawn\|SpawnUfoBig\|SpawnUfoSmall\|CheckUfoExit" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs && grep "_ufoActive" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs && grep "ShowWaveBanner\|HideWaveBanner" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs && grep "public void Update" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Game.cs</automated>
  </verify>
  <acceptance_criteria>
    - Game.cs содержит "private bool _ufoActive"
    - Game.cs содержит "private UfoBigModel _activeUfo"
    - Game.cs содержит "ScheduleUfoSpawn()"
    - Game.cs содержит "SpawnUfoBig()" и "SpawnUfoSmall()"
    - Game.cs содержит "CheckUfoExit()"
    - Game.cs содержит "public void Update(float dt)"
    - Game.cs содержит "ShowWaveBanner()" и "HideWaveBanner()"
    - Game.cs содержит "OnUfoGunShooting" и "OnUfoSmallShoot"
    - Game.cs содержит "isEnemy: true" в CreateBullet вызовах для UFO
    - Существующие методы Start, Stop, Restart, OnEntityDestroyed сохранены
  </acceptance_criteria>
  <done>Game.cs содержит полную логику UFO и баннера волны.</done>
</task>

<task type="auto">
  <name>Task 2: Расширить Application.cs — _game.Update + wave banner wire-up</name>
  <files>Assets/Scripts/Application/Application.cs</files>
  <read_first>
    - Assets/Scripts/Application/Application.cs (ПОЛНЫЙ файл — текущее состояние)
    - Assets/Scripts/Application/Game.cs (после Task 1 — новая сигнатура Connect)
    - Assets/Scripts/View/HudVisual.cs (ShowWaveBanner, HideWaveBanner методы)
  </read_first>
  <action>
Два точечных изменения в Application.cs:

**1. В OnUpdate — добавить вызов _game.Update(deltaTime):**

Найти:
```csharp
private void OnUpdate(float deltaTime)
{
    _model?.Update(deltaTime);
}
```

Заменить на:
```csharp
private void OnUpdate(float deltaTime)
{
    _model?.Update(deltaTime);
    _game?.Update(deltaTime);
}
```

**2. В OnGameStart — передать wave banner callbacks в Game.Connect():**

Найти:
```csharp
_game.Connect(_configs, _catalog, _model, _input, OnGameOver, OnScoreChanged);
```

Заменить на:
```csharp
_game.Connect(_configs, _catalog, _model, _input, OnGameOver, OnScoreChanged,
    onWaveBannerShow: wave => _hudVisual?.ShowWaveBanner(wave),
    onWaveBannerHide: () => _hudVisual?.HideWaveBanner());
```

Это единственные изменения в Application.cs. Весь остальной код остаётся без изменений.
  </action>
  <verify>
    <automated>grep "_game?.Update" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Application.cs && grep "onWaveBannerShow\|ShowWaveBanner" /Users/selstrom/work/projects/asteroids-specs/Assets/Scripts/Application/Application.cs</automated>
  </verify>
  <acceptance_criteria>
    - Application.cs содержит "_game?.Update(deltaTime)"
    - Application.cs содержит "onWaveBannerShow:"
    - Application.cs содержит "ShowWaveBanner(wave)"
    - Application.cs содержит "HideWaveBanner()"
    - Application.cs: OnGameStart() сохраняет остальные вызовы (_gameScreen.Connect и т.д.)
    - mcp__mcp-unity__compile завершается без ошибок
  </acceptance_criteria>
  <done>Application.cs подключён к Game.Update и передаёт wave banner callbacks в HudVisual.</done>
</task>

</tasks>

<verification>
1. mcp__mcp-unity__compile — компиляция без ошибок
2. mcp__mcp-unity__play — запуск Play Mode
3. Дождаться 25-40 секунд — Large UFO должен появиться с края экрана
4. При старте новой волны должен появиться баннер "WAVE N" на ~2.5 секунды
5. mcp__mcp-unity__stop
</verification>

<success_criteria>
- Game.cs содержит полную UFO логику (спаун, стрельба, уничтожение, баннер волны)
- Application.cs передаёт wave banner callbacks и вызывает Game.Update
- Проект компилируется без ошибок
- В Play Mode: UFO появляется, стреляет, исчезает при пересечении экрана
- Баннер WAVE N показывается при каждом старте волны
</success_criteria>

<output>
После выполнения создать `.planning/phases/05-ufo-progression-polish/05-04-SUMMARY.md`
</output>
