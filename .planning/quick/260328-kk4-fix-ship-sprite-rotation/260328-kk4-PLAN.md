---
phase: quick
plan: 260328-kk4
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Model/Components/MoveComponent.cs
  - Assets/Scripts/View/ShipVisual.cs
  - Assets/Scripts/Application/EntitiesCatalog.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Спрайт корабля поворачивается вместе с изменением направления движения"
    - "При старте корабль смотрит вверх (Direction=Vector2.up → угол 90°)"
    - "Поворот плавный — обновляется каждый кадр синхронно с физикой"
  artifacts:
    - path: "Assets/Scripts/Model/Components/MoveComponent.cs"
      provides: "Direction как ObservableValue<Vector2>"
    - path: "Assets/Scripts/View/ShipVisual.cs"
      provides: "Биндинг Rotation → transform.rotation"
    - path: "Assets/Scripts/Application/EntitiesCatalog.cs"
      provides: "bind.From(model.Move.Direction).To(vm.Rotation)"
  key_links:
    - from: "RotateSystem"
      to: "MoveComponent.Direction"
      via: "ObservableValue.Value = newDir"
      pattern: "ship.Move.Direction.Value ="
    - from: "EntitiesCatalog.CreateShip"
      to: "ShipViewModel.Rotation"
      via: "bind.From(model.Move.Direction)"
      pattern: "bind.From.*Direction.*Rotation"
    - from: "ShipVisual.OnConnected"
      to: "transform.rotation"
      via: "ViewModel.Rotation.Connect"
      pattern: "Rotation.Connect"
---

<objective>
Исправить отсутствие поворота спрайта корабля: спрайт всегда смотрит в одну сторону независимо от направления движения.

Purpose: Корабль в Asteroids поворачивается стрелками — это ключевой механизм управления. Без поворота игра неиграбельна.
Output: Спрайт корабля поворачивается синхронно с изменением Move.Direction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Сделать MoveComponent.Direction реактивным и обновить RotateSystem</name>
  <files>
    Assets/Scripts/Model/Components/MoveComponent.cs
    Assets/Scripts/Model/Systems/RotateSystem.cs
    Assets/Scripts/Model/Systems/MoveSystem.cs
    Assets/Scripts/Model/Systems/ThrustSystem.cs
    Assets/Scripts/Application/EntitiesCatalog.cs
    Assets/Scripts/Model/Entities/ShipModel.cs
    Assets/Scripts/Application/Game.cs
  </files>
  <action>
**Шаг 1 — MoveComponent.cs:** Заменить `public Vector2 Direction;` на `public ObservableValue&lt;Vector2&gt; Direction = new();`. ObservableValue уже импортирован через `using Shtl.Mvvm`.

**Шаг 2 — Исправить все обращения к Direction:** В проекте `Direction` читается и пишется как plain field — теперь нужно использовать `.Value`:

- `RotateSystem.cs` (строки 17-19): `ship.Move.Direction.y` → `ship.Move.Direction.Value.y`, `ship.Move.Direction.x` → `ship.Move.Direction.Value.x`, `ship.Move.Direction = ...` → `ship.Move.Direction.Value = ...`
- `MoveSystem.cs`: найти все `entity.Move.Direction` и добавить `.Value`
- `ThrustSystem.cs`: найти все `*.Move.Direction` и добавить `.Value`
- `EntitiesCatalog.cs` строки 90, 91, 124-125: `model.Move.Direction = ...` → `model.Move.Direction.Value = ...`
- `Game.cs` строки 173-176: `_ship.Move.Direction` → `_ship.Move.Direction.Value`
- `ShipModel.cs` строка 42: `Move.Direction = Vector2.up` → `Move.Direction.Value = Vector2.up`

Проверь grep по всему Assets/ на `\.Direction` (без `.Value`) чтобы не пропустить.

**Шаг 3 — EntitiesCatalog.CreateShip (строка ~62):** Добавить биндинг угла поворота:

```csharp
// Биндинг Model.Direction → ViewModel.Rotation (угол в градусах)
bind.From(model.Move.Direction).To(val =>
    vm.Rotation.Value = Mathf.Atan2(val.y, val.x) * Mathf.Rad2Deg);
```

Убедись что `vm.Rotation` уже объявлен в ShipViewModel (см. Задачу 2, которую нужно выполнить вместе с этой).
  </action>
  <verify>
    Проект компилируется без ошибок: в Unity Console нет compile errors после сохранения файлов.
  </verify>
  <done>
    MoveComponent.Direction — ObservableValue&lt;Vector2&gt;. Все обращения используют .Value. EntitiesCatalog биндит Direction → Rotation.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Добавить Rotation в ShipViewModel и биндинг в ShipVisual</name>
  <files>
    Assets/Scripts/View/ShipVisual.cs
  </files>
  <action>
**Шаг 1 — ShipViewModel (в том же файле ShipVisual.cs):** Добавить поле Rotation:

```csharp
public ReactiveValue&lt;float&gt; Rotation = new();
```

Добавить после `public ReactiveValue&lt;bool&gt; IsVisible = new(true);`.

**Шаг 2 — ShipVisual.OnConnected:** Добавить биндинг угла поворота на transform:

```csharp
// Биндинг поворота: Rotation (градусы) → Transform.rotation
ViewModel.Rotation.Connect(angleDeg =>
    transform.rotation = Quaternion.Euler(0f, 0f, angleDeg - 90f));
```

Вычитаем 90° потому что спрайт корабля смотрит вверх (Vector2.up = 90°), а Quaternion.Euler(0,0,0) = вправо. Таким образом при Direction=Vector2.up → angleDeg=90° → Euler(0,0,0°) = нейтраль спрайта.

Добавить биндинг в конец блока `OnConnected`, после биндинга IsVisible.

**Важно:** Проверь ориентацию спрайта. Если спрайт корабля нарисован смотрящим вправо (а не вверх), используй `angleDeg` без вычитания. Если смотрит вниз — `angleDeg + 90f`. Ориентируйся по результату в Play Mode.
  </action>
  <verify>
    В Unity Play Mode: корабль при старте смотрит вверх. При зажатой стрелке влево/вправо — спрайт плавно вращается. Пуля летит туда, куда смотрит нос корабля.
  </verify>
  <done>
    ShipViewModel содержит Rotation. ShipVisual биндит Rotation на transform.rotation. Корабль визуально поворачивается.
  </done>
</task>

</tasks>

<verification>
1. Компиляция без ошибок (Unity Console чиста)
2. В Play Mode: нажать A/D (или стрелки) — корабль поворачивается
3. Выстрел летит в сторону носа корабля
4. При Reset (новая игра) корабль снова смотрит вверх
</verification>

<success_criteria>
Спрайт корабля поворачивается при нажатии клавиш вращения. Пуля летит в направлении носа корабля.
</success_criteria>

<output>
После выполнения создать `.planning/quick/260328-kk4-fix-ship-sprite-rotation/260328-kk4-SUMMARY.md`
</output>
