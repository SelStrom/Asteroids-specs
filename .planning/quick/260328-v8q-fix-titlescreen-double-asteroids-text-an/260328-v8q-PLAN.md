---
phase: quick-260328-v8q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scenes/Main.unity
  - Assets/Editor/Phase6Setup.cs
  - Assets/Media/prefabs/vfx_blow.prefab
autonomous: true
requirements: []
must_haves:
  truths:
    - "TitleScreen показывает ровно одну надпись ASTEROIDS"
    - "Запуск игры не выдаёт ошибку Property <noninit> already exists"
  artifacts:
    - path: "Assets/Scenes/Main.unity"
      provides: "Сцена без дублирующего Title GameObject"
    - path: "Assets/Editor/Phase6Setup.cs"
      provides: "UpdateTitleScreen() удаляет обе версии title-объектов"
    - path: "Assets/Media/prefabs/vfx_blow.prefab"
      provides: "ParticleSystem без TextureSheetAnimation"
  key_links:
    - from: "Assets/Scenes/Main.unity"
      to: "TitleScreen m_Children"
      via: "удаление fileID: 1580767957 из m_Children"
      pattern: "1580767957"
---

<objective>
Исправить два бага Phase 6: дублирующийся заголовок ASTEROIDS на TitleScreen и ошибку ParticleSystem noninit property в vfx_blow.prefab.

Purpose: Устранить визуальный дефект и runtime-ошибку, мешающие верификации Phase 6.
Output: Чистая сцена с одним заголовком и корректный prefab vfx_blow без TSA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Удалить дублирующий Title GameObject из Main.unity</name>
  <files>Assets/Scenes/Main.unity</files>
  <action>
Открыть `Assets/Scenes/Main.unity` как текстовый YAML.

Найти и удалить три блока GameObject/Component с fileID-ами дублирующего Title объекта:

1. Удалить из списка `m_Children` объекта TitleScreen строку `{fileID: 1580767957}`.
   TitleScreen — это GameObject с дочерними: 1580767957 (Title), play_button, title_text (1101161491), leaderboard_button.

2. Удалить полностью YAML-блок GameObject с fileID 1580767956 (сам "Title" GameObject). Блок начинается с `--- !u!1 &1580767956` и заканчивается перед следующим `---`.

3. Удалить полностью YAML-блоки компонент дублирующего Title: fileID 1580767957, 1580767958, 1580767959. Каждый блок начинается с `--- !u!{type} &{fileID}` и заканчивается перед следующим `---`.

Оставить нетронутыми: `title_text` (fileID 1101161491) и все остальные дочерние объекты TitleScreen.

После правки убедиться что в файле нет ни одного упоминания fileID 1580767956, 1580767957, 1580767958, 1580767959.
  </action>
  <verify>
    <automated>grep -c "1580767956\|1580767957\|1580767958\|1580767959" /Users/selstrom/work/projects/asteroids-specs/Assets/Scenes/Main.unity || echo "0 — OK, fileIDs удалены"</automated>
  </verify>
  <done>Файл Main.unity не содержит fileID удалённого Title GameObject и его компонент. grep возвращает 0 совпадений.</done>
</task>

<task type="auto">
  <name>Задача 2: Исправить Phase6Setup.cs — удалять "Title" при UpdateTitleScreen</name>
  <files>Assets/Editor/Phase6Setup.cs</files>
  <action>
Открыть `Assets/Editor/Phase6Setup.cs`.

В методе `UpdateTitleScreen()` найти блок, который удаляет существующие дочерние объекты TitleScreen (ищи DestroyImmediate или удаление по имени "title_text").

Добавить удаление дочернего объекта с именем "Title" (старый TMP из Phase3Setup) рядом с существующей логикой удаления. Например:

```csharp
// Удалить старый Title (Phase3Setup legacy)
var oldTitle = _titleScreen.Find("Title");
if (oldTitle != null)
{
    DestroyImmediate(oldTitle.gameObject);
}
```

Добавить этот блок ДО создания нового title_text, чтобы повторный запуск Phase6Setup не оставлял двойника.
  </action>
  <verify>
    <automated>grep -n '"Title"' /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase6Setup.cs</automated>
  </verify>
  <done>Phase6Setup.cs содержит поиск и DestroyImmediate для дочернего "Title" внутри UpdateTitleScreen().</done>
</task>

<task type="auto">
  <name>Задача 3: Убрать TextureSheetAnimation из Phase6Setup.cs и vfx_blow.prefab</name>
  <files>Assets/Editor/Phase6Setup.cs, Assets/Media/prefabs/vfx_blow.prefab</files>
  <action>
**Часть А — Phase6Setup.cs:**

Открыть `Assets/Editor/Phase6Setup.cs`. В методе `CreateVfxBlowPrefab()` найти блок TextureSheetAnimation:
```csharp
var tsa = ps.textureSheetAnimation;
tsa.enabled = true;
tsa.mode = ParticleSystemAnimationMode.Sprites;
tsa.AddSprite(bulletSprite);
```
Удалить весь этот блок целиком. Оставить только назначение материала (`psr.material = mat`) для отображения текстуры через материал.

**Часть Б — vfx_blow.prefab:**

Открыть `Assets/Media/prefabs/vfx_blow.prefab` как текстовый YAML.

Найти секцию `TextureSheetAnimation` внутри блока ParticleSystem (поле `TextureSheetAnimation:`). Установить `enabled: 0` для этого модуля. Если поле выглядит как:
```yaml
TextureSheetAnimation:
  serializedVersion: N
  enabled: 1
  ...
```
Изменить `enabled: 1` на `enabled: 0`.

Не удалять весь блок, только отключить (Unity ожидает наличие всех модулей в YAML).
  </action>
  <verify>
    <automated>grep -n "textureSheetAnimation\|AddSprite" /Users/selstrom/work/projects/asteroids-specs/Assets/Editor/Phase6Setup.cs; grep -n "enabled:" /Users/selstrom/work/projects/asteroids-specs/Assets/Media/prefabs/vfx_blow.prefab | head -20</automated>
  </verify>
  <done>Phase6Setup.cs не содержит вызовов textureSheetAnimation/AddSprite. vfx_blow.prefab содержит TextureSheetAnimation с enabled: 0.</done>
</task>

</tasks>

<verification>
После выполнения всех трёх задач:
1. `grep "1580767956\|1580767957\|1580767958\|1580767959" Assets/Scenes/Main.unity` — должно вернуть 0 результатов
2. `grep '"Title"' Assets/Editor/Phase6Setup.cs` — должно найти строку с поиском и DestroyImmediate
3. `grep "textureSheetAnimation\|AddSprite" Assets/Editor/Phase6Setup.cs` — должно вернуть 0 результатов
4. В Unity: TitleScreen показывает одну надпись ASTEROIDS, консоль не содержит ошибки "noninit property"
</verification>

<success_criteria>
- TitleScreen в Unity Editor показывает ровно одну надпись "ASTEROIDS" (fontSize=96, title_text)
- В Console Unity нет ошибки `Property noninit already exists in the property sheet`
- Повторный запуск Phase6Setup (меню GSD > Phase6Setup) не создаёт дубликат Title
</success_criteria>

<output>
После выполнения добавить запись в STATE.md раздел "Quick Tasks Completed":
| 260328-v8q | Fix TitleScreen double ASTEROIDS text and ParticleSystem noninit error | 2026-03-28 | {commit} | [260328-v8q-fix-titlescreen-double-asteroids-text-an](./quick/260328-v8q-fix-titlescreen-double-asteroids-text-an/) |
</output>
