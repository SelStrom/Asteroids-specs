---
quick_id: 260327-p00
date: 2026-03-27
---

# Quick Task 260327-p00: Increase MCP server timeouts and add error handling

**Problem:** `AbortSignal.timeout(5000)` — 5 seconds too short. Unity's `EditorApplication.delayCall` + Unity API calls can take several seconds. All tools fail with "operation was aborted due to timeout".

**Fix:**
1. Extract common `callBridge()` helper with try-catch
2. Default timeout 15s, compile/open_scene/import_asset → 30s
3. On TimeoutError: return friendly JSON error instead of throwing
4. On connection error: return JSON with error message
5. Rebuild dist/index.js
